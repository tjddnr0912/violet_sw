# ROADMAP — Stage C 이후 향후 과제 (vitamin)

> **갱신:** 2026-06-22 — **§5 보강/성능 하드닝 백로그 신설**. 11개 서브시스템 전수 적대 재감사(46-에이전트 워크플로, 이미-구현 항목 한정)로 **32 confirmed finding**(perf 16·robustness 15, high 3/medium 7/low 21) 정리 → 개발 플랜화. silent-wrong/미구현은 제외(스코프=hardening), 측정-기각 dead-perf 재제안 0건. **단일 최대 리스크 = STAGED-DROP**(staged velab→vrun 사이드카 12/25 누락 → class/재귀-automatic-fn silent-wrong). 표=**[§5](#5-보강성능-하드닝-백로그-2026-06-22-적대-리뷰)**.
> **갱신:** 2026-06-10 — **7축 감사**(Gemini-fix 검토/spec-gap/sim-engine/front-end/메모리/운용성/병렬화) 반영: correctness 트랙 재개(§B), §D 사실관계 정정, **병렬화 트랙 §E 신설**. 잔여 항목의 단일 트래커 = [`REMAINING_WORK.md`](REMAINING_WORK.md).
>
> **동일자 후속(HEAD `0945dfe`, 566 green): 감사 발견 항목 일괄 처리 완료** — P0 정확성 9 · P1 의미론 9(severity/radix/`%m` 사이드테이블, in-body @(*), finish-flush, per-bit 멀티드라이버, E3018) · P2 운용 11/12(`--help/--version`·`--threads`·`--timeout`, VCD/델타 진단, 원자적 아티팩트, parser/array 캡, W3056 taxonomy) · P3 메모리 4(fork free-list·monitor in-place·clone 제거·native 고정스택) · P4 T0a/T0b/T1(`--threads` VCD writer 스레드, byte-identical 게이트, 실측 VCD 비중 40.9%) · native-eval follow-on(비교/시프트/DivMod/ternary/리덕션/논리 — expr-heavy VM 0.42x, eval-heavy 0.54x). **이후 P2-12 정책 소항목(`time` 64-bit unsigned 수용·`` `pragma`` 수용-무시·timescale clamp·implicit-net 명문화·same_path 회귀)과 P5 문서 동기화(-W* 미래형·예약 codes·exit 0/1/3)까지 같은 날 소진(571 green) — 트래커 완결.** 잔여 = §C/§E perf 축 + Phase-1.x.
> **2탄(같은 날, HEAD `8664627`, 611 green): perf 축 + Phase-1.x 전부 소진** — ①스케줄러축 라운드1(클럭-바운드 **≈1.85x**: "스케줄러-바운드"의 ~45%가 malloc/free였음 → 핫루프 할당 9원 제거) ②native-eval 구조 lane(select/concat/replicate — 신규 STRUCT_HEAVY 벤치 **VM 0.36x ≈2.8x**) ③vita-log 실코드화(`-Wno-*`/`-Werror=` 게이트 + exit class 2) ④filelist `-f`/`-F`(argv-레벨 전개) ⑤`vita explain` ⑥**format_version 4**(런타임 `#delay`=ExprId suspension-time 평가·`$dumpflush`/`$dumplimit`·`REGEN_GOLDEN=1` 골든 재생성 스위치) ⑦force/release(sample-once, per-net forced 플래그 — iverilog 오라클 동일 모델). **남은 트랙 = §1 잔여 표(아래) + Phase-2.**
> 직전: 2026-06-08 native-eval C4-lite 랜딩 + 멀티-top 다중 root. 현 단계 = **Stage C C1·C2 완료**(VM MVP, byte-identical) + **profile-driven 최적화 누적 ~6x**(eval-heavy 2781→461ms). 이 문서는 *여기서부터 무엇을 할지*를 트랙별로 정리한 단일 진실. perf 이력 = [`preview/18-acceleration-analysis.md`](preview/18-acceleration-analysis.md) §실측(+구 트래커 git 이력), 설계는
> [`superpowers/plans/2026-06-06-bytecode-vm-stage-c.md`](superpowers/plans/2026-06-06-bytecode-vm-stage-c.md).

---

## 0. 핵심 발견 — 왜 로드맵이 바뀌었나 (반드시 먼저 읽을 것)

Stage C는 본래 *"컴파일드 백엔드(바이트코드 VM)로 인터프리터를 이긴다"* — eval 트리워크 디스패치와
`Value` 힙할당(doc-18이 지목한 두 병목)을 native-eval(plan C3~C6)로 제거 — 였다. **프로파일링(`/usr/bin/sample`)이
이 전제를 측정으로 뒤집었다:**

- **1차 병목 = bit-serial bit-by-bit 처리**(net read/write, shift, resize) — 인터프리터·VM **공유** 경로. word化/inline로 정리해 **~6x**.
- **eval 트리워크 디스패치는 eval-light 벤치에선 ~1.5%뿐**이었다 — 이 한 점으로 "native-eval 저ROI"라 1차 결론. **그 결론은 너무 강했다(아래 정정).**

→ **2차 재평가 (2026-06-07 오후, 오너 제기로 재검토):** eval 비용은 *식 복잡도에 선형*이다. 연산자수 스윕(`t ≈ 0.39s + 0.058s×K`, R²≈1)으로 **eval 비중이 K=8에서 55%, K=16에서 70%, K=32에서 82%**임을 측정. 피연산자당 58ns 중 ~57ns가 Value 생성 + `eval_ctx` 디스패치 오버헤드(net-read ≈ literal로 확인, 환원불가 ALU ~1ns)라 레지스터 native-eval이 제거 가능(~4-6x on eval). **⇒ native-eval ROI = 워크로드 의존:**
> - **식-바운드 RTL**(넓은 ALU·CRC/crypto·깊은 조합 cone): **고ROI**, 설계당 ~2-3x. `EXPR_HEAVY`(K=16)에서 VM은 0.92x뿐 — **문장 컴파일(현 VM)은 식-바운드에 거의 무력, native-eval이 유일한 레버.**
> - **클럭/스케줄러-바운드 RTL**: eval 작음 → **저ROI**, 스케줄러 축이 답.

**결론(settled):** 당초 doc-18의 "코드젠이 진짜 가속 경로"는 **식-바운드 한정으로 옳았다.** native-eval(plan C4~C9)은 **막다른 길이 아니라 식-바운드 perf의 정당한 방향**이며, **P5 차분 게이트(compiled==interp byte동일)+iverilog 오라클이 정확성 리스크를 이미 대폭 상쇄**한다. 비용은 4-state 레지스터 머신(val+unk·width/sign 마스킹·X/Z 전파·>128bit fallback)의 고위험·다세션. 측정 상세 = doc-18 §실측 "native-eval 재평가", 영구 회귀 = `perf_baseline.rs` `EXPR_HEAVY`.

---

## 1. 트랙별 향후 과제

### A. Perf — 남은 것 (싼 윈 거의 소진)

| 항목 | 위치 | ROI | 비고 |
|---|---|---|---|
| ~~저빈도 value-op word化~~ ✅ 2026-06-11 | `eval.rs` 4종 word-parallel(`copy_bits` funnel) | 실현: STRUCT_HEAVY interp ≈1.44x | OOB-혼합 select만 bit-serial 폴백 잔존 |
| ~~`has_xz`/`arith`/`to_u128` 미세~~ | `value.rs`/`eval.rs` | — | ✅ 2026-06-11 **검사-폐기**: inline-Value 라운드 때 이미 word-parallel(마스크된 word 로드, bit 루프 0) — 남은 게 없음 |
| **스케줄러 축 — ✅ 라운드 1 완료(2026-06-10)** | `sched.rs`·`exec.rs`·`state.rs` 핫루프 할당 제거 | **실현: 클럭-바운드 ≈1.85x** | profile 결과 "스케줄러-바운드"의 ~45%가 malloc/free였음 — snapshot_prev/refresh-prev `clone_from`化, propagate 스크래치 재사용, wheel 버킷 풀, active/nba capacity 반납, `&'ir` reborrow로 Stmt/블록/Lvalue/args 클론 0, `Offsets` 인라인 enum. interp 61.8→33.4ms·VM 56.0→30.3ms, eval-heavy interp도 1.27x 부수개선. **잔여(라운드 2 후보): per-delta 전체-넷 스캔 → dirty-list**(넷 수 선형 — 대형 디자인용; 쓰기 훅+정렬로 byte-identity). 상세 = [doc-18 §실측 스케줄러축](preview/18-acceleration-analysis.md) |
| **native-eval (eval 코드젠) — 🔨 C4-lite 착수** | `native_eval.rs`(신규) + `backend.rs` `Op::EvalNative` | **식-바운드 高 / 그 외 低** | **C4-lite 랜딩(2026-06-08): ≤64bit 정수 서브셋**(Const·scalar Signal·Add/Sub/Mul·And/Or/Xor/Xnor·Not/Plus/Minus)을 VM 전용 native 레지스터로 평가, 그 외는 `eval_ctx` fallback. **expr-heavy에서 VM 0.92x→0.42x(≈2.3x), 클럭-바운드 불변(0.94x)** — 식-바운드 ~2-3x 예측 실현. 인터프리터=오라클, P5 차분게이트가 byte동일 강제. **follow-on 2탄(2026-06-10): select/concat/replicate 합류 — 신규 STRUCT_HEAVY 벤치 VM 0.36x(≈2.8x)**. **C6 lane(2026-06-10): array-indexed Signal(`LoadIndexed`, OOR/X 인덱스 sentinel 오라클 패리티) + 65..=128bit wide lane(별도 u128-pair 스택, narrow 무변경)** — WIDE_HEAVY 0.59x(≈1.7x)·MEM_HEAVY 0.72x(≈1.4x). **v6 ④(2026-06-11): wide 구조 트리오 랜딩**(WSelect/WConcatPair/WRepl, 65..=128bit 혼합-스택 — WIDE_STRUCT_HEAVY **VM 0.44x ≈2.3x**) + **real lane 측정-폐기**(REAL_HEAVY VM 0.90x — real은 합성 핫패스에 부재, 프로브 영구 잔류). 잔여 lane = signed >64 arith(오라클 X-poison 영역)·>128bit·sysfunc — 전부 컴파일-시 오라클 bail, 저ROI 박제 |

**판단(2026-06-10 갱신):** 세 축 모두 1라운드 수확 완료 — value word化 ~6x(eval-heavy) · 스케줄러축 ≈1.85x(클럭-바운드)+dirty-list R2(NETS_HEAVY ≈19.7x) · native-eval ≈1.4~2.8x(식/구조/wide/mem-바운드 VM, C6 포함). **~~①저빈도 value-op word化~~ ✅ 2026-06-11 — `eval_select`(in-range 단일 copy)/`eval_concat`/`eval_replicate`/`merge_x`를 word-parallel `copy_bits`(64-bit 두-워드 funnel 윈도)로 전환: STRUCT_HEAVY **인터프리터 551→382ms ≈1.44x**(VM 불변 — native lane 별개), 타 lane 회귀 0(725 green·iverilog 차분 포함). 잔여 perf 후보: `has_xz`/`to_u128` 미세(소소)·wide 구조 트리오·real native lane(좁은 영역, 저ROI).** *(C6 array-indexed + 128-bit lane ✅ 2026-06-10 — doc-18 §실측. net_to_edge/waiter 다넷 후보는 `perf_nets_scaling` 프로브(512→8192 평탄)로 **무근거 판명·폐기** — 같은 날.)*

### B. Correctness 후속 (perf와 독립)

- **🔴 2026-06-10 7축 감사 — correctness 트랙 재개.** silent-wrong 신규 확정(라이브 재현+iverilog 차분): **P0 클러스터 2개** — ①런타임 >64bit 절단(`to_u64`가 절단값 반환: relational `2^64>1`→0, shift-amount, negate, part-select offset, delay) ②elaborate 상수 도메인(미폴딩 param→**silent 0**(`W=MODE?16:8`→0), u32 도메인의 `1<<32` 오라클 발산·signed AShr·하강 genvar 폭주) + **P1 의미론**(`$fatal` no-op→**실패 TB exit 0**, force/release no-op, 비상수 `#delay`→#0, `$display("a=",a)` 인자 유실 등). Gemini shift fix(2026-06-10)는 검토 후 채택 — 단 근거 오류·잔존 한계는 P0-6으로 이관. **전 항목·증거·수정방향 = [REMAINING_WORK](REMAINING_WORK.md) P0/P1/P2.**
- **C7 — ✅ 검증+수정 완료 (2026-06-07, `fbb869c`).** `flush_postponed`가 `$strobe`/`$monitor`를 **마지막 실행 프로세스의 timescale multiplier**로 렌더하던 실버그 확인됨(혼합 timescale: 1ns 서브모듈의 `$strobe`가 같은 tick에 나중 실행된 1ps 형제의 `M`으로 `$time` 렌더). 수정: 등록 시점 `cur_time_mult`를 `FmtCapture.time_mult`에 스냅샷 → flush에서 per-capture로 렌더, 진입값 복원. 회귀 `cli/tests/timescale_postponed.rs`. *(주의: 단일 top만 실험으로 안 됨 — 아래 멀티-top 항목 참조. 반드시 top이 서로 다른 timescale의 서브모듈을 instantiate해야 재현.)*
- **문서부채 — ✅ 정리 완료 (2026-06-07).** doc-01 freeze 표 `enum`/`typedef`/packed `struct`를 IN-MVP로 정정(`union`/`string`/동적배열은 deferred 유지) · `$stime` **미구현**(`VITA-E3009`)을 hdl-ref/06/08에 명기 · `%t` plain-decimal(=`%0d`, `$timeformat`·필드폭 미적용) caveat 추가.
- **멀티-top-module 다중 root elaborate — ✅ 검증+수정 완료 (2026-06-08).** 인스턴스화되지 않은 bare top 모듈이 여럿이면 **마지막 선언된 것의 계층만** 시뮬되고 나머지는 조용히 누락하던 실버그 확인(immediate `$display`조차 안 나옴). 수정: `pick_top`(단일)→`pick_roots`(미인스턴스 **전부**를 선언순 root로) + `collect_instantiated`(generate 블록 재귀 스캔으로 generate-nested 자식이 spurious root 되는 것 방지) + `run`이 root마다 `elaborate_instance` 호출. **골든 무영향**(단일-top 설계는 root 1개 → byte-identical; sim-ir 타입 무변경, format_version 3 유지). VCD는 다중 top-level `$scope`로 정확 방출(범용 sorted-leaf 트리 walk가 단일 root 미가정). 중복 모듈명은 canonical(first-decl)로 dedup, cycle/pure-library는 last-declared fallback. 회귀: `elaborate` `v3_2b`(두 root)·`v3_2c`(generate-nested ≠ root), `cli/tests/multi_top.rs`(end-to-end 두 top + hierarchy 공존). IEEE 1364/iverilog 기본(미인스턴스=root) 일치. *(447 tests green, differential 7 + backend_equiv 11.)*

### C. 컴파일드 백엔드 전략 — ✅ 결정 + 🔨 C4-lite 착수 (2026-06-08)

1차 질문은 "eval=~4%면 VM 계속 갈 가치 있나?"였고 답은 "동결"이었다. **연산자수 스윕이 그 전제를
뒤집었다**(eval = 식 복잡도에 선형, 식-바운드 70-82%). **결정: native-eval(plan C4~C9)을 식-바운드 perf의
정당한 방향으로 채택** — "동결" 폐기. **→ 2026-06-08 C4-lite 첫 증분 랜딩**(아래).

- **왜 채택:** 식-바운드 RTL(ALU·crypto·깊은 조합)에서 native-eval 설계당 ~2-3x. 현 VM(문장 컴파일)은
  이 영역에 무력(`EXPR_HEAVY` 0.92x). 스케줄러 축(§A)과 상보 — 클럭-바운드는 스케줄러, 식-바운드는 native-eval.
- **왜 안전:** P5 차분 게이트(compiled==interp byte동일) + iverilog 오라클이 4-state 정확성 리스크를 강제.
  새 백엔드가 한 비트라도 틀리면 게이트가 즉시 red — native-eval을 "정확성 by-construction"으로 시도 가능.
- **🔨 C4-lite 착수 (랜딩됨):** **VM 전용 native 평가기 `native_eval.rs`** — codegen-able 바디의 assign RHS를
  post-order 레지스터 프로그램으로 컴파일(노드당 `Value` 미생성). **지원 서브셋: ≤64bit 정수** Const·scalar Signal·
  Add/Sub/Mul·BitAnd/Or/Xor/Xnor·BitNot/Plus/Minus. 그 외(real·>64bit·array-index·Div/Mod/Pow/시프트/비교/리덕션/
  ternary/concat/select/sysfunc/call)는 `try_compile`이 `None`→`eval_ctx` fallback. **인터프리터=오라클**,
  leaf는 `read_net`+`resize_keep_sign` 정확 재사용, 산술은 X/Z poison+u64 wrapping(w≤64는 sign-무관), bitwise는
  `value::{and,or,xor,xnor,not}_w` 동일 primitive. 측정: **expr-heavy VM 0.42x(≈2.3x), 클럭-바운드 0.94x 불변**.
  검증: native_eval 오라클 대조 단위 8 + backend_equiv native teeth 5 + 72-design P5 차분 + iverilog 차분, 460 green.
- **follow-on(다음 증분):** 비교/시프트/Div·Mod/리덕션/ternary/select·concat → 각각 의미론 추가; >128bit·signed-lane·real(C6);
  C9 인프라(content-addressed codegen 캐시·`kernel_abi_version` 헤더·ExprId→SourceLoc). frozen sim-ir 0줄 변경 유지.

### D. 언어/기능 커버리지 (perf 아님 — "유용성" 트랙)

vitamin은 **서브셋** 시뮬레이터. 실사용 가치는 "더 빠르게"보다 **"더 많은 RTL 지원"**일 수 있다.
2026-06-10 2탄까지의 결과: ~~force/release~~(✅ sample-once 구현) · ~~비상수 `#delay`~~(✅ 런타임 평가, v4) ·
~~`$fatal` 계열·b/o/h·`%m`~~(✅) · ~~`time` 타입~~(✅) · ~~`$dumpflush/$dumplimit`~~(✅) — **잔여 deferral은 전부
loud-reject로 확인됨(이제 참):**

- ~~proc-`assign`/`deassign` · `disable` 실동작~~ ✅ 2026-06-10 (§F-(F)) — **disable**: 동봉 named block의 break/continue 이디엄 실구현(doc-17 "Disable 후 Goto", lazy exit-BB로 기존 디자인 byte-불변; 크로스-프로세스/task/fork-경계/계층 경로=loud). **proc-assign/deassign**: force 기계 weak-rank 재사용(`assign_ranks` 사이드카 — force가 assign을 latent로 밀어내고 release가 복귀; iverilog const-rhs 차분 일치, 식-RHS는 iverilog "evaluated once" 자인이라 hand-IEEE 핀). `->event`+`@(ev)`만 잔존 — **단, 카운터-desugar 설계로 bump 불요 가능성 확인(§F-(B) 참조)**
- ~~force/release **full 재평가 모델**~~ ✅ 2026-06-10 — expression force는 IEEE §9.3.2 연속 재평가(`active_forces` 레지스트리, delta마다 재핀·BTree 결정성·mult 스냅샷). **iverilog와 의도적 발산**(iverilog는 "sorry: evaluated once" 자인 — const-rhs 차분만 유지, expression lane은 hand-IEEE end_to_end 핀)
- ~~intra-assignment delay(`a = #d b`)~~ ✅ 2026-06-10 — **blocking 형은 실semantics**(capture-now/write-later: tmp(폭=lhs 정확) → `Terminator::Delay` → write; `#0`=inactive·런타임 d 지원, iverilog 차분 일치). **NBA 형(`<= #d`)은 loud E3009로 이관** — 값-운반 delayed NBA 이벤트가 없으면 겹침 활성화(transport delay)에서 silent-wrong이라 차기 format bump 묶음
- ~~dynamic/associative array, queue~~ ✅ v5+⑥로 전부 랜딩(엔진+문법) · **`foreach(arr[i])` ✅**(파서 desugar — v6에서 uniform first/next 워크로 재작업) · **v6 bump ✅ 2026-06-11**: queue `insert/delete(i)`·assoc `first/next/prev/last`(+assoc-foreach)·string 키·bounded queue `[$:N]`(사이드카) 전부 랜딩. 잔여 = full `string` 타입(Phase-3 — string-키 foreach의 8-byte ref-var 한계 해소용)
- `+incdir+`/`+define+` filelist 버킷(PreOpts 플러밍), implicit-net 추론(W2003 활성화), `-Wwarn=`/`--log` tee
- 추가 SV 구문 (interface, package, assertion 등 — Phase-2+)

### E. 병렬화 (신규 트랙 · 2026-06-10)

현황: 스레딩 0·계획 0이었음(doc-18:19가 PDES만 "결정성 충돌·장기"로 박제). 감사 결론 — **엔진 내부 병렬(PDES/Verilator식 파티셔닝)은 결정성 invariant(3-OS byte-identical, tie 순서, eager VCD)와 정면충돌이라 최저 ROI(연구 트랙 유지)**, 실질 윈은 엔진 밖. *(→ 2026-06-11 T4 연구가 이 프레임을 정정: 결정성은 보존 설계가 존재해 차단 요인이 아님 — 진짜 제약은 워크로드 폭·직렬 몫·공수. 아래 T4.)*:

| 단계 | 내용 | 리스크/공수 |
|---|---|---|
| T0a | P5 차분(interp·VM) `thread::scope` 동시 실행 + Send sink | 0 / 시간 |
| T0b | VCD `BufWriter`(현 레코드당 ~1 syscall!) + dump-heavy perf 측정 | 0 / 시간 |
| ✅T1 | ~~`--threads ≥2`: VCD 전용 writer 스레드~~ — 완료(2026-06-10, byte-identical 게이트) | — |
| ❎T2+ | ~~front-end per-CU~~ — **2026-06-11 측정 폐기**: 400모듈/12k라인 vcmp(전 front-end)=**~10ms**(비병목) + vita는 단일-CU concat 모델(파일 간 `` `define `` 가시성)이라 per-CU 분할은 의미론 변경 없이 불가. parallel elaborate(비추천: arena ID=골든 계약) 결론 유지 | 폐기(측정) |
| ❎T4 | ~~엔진 내 PDES~~ — **2026-06-11 타당성 연구 종결(조건부 NO-GO)**: 프로브 3종 실측(τ 상주풀 0.3~0.5µs/delta·naive spawn 31~93µs 즉사, g≈700ns/activation, BSP mock 디스패치 측 최대 3.9x@T4) + sample 분류(직렬 잔류 ~20% → **Amdahl 상한 T4 ≈2.5x**) → 이상적 wide-synchronous(W≥64)에서만 2~3x, **corpus 워크로드(W=1~8)는 0~손해**. byte-identical은 보존 설계 존재(NBA-pure 클래스+run-splitting+per-process 로그 머지 — by construction)로 차단 요인 아님. **재진입 조건 핀: 지속 W≥64 + grain≥200ns 실워크로드 출현 시 BSP v1 착수.** 상세 = [doc-18 §PDES 타당성 연구](preview/18-acceleration-analysis.md#pdes-타당성-연구-2026-06-11--p4-t4-종결) | 종결(연구) |

**옵션 설계:** `--threads N`/`-j N`(vita·vrun), 기본 auto=`min(available_parallelism,8)`, env `VITA_THREADS`, `--threads 1`=현행 동일. **계약 = "모든 N에서 출력 byte-identical"**, corpus `--threads 1` vs `4` byte-diff 게이트로 강제. SimOpts out-of-band(골든 무영향). 상세 = REMAINING_WORK §P4.

### F. Phase-2 관문 — 차기 format bump 묶음 (2026-06-10 평가)

**원칙:** SchemaHash flip(=전 `.velab` 무효 + REGEN_GOLDEN 절차)은 비용이므로 frozen-IR 형상 변경은
**한 번의 v5 bump에 일괄**. v4 절차(REGEN_GOLDEN=1 스위치·canonical txt·RON 재생성)가 그대로 재사용된다.
반대로 **IR-무변경으로 가능한 Phase-2 항목은 bump를 기다릴 이유가 없다** — front-end/사이드테이블만으로 먼저 간다.

| 항목 | IR 영향 | 평가 |
|---|---|---|
| ✅**(A) NBA delayed write** `q <= #d rhs` | **완료 2026-06-10 (v5)** — `NonblockingAssign.delay`(ExprId, 실행 시 평가·mult 스케일) + `delayed_nba` wheel(값-운반, `apply_nba` 전역 seq 정렬로 문장순 보존) | iverilog 차분 4레인(NBA-region 착지·겹침 transport·`#0` 문장순·인덱스 동결). finish-vs-due-update 동시-틱 tie는 도구-발산 영역으로 핀 |
| ✅**(B) named event + `->`/`@(ev)`** | **완료 2026-06-11, sim-ir 0** — 카운터 desugar(64-bit Reg init 0, `->e`=`e=e+1`, `@(e)`=AnyEdge; 같은-슬롯 이중 trigger도 edge 보존). 값-표면 전부 loud(읽기/쓰기/range/init) | iverilog 차분 3레인(trigger/wake·혼합 리스트·no-latch). `.vu` AST 해시 재핀(NetVarKind::Event) |
| **(C) dynamic array / queue / assoc array — 📐 설계 완료(2026-06-10)** | handle-net + 엔진 힙(`dyn_heap: BTreeMap<NetId, DynObj>`, BitPacked 스토어 비침투): `NetKind` +3, `SysFuncId` +5, `SysTaskId` +5, `Signal/LvalChunk` word 재사용 — **v5 형상 diff 전량 확정** | 설계 = [`superpowers/plans/2026-06-10-dynamic-storage-design.md`](superpowers/plans/2026-06-10-dynamic-storage-design.md) (MVP 컷·OOB=X+warn-once·VCD 미덤프·결정성 계약·bump 체크리스트 포함) |
| ✅(D) interface / modport — **스파이크 완료(GO)** | **SimIr 무변경 확정** — 신호=평범한 net + 심볼 aliasing(cont-assign 금지: 방향 없음), `.vu` AST 해시만 1회 flip(핀 골든 0) | 설계 = [`superpowers/plans/2026-06-10-interface-flattening-spike.md`](superpowers/plans/2026-06-10-interface-flattening-spike.md). 구현은 v5 묶음과 같은 시기 권장(AST flip 1회로 수렴) |
| ✅(E) immediate assertion `assert(e) else $error` | **무변경** — 파서가 `Stmt::If`로 desugar(AST 동결 유지) + 디폴트 실패는 `$error("Assertion failed")` 합성(severity 테이블 경유 stderr+exit1) | **완료 2026-06-10** (654 green, iverilog 차분 일치 — X-cond=fail 포함). concurrent SVA는 별개(거대, Phase-3), `assert property`/`#0`/`final`=loud |
| ✅(F) `disable` 실동작 / proc-`assign`/`deassign` | **완료 2026-06-10, bump 0** — disable=동봉 named block Goto(lazy exit-BB, 기존 CFG byte-불변·비동봉은 loud); proc-assign=Force/Release 재사용+`assign_ranks` 사이드카(weak rank·latent 복귀, `.velab` trailer 세그먼트 append) | 665 green, iverilog 차분(disable 3종·assign const-rhs 2종)+staged trailer 왕복 |

**진입 시퀀스(권장):** ① IR-무변경부터 — ~~(E) immediate assert~~✅ → ~~(D) interface 스파이크~~✅ → ~~(F)~~✅ ②
~~(C) dynamic storage **설계 문서**~~✅ ③ ~~v5 bump 일괄~~✅ **완주(2026-06-10/11)**: bump(형상+REGEN, `e7f08e8`) → (A) 구현(`1617980`) → (B) 구현(`0a39dec`). **(C) 엔진 증분 ~~③dyn array~~✅·~~④queue~~✅·~~⑤assoc~~✅·~~⑥front-end 일괄~~✅(2026-06-11, 722 green — dyn/queue 문법은 iverilog 차분, assoc·interface는 iverilog 자체 미지원이라 hand-IEEE 핀; SimIr 무변경·.vu 재핀 1회). §F 전 항목 소진 — 잔여 후속 = modport 방향 강제·iface 파라미터·dyn 슬라이스/중첩 등 MVP cut 명시분** — [설계 문서 §6](superpowers/plans/2026-06-10-dynamic-storage-design.md) 순서.

---

## 2. 추천 우선순위 (다음 세션)

- ~~C7 `cur_time_mult`-during-postponed 버그 검증~~ — ✅ 완료 (`fbb869c`).
- ~~문서부채 정리~~ — ✅ 완료 (doc-01/05/06/08/display-io).
- ~~컴파일드 백엔드 전략 결정 (§C)~~ — ✅ 결정됨: **native-eval 채택**(식-바운드 perf), "동결" 폐기. 착수는 perf 우선화 시점.
- ~~멀티-top-module 다중 root elaborate (§B)~~ — ✅ 완료 (2026-06-08): `pick_roots`+generate 재귀 스캔, 골든 무영향, 447 tests.
- ~~native-eval 착수 (식-바운드 perf, §A/§C)~~ — 🔨 **C4-lite 랜딩 (2026-06-08): ≤64bit 정수 서브셋, expr-heavy VM 0.42x(≈2.3x), 460 tests, frozen sim-ir 무변경.**

**(2026-06-10 2탄 갱신 — 위 1~8 전부 완료. 다음 후보:)**

1. ~~스케줄러 라운드 2: dirty-list 넷 스캔~~ — ✅ 2026-06-10. dirty-list + `snapshot_prev` 삭제(증명된 no-op)로 **NETS_HEAVY 305→15.5ms ≈19.7x**(idle-넷 세금 제거), byte-identity 스위트 전부 green. 다음 스케일링 후보 = net_to_edge/waiter 자료구조.
2. ~~filelist typed 버킷~~ — ✅ 2026-06-10. `-D`/`-I` + `+define+`/`+incdir+`(verbatim/베이스해소) → PreOpts, `E-FLIST-WRONG-STAGE`(velab/vrun), `W-FLIST-OVERRIDE`(단일값 knob last-wins 경고). 잔여=DUP-CTX(sticky 도입 시)·--dump-filelist.
3. ~~native-eval C6 lane~~ — ✅ 2026-06-10. array-indexed Signal(`LoadIndexed`/`WLoadIndexed`, OOR/X→sentinel) + 65..=128bit wide lane(별도 u128-pair 스택 — narrow 무변경·무세금, WIDE_STACK=8). WIDE_HEAVY 0.59x·MEM_HEAVY 0.72x. 잔여 native lane(저ROI 문서화): signed >64 arith/divmod(오라클 X-poison 영역)·wide 구조 트리오·>128bit·real·sysfunc.
4. **vita-log 2단계** — `--log` tee(단일 writer 불변식, doc-13)·`-q`/`-v` verbosity·counts summary epilogue.
5. ~~언어 커버리지(§D 잔여)~~ — ✅ 2026-06-10 intra-assignment delay(blocking 실구현·NBA는 loud-defer→bump 묶음) · force 연속 재평가. implicit-net은 P2-12에서 **E3010 정책으로 확정**(추론 대신 명시적 에러 — `default_nettype` 지원이 미래 옵션) — 항목 종결.
6. ~~Phase-2 관문~~ — ✅ 2026-06-10/11 §F 진입 시퀀스 완주: (E) assert·(D) 스파이크·(F) disable/proc-assign·(C) 설계·v5 bump·(A) NBA-delay·(B) named-event. 잔여 = (C) 엔진 증분(③④⑤)+⑥ front-end.
7. **운영 인프라** — 3-OS CI 매트릭스 실구동(doc-09 §285 스케치 → 실 워크플로), `--dump-filelist`, RULE-V composite 해시(Phase-2).

---

## 3. 교훈 (방법론 — 재사용 가치)

- **병목은 양파다.** doc-18의 두 예측(Value-alloc·tree-walk)이 첫 측정엔 둘 다 "아님"이었지만, 실은 bit-serial 처리가 alloc을 가리고 있었을 뿐. 표면층 제거 → 재측정 → 다음 층. **최적화는 한 번 측정으로 끝나지 않는다.**
- **"실패한" 실험도 선행 최적화 후 재시도 가치.** inline-Value가 1차엔 ~0(net-write per-bit 루프가 alloc 가리고 Deref 오버헤드 상쇄) → 그 루프 word化 후 3차엔 1.55x.
- **사이클 = profile → 최소 fix → re-profile 반복.** 각 fix는 항상 bit-exact(suite + iverilog 차분이 스펙). `cargo test -p sim-engine --test perf_baseline -- --ignored --nocapture`로 before/after 측정, `/usr/bin/sample`(macOS, sudo 불요)로 self-time 히스토그램.
- **공유 경로 최적화가 backend-전용보다 유리했다** — interp·VM 둘 다 빨라지고 위험도 낮음.
- **타 모델 수정 리뷰는 "코드 ≠ 서술" (2026-06-10).** Gemini shift fix는 코드는 옳았지만 근거 두 건이 틀렸다(존재하지 않는 "중복 매치 암", 오라클 미확인 "0이 정답" — iverilog는 4294967296). 리뷰는 diff만 읽지 말고 **오라클 라이브 차분**(t1/t2)으로 닫을 것. 또: "warn 떴으니 loud-reject"라고 믿지 말 것 — warn+no-op는 silent-wrong의 사촌(§D 정정 사례).

---

## 4. Phase-2+ 퓨처 플랜 (2026-06-11 — Phase-1 작업 큐 소진 후 스코프 결정용)

> Phase-1 큐 소진(765 green, format_version 6, PDES 연구 종결) 시점의 **전체 잔여 지형도**.
> 두 축으로 펼친다: §4.1 스코프 확장 트랙(결정 대기) · §4.2 의도적 MVP 컷 인벤토리(전부 loud/문서화 —
> 해제 시 합류 트랙 매핑). 원칙은 그대로: **frozen-IR 변경은 한 번의 v7 bump에 일괄**(§F 선례),
> IR-무변경 항목 선행, 오라클(iverilog) 차분 가능 영역 우선, 진입 전 §F식 관문 평가(스파이크→설계→bump).

### 4.1 스코프 확장 트랙

| 트랙 | 내용 | IR/AST 영향 | 오라클 | 공수 | 비고 |
|---|---|---|---|---|---|
| ~~**P2-A worklib**~~ ✅ **2026-06-12 v1 완료**(`33ec95c`+`db48aa1`) | `vcmp --work`(lib.toml 정규형+content-addressed CU blob, E2001 dup·소스경로 재컴파일=교체·GC) + `velab -L/--top`(클로저 로딩·**unit_map 승자 기반 머지** — passenger 그림자가 로드 순서로 이기는 결함을 자가 리뷰로 적발·수정) + **vrun RULE-V 자동 게이트**(매니페스트/blob/소스·include raw 재해시=E9003 exit 2) + E9005 신설. 적대 리뷰 9불변식 PASS. 디퍼: `-y`/`-v`/`-P`/`--lib-map`·per-unit blob·vrun -L 재배치. E8003 sticky 일반화=**N/A 판정**(sticky 표면이 timescale뿐 — 이미 커버) | 0 (bump 0, 골든 불변) | e2e 11 | 완료 | lib-모드 .velab의 경로 문자열은 same-machine 스코프(리뷰 D-9: cross-OS 복사는 loud exit 2) |
| ~~**P2-B Phase-2 system tasks**~~ ✅ **2026-06-12 완료**(v7 슬라이스 2~7) | casez/casex 정밀(`CasezEq/CasexEq` — x는 strict, redor 공식 폐기) · bit-vector(`$countones/$onehot(0)/$isunknown` 4-state, `$bits`=elaborate const-fold 3-레인: array view/ir_bits_of/decl-order prescan) · `$random`=**Annex N 핀**(LCG+float-mantissa, dist_uniform의 end+1 스케일 2^32+floor-캐스트 — 어구 그대로 읽으면 양음 둘 다 ±1 발산, 4-draw 라이브 핀)·`$urandom(_range)`=splitmix64 자체 계약 · `$stime` · plusargs(CLI `+args`→SimOpts, test=prefix probe·value=인터셉트) · 파일 I/O(fd=0x8000_0003…·MCD bit1…·W4022) · `$readmemb/h`(선언-인덱스 도메인 @addr·W4023) | v7 bump #1 소진 | iverilog 차분 전량(+$urandom 자체핀) | 완료 | **시드-부작용 함수 = StmtEffect 인터셉트 패밀리**(seeded $random/$value$plusargs/$fopen — direct-rhs만, 그 외 loud) |
| ~~**P2-C full `string` 타입**~~ ✅ **2026-06-12 완료**(v7 슬라이스 8·10) | `NetKind::String`=힙 핸들(dyn 선례) — read=**is_str packed 구체화**(ctx resize 바이패스, is_real 선례)·write=leading-NUL strip(§6.16) · 메서드 len/getc/substr/toupper/tolower/compare(+putc=SysTask) · **모든 비교 = StrCmp 라우팅**(packed zero-ext는 비사전식·정적 사이징은 절단) · `$sformat(f)`(렌더=커널측 인터셉트) · concat=loud($sformatf 우회) | v7 묶음 #2 + AST flip 1회 | iverilog 부분 차분(decl/len/substr/비교/$sformatf)+hand-IEEE(toupper/getc/putc/compare — iverilog 미지원; string NBA=vvp 내부에러) | 완료 | string-키 foreach 한계 해소는 후속(8byte ref-var 표면 그대로) |
| ~~**P2-D package**~~ ✅ **2026-06-12 완료**(v7 슬라이스 9) | run() 진입시 패키지 일괄 fold($pkg$ 합성 스코프) → import(CU+module)가 const는 (3b) 전·func/task는 (3.5) 후 skip-if-present 바인딩(**로컬 승리** iverilog 핀) · `pkg::sym`=expr/const 양쪽 fold · typedef 타입명=파서 unit-global 맵 편승(문서화) | IR **0** 확정(interface 선례) | iverilog 차분 | 완료 | 패키지 변수/패키지내 import/scoped 함수콜=loud 디퍼 |
| ~~**P2-E 절차 고급**~~ ✅ **2026-06-12 완료**(914 green) | ⭐관문 적발: join_any/join_none은 **이미 전체 동작**(파서+사이드카+배리어 — 차분 핀만 추가) · `disable fork`=자손 transitive kill(activity `dead` 마킹+`run_body` 단일 초크 드롭, DisableKind::Fork 기존 IR=IR-0) · `do-while`=파스 desugar · `unique/priority` if/case=위반(no-match) 표면을 `$warning` else/default arm으로 desugar(iverilog 핀; priority-if 문법은 iverilog 거부→hand-IEEE; 다중-match 검사=문서화 컷) · `final`=ProcKind::Final(.vu 재핀)+final_procs 사이드카(11번째 trailer)+종료 시퀀스(타이밍=loud §9.2.3) | AST flip 1회(.vu), IR 0 | iverilog 차분+hand-IEEE 2핀 | 완료 | **✅ `wait fork` 2026-06-14 v8로 완료**(`bb0c5ba` — Activity.wait_fork 누적 직계자식 배리어, disable-fork 훅 불요, iverilog 차분 5; ⭐동봉: edge-sensitive always mid-body 재발화 일반 엔진 버그=Activity.busy 가드). **✅ automatic/recursive frame-call(콜스택 모델) 완료 — B-track 2026-06-17(§4.1.1 행), E3009 해제** |
| ~~**P2-F concurrent SVA 서브셋**~~ ✅ **2026-06-14 완료(Phase-3 진입, 933 green)** | 단일 클럭 `assert property(@(clk) a \|-> b)`/`\|=>` + `$past/$rose/$fell/$stable` + 무-implication(`property(@(clk) e)`=`1'b1 \|-> e`). **노선 확정대로 전부 desugar(IR-0)**: ConcurrentAssert→합성 clocked always 체커(`materialize_sva_checkers`, named-event/foreach 선례), `\|->`=`if(ante&&!cons)$error`·`\|=>`=1-bit X-init pend reg 1-클럭 지연·$error=immediate-assert severity 재사용; sampled fn→prev-reg(signal당 공유·NBA `prev<=sig`). **⭐iverilog 13.0이 concurrent assert AND $past류 둘 다 거부→전면 hand-IEEE 핀**(S1 `cf8a6f1`/S2 `b09e7b7`/S3 `34354ac`). 적대 리뷰 3렌즈: F1(`\|=>` 다중비트 절단→reduction-OR)·F3(hierarchical aliasing→E3009) 수정, F2(lenient-X) 문서화, determinism CLEAN | AST flip(v8 일괄)·IR 0(순수 합성) | hand-IEEE(iverilog 미지원) | 완료 | **✅ full SVA 서브셋 + 트랙 A 완료(2026-06-15/16, 상세 §4.3 #6): S4–S9(`##n`/`##[m:n]`/`##[m:$]`/`[*n]`/`[*m:n]`/`throughout`/`[->n]`/`[=n]`/`within`) + S10–S15(module-level assert property·action block·disable iff·`[*m:$]`·sequence consequent·multi-clock S15=defer→A3 정준 구현) + named property/sequence 선언·인스턴스화 + A1 formal args·A2 action sampled·A3 multi-clock 정준·A4 generate-scope/property-ref-property. 전부 순수 IR-0(fmt_ver 8 유지·`.vu` 재핀만). **✅ 트랙 A 이후 후속(2026-06-16/17, §4.3 #6 말미): R2 outer-`\|=>` prop-ref skew 기본형(`3ba7763`)·repeat-event intra-assign blocking(`cfef719`)·faithful deferred immediate asserts `assert #0`/`final`(`592cb10`).** 잔여(LOUD): multi-term 시퀀스 cross-clock·sequence local var·recursive property·outer-`\|=>` prop-ref skew 고급형(2-cycle·시퀀스 antecedent·중첩·cross-clock)** |
| ~~**B-track frame-call(콜스택 모델)**~~ ✅ **2026-06-17 전체 완주(1145 green, main)** | automatic/recursive **함수**(B1 `a3c1c92`+`b916131`)·**태스크**(B2 `4b2cb4e`)·**disable-unwind**(B3 `7883a85`)·**변수 lifetime override**(B4 `cb79261`). 콜스택 형상이 PR1-B/M3서 frozen 예약돼 **순수 IR-0**(schema 093e…불변·fmt_ver 8). 함수=`Expr::Call`(&self eval, frame arena 재사용)·태스크=`Terminator::Call`+`TaskCallInfo` 사이드카(positional in/out 바인딩)·재귀=Rust 스택+`MAX_CALL_DEPTH` 가드(64MB worker)·automatic=per-call window·static=공유 slab(iverilog 손상 충실)·B4=per-slot lifetime(window/slab 혼합). frame-local nets=진짜 ir.nets+사이드카 라우팅. VM 제외(P5 vacuous). **⭐잠복 B1 CLI 갭 동시 수정**(한방 vita SimOpts가 func_table 미전달). | IR **0**(frozen 슬롯 emit·`.vu` B4 재핀 1회) | iverilog 차분(fact·static/auto 60/30·mutual·task·disable) + hand-IEEE(B4 override·func self-disable reject) | 완료 | **잔여(별개)**: cross-frame disable(outer 프레임)=loud·~~staged vrun 사이드카 직렬화(현 one-shot vita만)~~ ✅ **2026-06-22 해소**(§5 STAGED-DROP — 14번째 trailer)·block-local `automatic`(form 2) |
| 조건부① PDES BSP v1 | 재진입 조건(지속 W≥64 + grain≥200ns 실워크로드) 충족 시 — doc-18 §PDES 스케치 + T1식 byte-diff 게이트 | SimOpts out-of-band | byte-diff 게이트 | 大 | 트리거 대기 |
| 조건부② cycle-based 컴파일드 모드 | Verilator-lane(2-state·정적 스케줄) 옵트인 — doc-18 §2b | 별도 백엔드 | 자체 차분 | 大 | 사실상 별제품 — 수요 확인 후 |
| 조건부③ native-eval 잔여 lane | signed>64 arith·>128bit·sysfunc lane | 0 (VM 전용) | P5 게이트 | 小~中 | 저ROI 박제 — 워크로드 증거 시 |
| 장기④ VHDL front-end | parse까지 언어의존 구조라 시임 준비됨 — lexer/parser/AST→elaborate 신규 | sim-ir 무변경이 설계 목표 | GHDL 차분 | 大大 | Phase-4+/별도 결정 |

### 4.2 MVP 컷 인벤토리 → 해제 매핑 (현재 전부 loud-reject 또는 문서화된 degrade)

| 컷 (현재 동작) | 해제 트랙 |
|---|---|
| `class`/OOP · `program` · `union` (parse/elab loud) | Phase-3+ (P2-F 이후 — 검증 생태계 일괄) |
| ~~full `string` 타입~~ ✅ v7(잔여 컷: concat=loud→$sformatf 우회·decl init·port·string-키 foreach ref-var·atoi/itoa류 메서드=loud) · wildcard assoc `[*]`(loud) | ~~P2-C~~ 잔여 컷만 후속 |
| ~~파일 I/O · `$readmem*` · random · plusargs · `$stime`~~ ✅ v7 | math transcendentals만 잔존(libm 3-OS 리스크 — 순수-Rust libm 핀 결정 대기) |
| ~~`final` · fork 고급 · automatic/recursive frame-call (E3009)~~ ✅ P2-E + **B-track**(frame-call 2026-06-17, §4.1) | 완료 (잔여: cross-frame disable·block-local `automatic` form 2=loud) |
| ~~`assert property`~~ ✅ P2-F(SVA 서브셋, Phase-3) · ~~`assert #0`/`final` 연기형 assertion~~ ✅ **22탄**(`592cb10` — Observed/Reactive 리전, IR-0) · ~~repeat-event intra-assign~~ ✅ **blocking `= [repeat(n)]@(ev)`**(`cfef719`) **+ NBA `<= [repeat(n)]@(ev)`**(`f4aab23`=N1, capture-now/`fork…join_none`/NBA-write, iverilog 차분) · clocking block (loud) | ~~P2-F~~ — clocking block만 후속 |
| ~~instance array · 다차원 슬라이스/whole-array 대입 · per-dim bounds · `$dump*` 배열 · casez/casex 정밀 · `assign #d` inertial · wide 산술~~ ✅ Phase-1.x 정밀화 소묶음 전량(2026-06-12) + casez/casex는 v7 슬라이스 2 | 완료 — 잔여는 doc-01 표 정본 |
| `defparam` (E3009) | **해제 안 함 권장** — IEEE deprecated, `#(.param())` override로 충분(정책 박제) |
| hierarchical 이름/함수/태스크 참조 (E3009) | P2 소항목(읽기 한정 검토) 또는 유지 |
| `` `default_nettype`` (implicit-net=E3010 정책) | 유지(명시적 에러가 정책) — worklib 후 옵션 재평가 |

### 4.3 권장 진입 순서 (제안 → 진행 중)

1. ~~**P2-A worklib**~~ ✅ 2026-06-12 완료(§4.1 행 참조).
2. ~~**v7 bump 묶음**~~ ✅ **2026-06-12 전량 완료(10 슬라이스, 851→903 green)** = 형상 bump(fmt_ver 7, 기능 0) → casez/casex → bit-vector+$bits → random/$stime → plusargs → 파일 I/O → readmem → AST flip(.vu 재핀 1회) → P2-D package → P2-C string. math transcendentals만 잔존 디퍼(libm 3-OS 리스크 — 순수-Rust libm 핀 결정 대기).
   **관문 평가(2026-06-12) 결과:**
   - **IR-0 선행분**(bump 불요 — 즉시 진행): ~~①instance array 언롤~~ ✅ 2026-06-12(`00ae25e` — iverilog 핀: 선언순 첫 인덱스=MSB 청크·W=P 공유/N·P 슬라이스·`[-1:0]`은 합법 2-원소 발견) → ~~②다차원 부분 슬라이스/whole-array 대입~~ ✅ 2026-06-12(IEEE §7.6 위치 대응 element-wise desugar; **iverilog가 기능 미지원→hand-IEEE 핀 레인**; 대입 외 whole-array=silent word-0→loud 승격, `$dump*` 인자만 현상 유지; 잔여 컷은 doc-01 표 정본) → ~~③per-dim bounds~~ ✅ 2026-06-12(d≥2 인덱스마다 `lo≤idx≤hi` 가드 IR 합성, 1-D byte-불변; **⚠️ iverilog도 inner-dim alias라 hand-IEEE §7.4.6 핀**; unpacked=X+E4002·packed=silent-X 벡터 계약 일치) → ~~④`assign #d` inertial~~ ✅ 2026-06-12(세대 카운터로 pending 무효화 — 좁은 펄스 필터·경계 펄스 생존, iverilog 차분 5핀; 엔진-로컬, IR 0) → ~~⑤`$dump*` 배열 per-element/depth~~ ✅ 2026-06-12(원소당 `$var`+dims 10번째 trailer 사이드카·depth/scope/net 인자 선별(level=iverilog 핀, 배열 인자=iverilog가 에러라 hand-확장)·W4021 신설(53 codes)) → ~~⑥>64 signed/>128 unsigned multi-word 산술~~ ✅ 2026-06-12(`arith_wide`+mw 커널(add/sub/mul/divmod/pow)+`%d` 임의 폭 십진 — 전부 iverilog 차분, VM=커널 공유 parity). **정밀화 소묶음 ①~⑥ 전량 소진 — 다음 = v7 bump 일괄.**
   - **bump 합류 확정분**: casez/casex 정밀 분리는 **IR-0 불가 판정** — scrutinee 측 z 식별에 per-bit `===`가 필요해 `BinOp::CasezEq/CasexEq` 신설(현 `redor(xor)!==1`은 casex엔 정확, casez엔 over-lenient). + SysFunc/SysTask 다수(파일 I/O·readmem·**random=IEEE Annex 알고리즘 핀**(iverilog 차분 가능; `$urandom`은 구현정의→hand-pin)·bit-vector·plusargs·`$stime`·`$sformat(f)`) + `NetKind::String` + AST flip 1회(string 타입+package+`pkg::` scope expr).
   - **측정-디퍼**: math transcendentals(`$ln`/`$sin`…)는 **3-OS libm 발산 리스크** — 순수-Rust `libm` 핀 도입 결정 전까지 loud 유지.
3. ~~**P2-E 절차 고급**~~ ✅ 2026-06-12 완료(§4.1 행 — wait fork=v8 합류·automatic/recursive=콜스택 디퍼).
4. ~~**P2-F SVA 관문 평가**~~ ✅ 2026-06-12 — **조건부 GO 박제**(§4.1 행: desugar 서브셋 노선 확정, 구현은 Phase-3 진입 결정 시).
5. ~~**v8 bump 묶음 + Phase-3 진입(wait fork + SVA 서브셋)**~~ ✅ **2026-06-14 완료(933 green, 5커밋, 3-OS CI green)** — ②v8 bump(`WaitCause::Fork`+AST flip, format_version 8, inert) → ①wait fork(IEEE §9.6.1, +edge-sensitive-always 재발화 일반 엔진 버그 수정) → ①SVA 서브셋(S1 파서·S2 체커 desugar·S3 sampled fn, **전면 hand-IEEE**=iverilog 미지원, 적대 리뷰 F1/F3 수정·F2 문서화). §4.1 P2-E/P2-F 행 참조. **→ §4.3 권장 진입 순서 + Phase-3 진입 슬라이스 전 항목 소진.**
6. **full SVA 시퀀스 (사용자 스코프=오토마타 트랙까지, 多슬라이스 진행 중)**:
   - ~~**S4 상수 `##n`+`[*n]`**~~ ✅ 2026-06-15(945 green, `c4efbd5`) — `expand_sequence`→`synth_seq_pipeline` 시프트레지스터, fmt_ver 8 유지·`.vu` 재핀. 적대 4렌즈 CLEAN, MEDIUM 1건(`[*` 토큰 wildcard 진단 degrade) 수정.
   - ~~**S5 유계 범위 `##[m:n]`/`[*m:n]`**~~ ✅ 2026-06-15(950 green, `d1c8120`) — disjunction(병렬 파이프라인 OR-reduce), AST/IR 무변경(min/max 재사용), flat byte-identical, `SVA_SEQ_ALT_CAP=256`. 적대 3렌즈 CLEAN(terminal repeat 상한=run≥m·mid-seq 윈도 독립).
   - ~~**S6 unbounded delay `##[m:$]`**~~ ✅ 2026-06-15(955 green, `85f7f10`) — `SeqHop::{Fixed,AtLeast}` + never-reset armed latch(`armed<=armed|cur`), `seq_delay_reg` 헬퍼로 Fixed byte-identical. 적대 3렌즈 CLEAN(16/16 latch·X-propagation safe·`|=>` 연동).
   - ~~**S7 `throughout`**~~ ✅ 2026-06-15(958 green, `c609b86`) — `Sequence::Throughout` variant(AST flip, sim-ir 골든 무변경), `expand_sequence`가 `SeqAlt=(terms,Option<guard>)` 반환, guard=`|cond`를 seed+모든 shift stage에 AND(`guard_and`). bounded inner만(unbounded→loud E3009), flat byte-identical(guard=None=identity). contextual keyword(`wire throughout;` 안전). 적대 2렌즈 CLEAN(start/gap/end kill·range per-alt·|=>·multi-bit reduction-OR).
   - ~~**S8 `[->n]` goto + `[=n]` nonconsec**~~ ✅ 2026-06-15(964 green, `eb1499f`) — **existence-latch FSM**(goto: `reg_s` 단계별+b-gated advance·match=`b&avail_{n-1}`; nonconsec: goto+`ext` latch `cur=match_g|(ext&~b)`). ⭐**정정 확정: IR-0 tractable**(`|->`=any-completion만 필요, exact-count 불요; 이전 "single-bit 머지 불가→infeasible"는 오판). 렉서 `[->`/`[=`·AST `RepeatKind` flip(sim-ir 골든 무변경)·single count·boolean operand만. 적대 3렌즈 CLEAN(overlap-divergence·n-th-b boundary·렉서 무충돌).
   - ~~**S9 `within`**~~ ✅ 2026-06-15(967 green, `eb2b4bd`) — `synth_within`이 `match(seq2) & OR_{i=0}^{L-k1} reg^i(match(seq1))`로 desugar(`Sequence::Within` variant·AST flip·sim-ir 골든 무변경). contextual keyword·top-level 한정·bounded boolean operand만. 적대 2렌즈 CLEAN(윈도-엣지 off-by-one 정확·straddle 거부·byte-identity).
   - **🏁 full SVA 시퀀스 서브셋 완성(S4-S9, 2026-06-15)**: `##n`·`##[m:n]`·`##[m:$]`·`[*n]`·`[*m:n]`·`throughout`·`[->n]`·`[=n]`·`within` 전부 순수 IR-0(트랙 전체 sim-ir 무변경=format_version 8 유지, `.vu` AST-hash 재핀만), 슬라이스별 적대 리뷰 통과, 3-OS CI green. ⭐핵심 교훈: `|->`=any-completion 의미론이라 exact-count 불요→**existence-latch FSM**(goto/nonconsec)·**OR-window**(within)으로 오토마타까지 전부 합성 RTL로 표현(이전 "오토마타=infeasible/새 IR 필요" 우려 반증).
   - **🏁 트랙 A(assertion 프레이밍 + 잔여 시퀀스) 완료(S10-S15, 2026-06-15)**: ~~module-level `assert property`(S10)~~·~~`[*m:$]` unbounded repeat(S13, gated run-latch)~~·~~action block `[pass] else fail`(S11)~~·~~disable iff(S12)~~·~~sequence consequent(S14, obligation chain)~~ 전부 구현(각 슬라이스 적대 리뷰 3렌즈 통과; S13 repetition-count cap·S14 multi-bit antecedent HIGH 수정 동봉). sim-ir 무변경(fmt_ver 8), S11/S12/S14는 `.vu` AST-hash 재핀, S10/S13/S15는 AST 무변경.
   - **multi-clock(S15) = 조건부 defer**: 두-프로세스 구현은 보류(단일-`always` 체커 모델 미연장 + iverilog 오라클 부재로 동시-틱 region 순서·cross-domain hand-reg race·c1==c2 collapse 3개 oracle-free 판단). **단, 게이트가 적발한 silent-accept 구멍 닫음**: OR-of-clocks 프로퍼티 클럭 `@(posedge c1 or posedge c2)`이 단일 `always @(c1 or c2)` 체커로 조용히 빌드되던 것을 loud 거부(`materialize_sva_checkers`에서 `Sensitivity::List` len>1/`@*` 차단), 프로퍼티 본문 내 2번째 `@`는 parser가 전용 진단. **향후 two-process 설계(구현 시)**: v1을 `@(c1) seq1 |=> @(c2) boolean_cons`(NonOverlap만)로 한정 — 프로세스 A `always @(c1)`가 seq1 매치를 1-bit consume-once `hand` reg에 NBA, 프로세스 B `always @(c2)`가 `if(hand && !cons) fail; hand<=0`. `|->` overlap cross-clock·`##0`-cross-clock은 loud, c1==c2는 기존 단일클럭 경로로 byte-identical 라우팅. 동시-틱 tie는 전부 hand-IEEE 핀(오라클 없음).
   - **bit-select event control 패닉 수정(2026-06-15 `51c11b9`, 1018 green)**: S15 적대 리뷰가 표면화한 pre-existing 버그 — `always @(posedge clk[0])`가 `sens_event_net`의 `warn`+POISON_NET→`net_to_edge[u32::MAX]`(sched.rs:398)로 패닉. 엔진 edge 모델이 bit0만 검사(IEEE: vector posedge=LSB)이므로 **LSB 상수 bit-select(`index==nv.lsb`, 양 방향 모두)은 underlying net으로 arm=정확**(bare ident와 동일 net id→골든 byte-identical, iverilog 차분 일치). 그 외(non-LSB·part-select·변수 인덱스·array·packed·computed base)는 loud E3009. **level(non-edge) bit-select도 거부** — 엔진 level=whole-net 변화라 sibling-bit 변화에 silent over-trigger(iverilog `@(clk[0])`는 clk[0]만). catch-all `warn`→`error`가 핵심(had_error→IR 폐기→엔진 미도달). elaborate-only(sim-ir/fmt_ver 8 무변경), `event_control.rs` 14 e2e, 3렌즈 리뷰 CLEAN. 후속(별개 subsystem)=P0-10(literal.rs ≥2³² 절단).
   - **P0-10 unsized 리터럴 폭 수정(2026-06-16 `2233ccd`, 1025 green)**: clk[0] 리뷰가 표면화한 pre-existing — `parse_int_literal`이 unsized 리터럴을 고정 32-bit로 pack해 ≥2³¹ silent 절단/오부호(IEEE §3.5.1 위반). 폭을 값 담게 성장(평decimal=max32,nbits+1·`'d`=max32,nbits·`'h/'b/'o`=digit-span, iverilog `$bits` byte-exact), <2³¹는 32 유지(골든 byte-identical), `MAX_NET_WIDTH` 캡(net 대칭). elaborate-only(fmt_ver 8 무변경). 잔여=[P-perf] `decimal_bits` O(n²)(pre-existing, REMAINING_WORK 기록).
   - **[P-perf] `decimal_bits` O(n²) parse DoS 수정(2026-06-16, 1030 green)**: P0-10 리뷰가 표면화한 pre-existing(diff 무관) — schoolbook base-10 장제법이 비트당 전폭 패스 = O(digits²)(40000자리=27s). **Horner base-10¹⁹/base-2⁶⁴ limbs(u128)**로 교체(40000자리 25ms ~1100×) + **`MAX_DECIMAL_DIGITS` 시간-경계 가드**(over-cap O(n) 거부, 320000 nines 3.65s→16ms) + **`alloc_width` clamp**(거대 explicit-width pack의 ~1GiB 할당 차단 = 적대 리뷰가 끌어낸 인접 hole, const 경로 DoS 동반 수정). byte-identity는 멀티워드 characterization(`t11c`)+51/51 python+iverilog로 락, 골든/fmt_ver 8 무변경. 적대 3렌즈 리뷰(286+ 프로브) CLEAN(유일 MEDIUM=O(n²)/가드-경계를 base-10¹⁹+가드축소로 소진). ⭐교훈: 가드는 폭-경계가 아니라 **시간-경계**여야(슬랙 320000은 변환-후-거부=잔존 DoS); 인접 hole(거대 explicit-width pack, const-eval cap 우회)은 적대 리뷰가 끌어냄.
   - **🏁 named `property`/`sequence` 선언·인스턴스화(2026-06-16, 1051 green)**: 사용자 "잔여 SVA". `sequence NAME; <seq>; endsequence`·`property NAME; <spec>; endproperty` + `assert property(NAME)`(프로퍼티 인스턴스)·시퀀스 본문 내 `NAME`(시퀀스 인스턴스). **순수 IR-0**(sim-ir/fmt_ver 8 무변경, `.vu` AST-hash만 재핀). 관문=워크플로우 5-agent GO/IR-0. **AST**: `Sequence::Instance{name,args}`+`ModuleItem::{SequenceDecl,PropertyDecl}`+`SeqDecl`/`PropDecl`(formals reserved=미래 슬라이스 re-pin 0). **파서**: contextual kw `sequence`/`endsequence`/`endproperty`·property-instance 2-토큰 분기·`parse_property_spec` 추출. **elaborate**: `seq_table`/`prop_table`(func/task 선례 whole-body prescan=forward-ref·per-instance saved/restore=cross-module 격리)·`expand_sequence` Boolean(Ident)→seq_table 인라인·collect splice·`inline_named_sequence`/`resolve_named_top` 재귀 가드. **오라클 NULL**(iverilog가 named·inline 둘다 거부)→hand-IEEE, 검증=named≡inline 행동 등가(VCD byte-identical). **적대 4렌즈 리뷰 → MEDIUM 4 수정**: ①named-seq `within` 본문이 inline과 달리 E3009(top-level 라우팅이 리터럴만 봄)→`resolve_named_top` peel · ②재선언 경고 "first used"인데 last-wins→first-wins(insert-if-absent) · ③`assert property(NET/SEQ)`="unknown property" 오해→seq_table 확인 메시지 정교화 · ④`module sequence`(V2005 합법) masking→`sequence IDENT ;` 2-토큰 가드. 잔여(LOUD·deferred): generate-내 decl·property가 property 참조. ⭐교훈: 시퀀스-인스턴스는 `Boolean(Ident)` 유지(파서 무변경=기존 byte-identical), 해소는 elaborate(func/task-name 선례); top-level 연산자(within) 라우팅은 named peel 후 수행해야 named≡inline.
   - **🏁 SVA 트랙 A1–A4 완주(2026-06-16, 1088 green, 4커밋 push)**: 사용자 "A1부터 A4까지 깔끔하게". 각 슬라이스 TDD + 적대 리뷰 4렌즈(각 라이브 재현). **A1 formal arguments**(`989e7b1`, 1062 green, IR-0·**.vu 재핀 0**=필드 기-reserve): `sequence s(x,y)`/`property p(x,y)` 위치 바인딩+formal→actual 치환(`subst_expr`/`subst_sequence`/`subst_sensitivity`), property-instance args, in-body `s(a,b)`=`Boolean(Call)`→elaborate 인라인, 결정적 `is_sequence_decl_ahead`(body-then-endsequence 형상으로 `sequence u(.o(o))` 인스턴스화와 분기). 적대 5홀: HIGH `resolve_named_top`이 bare top-level param seq를 arity-체크 없이 인라인(formal-shadows-net=silent-wrong)→arity-check·HIGH+MEDIUM 포워드스캔 poisoning/budget→bounded body-terminator 규칙·LOW 잘못된 formal 리스트 silent→loud. **A2 action-block sampled values + local-var 거부**(`393b010`, 1070 green, IR-0): `rewrite_sampled_stmt`가 pass/fail action 문장의 Expr leaf를 `rewrite_sampled` 통과(prev-reg 공유·cons/dis 뒤 호출=byte-identity·no-sampled=net 0할당), 시퀀스 local var(`int x;`/`(a,x=d)`)=per-attempt 저장소 불가→targeted loud(`at_sva_local_var_decl`/`at_sva_match_item_paren`). 적대 1홀: MEDIUM typed-decl 복구가 첫 `;`서 멈춰 다중 decl 미소거→decl마다 `;` 관통 skip. **A3 multi-clock 정준 패턴**(`af39b9a`, 1079 green, **유일 AST 변경=.vu 재핀**): `@(c1) ante |=> @(c2) cons`=두-프로세스 핸드오프(`synth_multiclock`: c1 SET-only `if(|ante) handoff<=1` + c2 CHECK+DISCHARGE `if(handoff) begin if(!cons)$error; handoff<=0; end`), 양측 별도 SvaRegs(ante c1 sample·cons c2 sample), `consequent_clock: Option<Sensitivity>`(ConcurrentAssert+PropDecl, subst 관통). 정준 외 전부 loud(|-> w/clock·OR-clocks·multi-term·disable/action+2clk·mid-seq @). 동시-틱 tie=prior-handoff(NBA region)=hand-IEEE 핀(determinism 테스트). 적대 2홀: HIGH c2>c1 시 level-handoff 매 c2 재발화(1매치→다발화)→set-only+single-shot discharge·LOW 메시지 `expected <X>` 프레임 깨짐→명사구. **A4 generate-scope decls + property-references-property**(`43d41c2`, 1088 green, IR-0·schema_hash 무변경): (1) `collect_gen_sva_decls`가 generate 구조(For/If/Case/Block/nested) 재귀 수집(const-eval 0·genvar 미바인드=use-site loud·both-branch first-wins+W3056); (2) bare named OVERLAP property consequent `…|->q`→`!q.ante||q.cons`(단일-틱 `b|->c`≡`!b||c`, `flatten_overlap_property` 재귀 체인 p→q→r·`sva_inline_stack` 사이클 가드·`sva_clock_signal` span-무관 클럭 매치). loud: 다른/multi 클럭·disable·cons-clock·formals·inner `|=>`·non-boolean·recursion·net이 동명이면 leaf 승. **적대 4렌즈 410+런 0확인결함**(트랙 최청정 슬라이스). ⭐교훈: 슬라이스마다 적대 리뷰가 인접/일반 결함 적발(A1 resolve_named_top silent-wrong, A3 c2>c1 over-fire) — 라이브 재현 필수. Sensitivity PartialEq는 span 포함→클럭 비교는 span-무관 헬퍼 필요.
   - **🏁 트랙 A 이후 후속 슬라이스(2026-06-16/17, 1097→1125 green)** — 각 TDD+적대 4렌즈, 전부 format_version 8 유지(순수 IR-0):
     - **(R2) SVA outer-`|=>` prop-ref skew 기본형**(`3ba7763`, 1097 green, IR-0·sim-ir frozen·AST 무변경): A4가 OVERLAP inner(`!b\|\|c`)만 평탄화하던 것을, inner가 NonOverlap `q: @(clk) b |=> c`인 `a |-> q`를 `(a&&b) |=> c`(overlap outer+boolean antecedent IEEE 항등)로 재작성해 기존 top-level `|=>` pend-reg에 1-클럭 skew 위임. `flatten_prop_consequent` inner-`|=>` fold 분기(+`peel_nonoverlap_property` 비방출 프로브·`is_property_name`). **고급형 잔존(loud)**: 2-cycle outer+inner `|=>`·시퀀스 outer antecedent·양측 중첩 prop-ref·다른/multi 클럭·inner disable iff/formals/consequent_clock. 오라클 NULL→hand-IEEE(800-waveform fuzz+~37 경계, hand-expanded 등가). 적대 4렌즈 CLEAN.
     - **(R) repeat-event intra-assignment blocking**(`cfef719`, 1104 green, IR-0·AST flip=`.vu` 재핀·sim-ir frozen): IEEE §9.4.5 `lhs = @(ev) rhs`·`lhs = repeat(n) @(ev) rhs`=capture-now/wait-n/write(`tmp=rhs; @(ev)×n; lhs=tmp`), EventCtrl lowering 재사용. **iverilog 13.0 지원→차분 오라클**(value+`$time` tick-for-tick). `lower_intra_event_assign`가 count를 **scope-aware** fold(파라미터 count 동작; `Stmt::Repeat`의 scope-blind fold는 wait 묵살이라 미사용), 런타임/over-cap count=loud E3009, `repeat(0)/(<0)`=즉시 write. NBA `<= @(ev)` 형=out-of-scope(파스 advisory). 적대 4렌즈: count-fold 묵살 결함 1건 발견·수정.
     - **(B) faithful deferred immediate asserts**(`19ff00a`+`592cb10`, 1125 green, IR-0·사이드카+엔진 Observed/Reactive 리전·sim-ir frozen fmt_ver 8): `assert #0`=Observed·`assert final`=Reactive(§16.4 deferred — end-of-sim `final` 블록 아님). 어셔션당 flush 마커(`DeferMarkTable`/`DeferActTable` trailer)+엔진 `Postponed` 2맵·NBA-후 Observed→Reactive 성숙(§4.4 order). flush 키=`(marker,aid,gen)`(설계 트레이스가 plan의 action-StmtId 키 잠복버그 선제 적발). 오라클 NULL(iverilog deferred 거부)→hand-IEEE. 적대 4렌즈 HIGH 2건 수정: D1(재활용 aid가 완료 prior 인스턴스 실패 flush→소멸=`Activity.gen` 세대 스탬프)·D2(action 인자 maturation 재평가=reach서 메시지 렌더).
- **그 외 결정 후보**(스코프 결정 영역) — **§4.4 스케줄 표 정본**: class/OOP·`program`·union(검증 생태계) · 커버리지 · math transcendentals(순수-Rust libm 핀). **~~automatic/recursive 콜스택 모델~~ ✅ B-track 2026-06-17 완료**(§4.1). **SVA 트랙 A1–A4 + R2(outer-`|=>` skew 기본형) 완료**; 잔여 SVA(별개·후속)=multi-term 시퀀스 cross-clock(A3 확장)·outer-`|=>` prop-ref skew **고급형**(2-cycle·시퀀스 antecedent·중첩·cross-clock; 기본형 ✅ `3ba7763`)·action block의 sampled local var·sequence local variable·recursive property.
- 조건부/장기 트랙(PDES·cycle-based·native lane·VHDL)은 각자의 트리거(워크로드 증거/수요) 충족 시에만.

### 4.4 다음 구현 스케줄 (2026-06-17 — N1 완료 후 갱신)

> Phase-1 큐 + Phase-2 system-tasks + SVA 트랙 A + **frame-call B-track** + **N1(NBA repeat-event)** 소진 후 남은 언어/검증 커버리지 후보. 원칙 불변: IR-무변경(IR-0) 선행, 오라클(iverilog) 차분 가능 영역 우선, frozen-IR 변경은 한 번의 bump에 일괄, 진입 전 §F식 관문 평가(스파이크→설계→구현). 권장 순서 = 표의 **추천** 열(⭐ = 다음 착수 권장).

| 항목 | 제목 | 설명 | 추천 |
|---|---|---|---|
| ~~**N1**~~ | ~~NBA repeat-event (`x <= [repeat(n)] @(ev) rhs`)~~ | ✅ **2026-06-17 완료(`f4aab23`, 1161 green)**: capture-now(RHS+LHS-index)/`fork…join_none`/NBA-write desugar, EventCtrl 재사용·`repeat(0)`=plain NBA. 순수 IR-0(sim-ir/fmt_ver 8 무변경, `Stmt::NonBlocking.event` AST flip=`.vu` 재핀). iverilog 차분 오라클 tick-for-tick. 적대 4렌즈→**2 silent-wrong 수정(blocking 쌍둥이 동봉)**: X/Z 상수 count=0회(런타임 오분류 수정)·const part-select `[msb:lsb]` lvalue 1-bit temp(width fold Add/Sub 재귀). 동시-틱 region tie=pin(committed 값 일치, vita LRM-faithful). | ✅ 완료 |
| ~~**N2**~~ | ~~SVA 잔여(multi-term cross-clock·고급 prop-ref skew·sequence local var·recursive property)~~ | ✅ **트랙 종결(2026-06-18)**: **N2a-1**(`e804546`, multi-clock seq antecedent `@(c1) a ##1 @(c2) b |-> c`) · **N2b**(`9fbb02d`, 2-cycle non-overlap prop-ref skew) · **N2d**(`7a2fe9c`, property-level `and`/`or` + recursive property: `PropExpr` 트리→per-clock viol 환원, always/weak-until idiom; 적대 4렌즈→HIGH silent-wrong 수정=skew 추적+parser bracket depth+nesting cap) · **N2a-2**(`e84656f`, cross-clock N-clock chains + `|=>` consequent: `synth_crossclock` 일반화=`##1` 핸드오프 체인+`hf_final` 추가단, `collect_xclock_segments` 평탄화; 적대 3렌즈→timing/scope CLEAN·flatten 2 verdict-safe fix). 전부 순수 IR-0. **⏸ N2c**(sequence local var = per-attempt thread storage가 thread-collapsing 파이프라인과 근본 상충 → honest loud reject, defer 확정). **잔여 후속**(별도 트랙, 저우선): cross-clock multi-term segment lane. | ✅ 완료(N2c=defer) |
| ~~**N3**~~ | ~~hierarchical 이름 참조(읽기 한정)~~ | ✅ **완료(2026-06-18, `a95b44b`, 1233 green)**: `tb.dut.x`/`top.m.l.v` 식-내 read를 deferred-ref+arena-patch로 해소(child net=pass8라 placeholder Signal→전 인스턴스 elaborate 후 `resolve_deferred_hier` 패치). 순수 IR-0·elaborate-only(sim-ir/fmt_ver 8·AST `.vu` 무변경). `hier_lookup`=IEEE §23.6 commit-to-scope(leading seg를 innermost scope에 바인드 후 그 안에서 remainder, 미발견=error·outward 미계속). iverilog 강한 오라클 value-pin. 적대 3렌즈→**HIGH silent-wrong 2 수정**: wrong-net(whole-tail outward strip→commit-to-scope)·packed multidim element silent 1-bit(→loud, element select=deferred lane)+named genblock `gblk.x` 해소. WRITE=loud(read-only). 디퍼(loud): indexed-seg parse(`g[0].x`)·array-element read·$dumpvars hier arg·hier param. | ✅ 완료 |
| **N4** | clocking block | ⛔ **조건부 NO-GO(2026-06-18, 관문 평가)**: 파서·`cb.sig` 입력샘플 read·`@(cb)` 전부 시제(試製)했으나 — **clocking 입력 샘플링은 Preponed 리전이 필수**(IEEE §14.13), vita 스케줄러엔 부재. NBA 샘플러(`always @(ev) cb_q<=sig`)는 관용구 `@(cb); x=cb.sig`(엣지서 read)서 **1 clocking-cycle lag**(read는 Active서 resume, 샘플러 NBA는 그 후 settle → 직전 엣지 샘플 반환; 실측 `cb.q=11` vs 정답 `12`)=silent-wrong. live-read는 엣지-read 정답이나 지연-read 오답 → 양쪽 만족 불가. iverilog는 clocking 자체 거부(오라클 無)=hand-IEEE. **재진입 조건**: Preponed/sampled-value 스케줄링 리전 도입(신규 인프라). 시제 코드는 revert(미커밋). | ⛔ NO-GO(Preponed 리전 필요) |
| ~~**N3.1**~~ | ~~hierarchical 배열 element read (`dut.mem[i]`)~~ | ✅ **완료(2026-06-18, `95cc674`, 1240 green)**: 2-seg hierarchical base의 indexed read를 deferred(index를 lowering-time에 lower→`idx_eid` 저장, base net은 fixup서 `hier_lookup`→shape로 select 빌드: single-dim unpacked array=`net[idx-lo]`·scalar/vector=bit-select; multi-dim/packed/dyn=loud follow-on). 순수 IR-0(AST 무변경). iverilog 강한 오라클 value-pin. 적대 2렌즈→**HIGH silent-wrong 수정**(index가 function formal일 때 fixup 재-lower가 subst 소실→shadowing outer net 묵묵 read; f=10 vs 13) + MEDIUM 3(param/genvar index fold·nested hier index) 전부 lowering-time lower로 일괄 수정. 22 테스트(teeth 4). | ✅ 완료 |
| ~~**N3.1-followon**~~ | ~~multi-dim `dut.grid[i][j]` + packed `dut.pm[i]` hierarchical element~~ | ✅ **완료(2026-06-18, `7d2f9b4`, 1252 green)**: `DeferredHierSelect.idx_eid`→`idx_eids: Vec`(모든 인덱스를 lowering-time lower=N3.1 HIGH fix를 전 인덱스로 확장)·신규 `hier_sel_chain`(BitSelect 체인이 2-seg hierarchical Ident에 bottom, known-net=interface alias는 normal path)·`resolve_deferred_hier_sel`가 resolved net shape로 dispatch: unpacked array→`build_hier_array_read`(local `lower_array_read` 미러, row-major flat word=신규 eid-sourced `flatten_word_eids`)·multi-dim packed→`build_hier_packed_read`(`lower_packed_read` 미러). `flatten_word_eids`는 `flatten_word`의 별도 복사라 local push order=골든 byte-불변. iverilog 강한 오라클(non-zero-based·descending·asymmetric·array-of-packed whole-elem 전부 일치). 적대 4렌즈(워크플로우, 라이브 iverilog)→**HIGH silent-wrong 수정**: array-of-packed(`reg [3:0][7:0] qm[0:1]`=net_is_static_array AND packed_dims)가 array path로 가서 sub-index `dut.qm[i][j]`를 silent 1-bit select(vita 1 vs iverilog byte bb)=follow-on 前 loudness 회귀 → 가드(array-of-packed+surplus index=loud deferred, whole-elem `dut.qm[i]`=정상 32-bit). loud(silent-wrong 0): partial slice·bit-of-bit·scalar multi-index·packed over-index·hier WRITE·event/dyn·3-seg indexed. inner-dim OOB=local parity(X, §7.4.6). **잔여(별개 pre-existing)**: LOCAL `lower_array_read`/`collect_array_write`도 array-of-packed sub-index를 silent 1-bit(read+write 공유 gap)→전용 슬라이스. 33 테스트(teeth: formal-index·sub-index-loud·whole-elem·OOB). | ✅ 완료 |
| **N5** | 커버리지(covergroup/coverpoint/cross/bins/options) | **✅ 완성(2026-06-19, 1516 green, 6커밋 `acc801b`→`398f0f8`, DEVLOG 46탄)**: `covergroup NAME[(@clk)]; [LBL:] coverpoint EXPR [iff(g)] { bins/ignore_bins/illegal_bins/default [iff] } [option.at_least/weight]; cross a,b; endgroup` + `cg c=new;`·`c.sample()`·real `c.get_coverage()`. **iverilog covergroup 전면 거부→hand-IEEE §19.5**(전 % hand-computed). 순수 IR-0(parser+elaborate, sim-ir/fmt_ver 9 무변경, AST `.vu` 재핀 A/F/C/D). 64-bit hit-bitmap=per-bin counting-bit(membership 술어 합성·`ignore∪illegal` subtract·illegal=`$error`)·explicit 0-bin은 auto-fallback 금지(`has_explicit` 디스패치)·at_least>1=per-bin 0-init saturating 카운터·cross=같은-sample-AND product bitmap·get_coverage=real `Σ(wᵢ·covᵢ)/Σwᵢ` per-cp 가중평균·`@(clk)`=`always inst.sample()` 합성. **적대 워크플로우→silent-wrong 3 수정**(0-bin auto-fallback cardinal·pooled-vs-per-cp-avg 잠복·at_least X-init 카운터). 39 coverage 테스트(8 legacy byte-identical 보존). **N5-G 완료(2026-06-19, 47탄)**: `coverpoint_domain` 정밀 자기결정폭(binop/shift/concat/replicate/select/compare/function-call)+**cardinal 마스크 수정**(bin index `& (num_bins-1)`=W-절단+캡; 정밀화가 hardcode `&63`의 >100% silent-wrong을 노출). **잔여(loud 한계)**: cross-of-iff·cross-level option·wildcard/transition/binsof/fixed `bins[N]`. | **✅ 완료(N5-G 포함)** |
| ~~**N3.2**~~ | ~~LOCAL array-of-packed sub-element read/write (`qm[i][j]`)~~ | ✅ **완료(2026-06-18, 1261 green)**: `lower_array_read`/`collect_array_write`가 net이 `packed_dims`에 있고 unpacked D개 뒤 trailing 인덱스가 있으면 packed-dim flatten으로 라우팅(offset=`flatten_word(packed_dims,trailing)`·width=잔여 packed dim 곱·`SelKind::PartIdxUp`, base=`word:Some(element)` — `lower_packed_read`/`collect_packed_write` 미러). 엔진 무변경(`{word:Some,PartIdxUp}`가 `write_chunk`서 `base+off` 착지). 순수 IR-0(sim-ir/AST/schema/fmt_ver 8 무변경). iverilog 강한 오라클(descending byte-exact: read/write·3-packed-dim·non-zero-based·signed·var-index·OOB sentinel·concat-width 전부 일치). plain array/vector/packed net=byte-identical(`net_is_static_array`+`packed_dims`+trailing 게이트). 적대 3렌즈(라이브 iverilog ~30 probe)→SHIP_AS_IS, in-scope 결함 0. | ✅ 완료 |
| ~~**N3.3**~~ | ~~ascending packed-dim direction (`reg [0:3][7:0]`)~~ | ✅ **완료(2026-06-18, 1270 green)**: `packed_extents`/`packed_dims`가 per-dim `ascending`(msb<lsb) 기록, `flatten_word`/`flatten_word_eids`가 ascending 차원에서 `coord = hi - idx`(=`lo+size-1 - idx`, lsb 엔드포인트 = 내부 bit 0; `norm_offset_for_net` 미러). 신규 `packed_split` 헬퍼로 5개 PACKED 호출부(lower_packed_read·collect_packed_write·build_hier_packed_read·N3.2 array-of-packed r/w 2곳)는 방향 전달, 9개 UNPACKED 호출부는 `&[]` 전달 → descending+unpacked **byte-identical**(골든 0 flip). 순수 IR-0(sim-ir/AST/schema/`fmt_ver 8` 무변경). iverilog 강한 오라클(asc read/write·mixed asc-desc·asc-inner bit-select·3-dim·non-zero base·parametrized·OOB→xx·X→xx·packed port·hierarchical read 전부 byte-exact). 적대 3렌즈(워크플로우, 라이브 iverilog)→**0 finding·0 regression**(correctness/byte-identity/completeness 전부 CLEAN). 18 테스트(9 신규 `packed_ascending_n33.rs` + 9 `array_of_packed.rs` 정정). **적발(별개)**: N3.4 part-select-on-packed. | ✅ 완료 |
| ~~**N3.4**~~ | ~~part-select on multi-dim packed (`x[3:2]` element-range)~~ | ✅ **완료(2026-06-19, 1354 green, `63e7070`)**: read(`try_packed_part_select` in lower_expr)+write(`try_packed_part_select_lval` in collect_lval_chunks)가 bare 다차원 packed net(packed_dims, ≥2 dims, const descending)의 part-select를 OUTER 차원 원소-범위로 — offset=`flatten_word([lsb])`·width=count×elem_w·`PartIdxUp`, `lower_packed_read` 미러. plain vector/ascending(loud)/단일차원은 fall-through. 순수 IR-0(elaborate lowering only). iverilog 강한 오라클(`x[3:2]`=aabb·write 12340000·3-dim·non-zero-base `[5:2]`). 적대 3렌즈→**HIGH 1 수정(N3.4가 NEW 유발)**: 상한-OOB `x[4:2]`(msb>outer hi)가 lsb만 검증돼 net 너머 silent r/w → full-range 검증+E3009 loud(iverilog 컴파일 거부 매칭). determinism/VM/staged/golden CLEAN. | ✅ 완료 |
| ~~**N3.4-followon**~~ | ~~array-of-packed part-select `qm[0][3:2]` + const indexed part-select `x[c+:w]` + nested range-idx loud~~ | ✅ **완료(2026-06-19, 1363 green, `24162ff`)**: 세 자매 케이스, 순수 IR-0(sim-ir/AST/schema/fmt_ver 8 무변경). ①array-of-packed 원소 part-select `qm[i][msb:lsb]` r/w → OUTER 원소-범위(신규 `packed_ps_base`/`_lval`가 base를 `(net, Option<elem_word>)`로 해소). ②const indexed part-select `x[c+:w]`/`x[c-:w]`(bare+array elem)→원소-범위 fold(신규 `try_packed_indexed_part(_lval)`+`packed_indexed_range`, `packed_outer_range`=공유 const-range core, offset=(l-olo)*elem_w value-identical). var offset `x[i+:2]`=loud(iverilog abort, 오라클 無). ③nested range-then-index `x[3:2][0]`/`v[3:0][0]`=loud(iverilog 보편 거부). 적대 3렌즈(라이브 iverilog)→**HIGH M1 수정**: `-:` low-boundary `x[0-:1]`/`x[1-:2]`(최저 원소=index 0)가 `c-w+1`의 `c-w` 선평가로 u32 underflow→debug panic → `(c+1)-w`(가드가 c+1≥w 보장). det/VM/staged/golden CLEAN. **deferred(MEDIUM, 문서화·loud-never-wrong)**: ascending outer-dim packed part/indexed-part `reg [0:3][7:0]; x[0:1]`(iverilog=aabb)는 loud 유지=별개 ascending 슬라이스. 10 iverilog-pin 테스트. | ✅ 완료 |
| **N6** | math transcendentals (`$ln`/`$sin`/…) | real-math system 함수 + `$dist_*` 초월. **2026-06-19 사용자 결정=DEFER 유지(Conditional)**: 플랫폼 libm은 OS마다 last-ulp 발산→3-OS byte-identical 골든 계약 위반; 순수-Rust `libm` 핀이 결정성 복원 유일 길이나 **의존성 0 기조 유지로 미도입**. expr-form=loud E3009. 도입 결정 시 구현은 작음. | **Conditional** — libm 핀 도입 시(defer 확정) |
| ~~**N7**~~ | ~~class / OOP (코어+상속+가상)~~ | ✅ **완료(2026-06-19, 1578 green, DEVLOG 48탄, 전부 IR-0)**: `class`/data members(정확폭+§8.8 디폴트)/`new`(+ctor args)/`this`/handle+null/ref-copy/단일상속 `extends`+`super`/non-virtual+**virtual 동적 디스패치**(vtable, hand-IEEE: iverilog 13 가상버그보다 정확)/value+void 메서드/`return`/begin-less 다중문 바디. 핸들=`NetKind::Integer` obj-id + 엔진 `class_heap`(RefCell) + layout/vtable 사이드카(골든 무변경). 적대 워크플로 6렌즈→**8 silent-wrong 수정**(forge use-after-free·formal-shadow `this.v=v`·keyword-less virtual·>32bit 필드 절단·필드 shadowing·handle↔int leak·handle 산술). **defer(loud)**: randomization/program/union/virtual interface/chandle/parameterized/static·local·real·string·array 멤버·staged-vrun. §4.5 참조 | ✅ 완료(코어+상속+가상) |

> 조건부/장기(PDES BSP·cycle-based 컴파일드·native-eval 잔여 lane·VHDL front-end) = §4.1 하단 행, 트리거 충족 시.

### 4.5 미구현 기능 전체 인벤토리 (format_version 9, ~1608 tests green)

> **2026-06-18 작성 · 2026-06-20 갱신**(50탄 dyn 2-state fix + 스펙 전수 재감사) — `docs/preview/` SPEC 전수 감사(워크플로우 6 버킷 audit→적대 verify→synth, 각 항목 `crates/` grep+`vita` 라이브 확인) 결과를 단일 표로 통합. **권장 구현 우선순위 순**(작은 정확성 핀 → 결정-차단 소형 → 대형 검증 트랙 → SVA/계층 후속 → 조건부/NO-GO → 장기 → 비계획). `N3.2`/`N4`/`N5`/`N6`/`N7`은 §4.4 라이브 트래커 id 재사용. 상태 의미: `silent-degrade`=정확성 결함(우선) · `loud-reject`=명시적 거부(안전) · `deferred`/`NO-GO`=결정·차단 대기 · `won't-fix`/`out-of-scope`=계획 없음. **이미 구현된 기능은 §4.1~§4.4 + doc-01 freeze 표가 정본**(여기 안 나오면 구현됨).

#### 계획됨 (Planned)

| 순위 | id | 항목 | 상태 | 우선순위 | 비고 / blocked-on |
|---|---|---|---|---|---|
| ~~1~~ | ~~N3.2~~ | ~~LOCAL array-of-packed 서브원소 read/write (`qm[i][j]`)~~ | ✅ **완료(2026-06-18, 1261 green)** | — | packed-dim flatten 라우팅(IR-0, `lower_packed_read` 미러), 적대 3렌즈 SHIP_AS_IS. §4.4 참조 |
| ~~1~~ | ~~N3.3~~ | ~~ascending packed-dim direction (`reg [0:3][7:0]`)~~ | ✅ **완료(2026-06-18, 1270 green)** | — | `packed_extents`/`packed_dims` per-dim `ascending`+`flatten_word(_eids)` `coord=hi-idx`(IR-0, `packed_split` 헬퍼로 5 packed 호출부만 방향 전달, 9 unpacked=`&[]` byte-identical). 적대 3렌즈 0-finding. §4.4 참조 |
| ~~1~~ | ~~N3.4~~ | ~~part-select on multi-dim packed (`x[3:2]` element-range)~~ | ✅ **완료(2026-06-19, 1354 green, `63e7070`)** | iverilog | read+write가 OUTER 차원 원소-범위 선택(`try_packed_part_select`/`_lval`→`packed_outer_part_select`+`flatten_word([lsb])`·width=count×elem_w·PartIdxUp, `lower_packed_read` 미러). 순수 IR-0. 적대 3렌즈→HIGH 1 수정(상한-OOB `x[4:2]` full-range 검증+E3009 loud=iverilog 거부 매칭). **신규 follow-on(pre-existing, out-of-scope)**: array-of-packed 원소 part-select `qm[0][3:2]`·indexed-part `x[i+:2]` on multi-dim packed·nested range-then-index `x[3:2][0]` |
| ~~2~~ | ~~N6~~ | ~~math transcendentals on real~~ → **Conditional (defer 확정)** | ⏸ **DEFER 확정(2026-06-19 사용자 결정)** | Conditional | 순수-Rust `libm` 핀 도입은 결정성 복원의 유일 길이나 **의존성 0 기조 유지로 미도입** → loud E3009 유지. `$dist_*` 초월도 동반 defer. 도입 결정 시 소형. 조건부 섹션 참조 |
| ~~3~~ | ~~SYS-READ~~ | ~~file READ family (`$fread`/`$fscanf`/`$fgets`/`$sscanf`/`$feof`/`$fgetc`) + `$writememb`/`$writememh`~~ | ✅ **완료(2026-06-18~19, rank 5, 9커밋, DEVLOG 36–38탄)** | — | `$writememb/h`(readmem 미러)·`$fgetc`/`$feof`/`$ungetc`(fopen StmtEffect intercept 패밀리)·`$fgets`/`$fread`(목적지 write+count)·`$fscanf`/`$sscanf`(scanf 포맷 파서+멀티 ref-write). 전 거동 iverilog 라이브 검증. 적대 리뷰가 silent-wrong 다수 적발(wide값·4-state·NUL·x-z·write-mode) |
| ~~4~~ | ~~SYS-INTRO~~ | ~~introspection + `$changed`/`$sampled` + `$dist_*` + `$countbits` + `$monitoron/off` + `$exit` + `$system`~~ | ✅ **완료(2026-06-19, DEVLOG 47탄)** | — | array-query const-fold(rank 2)·`$changed`/`$sampled`(rank 3)·`$dist_uniform`/`$cast`/`$monitoron/off`(rank 6) 기완료. **47탄(IR-0, no bump)**: `$countbits`(per-bit case-eq Σ desugar)·`$typename`(타입 메타 const-fold, signed 한정자·string 포함)·`$exit`(=`$finish` 재사용). 적대 3 fix(string typename·countbits-real guard·signed 한정자). **잔여(defer/out-of-scope, loud)**: transcendental `$dist_*`(N6/libm)·`$system`(3-OS-hostile shell-out=out-of-scope) |
| ~~5~~ | ~~DIR-PP~~ | ~~전처리기 잔여: token-paste·stringification·`` `begin_keywords ``·`` `unconnected_drive ``~~ | ✅ **완료(2026-06-18, Medium 묶음 rank 1, 1286 green)** | — | `substitute()`에 token-paste(``` `` ```=두 backtick 삭제·whitespace 보존, iverilog parity `a``b`→ab·`a `` b`→`a  b`)+stringification(`` `" `"`` 내부 param 치환·`` `\`" ``→`\"`·내부 paste 삭제) 추가, **object-like 매크로도 `substitute` 경유**(empty params=identity 증명). 4 directives accept-ignore(begin_keywords/unconnected_drive=arg line 소비, end/no-드라이브=token만; keyword-set narrowing은 미구현=loud-reject 한계). 순수 IR-0(hdl-preprocess only, sim-ir 무관). 13 unit+4 e2e, 적대 3렌즈(HIGH refuted=vita가 iverilog와 이미 일치, regression/scope CLEAN). §4.5.1 참조 |
| ~~6~~ | ~~N5~~ | ~~coverage (covergroup/coverpoint/cross/bins/options) + N5-G expr-width~~ | ✅ **완성(2026-06-19, DEVLOG 46·47탄)** | — | bins/ignore/illegal/default·iff·cross·option.at_least/weight·real-% per-cp 가중평균·`@(clk)` auto-sample(46탄) + **N5-G 정밀 expr-coverpoint 폭(47탄)** 전부 hand-IEEE §19.5. 순수 IR-0. silent-wrong 4 수정(46탄 3 + 47탄 cardinal `&63`→`&(num_bins-1)` >100% 마스크). §4.4 참조 |
| ~~7~~ | ~~N7~~ | ~~class/OOP (코어+상속+가상)~~ | ✅ **완료(2026-06-19, 1578 green, DEVLOG 48탄)** | — | **코어+상속+가상 완결**(IR-0): class·필드·`new`/ctor·this·handle/null·ref-copy·`extends`/`super`·virtual 동적 디스패치(vtable)·value+void 메서드·`return`. 핸들=Integer obj-id+엔진 `class_heap`(RefCell)+사이드카. 적대 6렌즈→8 silent-wrong 수정. **defer(loud)**: randomization(constraint solver)·`program`·`union`·virtual interface·chandle·parameterized class·static/local/protected·real/string/array 멤버·staged-vrun. §4.4 참조 |
| ~~8~~ | ~~SVA-REST~~ | ~~SVA 잔여 property ops·cover·let·assertion-control·`[+]`~~ | ✅ **대부분 완료(2026-06-20, DEVLOG 49탄)** | Low | **🏁 SVA-REST 일괄 완료(전부 IR-0·sim-ir/fmt_ver 9 무변경; AST `.vu` 재핀=`PropExpr::{Not,Until,Eventually,Always}`+`Stmt::CoverProperty`+`ModuleItem::LetDecl`/`LetDecl` 1회).** ①property ops `always`/`not`/`implies`/`iff`/`until`(weak safety)·`s_until`/`s_eventually`(strong liveness=end-of-sim `final` pend 체크)·`nexttime`(=`1'b1 \|=> p`) — 새 precedence 래더(`implies`/`iff`<`until`<`or`<`and`<unary<impl), `prop_expr_violation` 확장 + `synth_liveness`(arm/clear pend + `final $error`). ②`cover property`=clocked match 카운터+end-of-sim `$display`(`synth_seq_match` 추출=flat 경로 byte-identical 공유). ③`let NAME[(formals)]=expr;`=elaborate use-site 치환(named-seq 선례, net이 동명이면 net 우선). ④`$assertoff`/`$asserton`/`$assertkill`=글로벌 엔진 게이트(`assert_fire` StmtId 사이드카+`assert_ctl` 제어사이트→no-op Display, `do_systask` 인터셉트; 유저 `$error`/cover `$display` 미게이트). ⑤`seq[+]`=`[*1:$]` 슈가(atomic `[+]` 토큰=`[+x]` 인덱싱 무충돌). ~~`assume property`~~ ✅ 48탄. **적대 워크플로 6렌즈→8 findings(IR-0 무결성 렌즈=0 confirm), 2 silent-wrong 수정+6 문서화**(수정: let이 동명 function silent-shadow→callable 우선·scoped `$assertoff(lvl,scope)` silent over-disable→loud / 문서화: X-boolean lenient(기존 F2)·`$assertoff/kill` in-flight coarse 게이트·`not` 비-strict precedence·**liveness finish-edge**=`$finish`가 샘플 posedge 정확일치 시 클럭 체커 미샘플→`final` pend stale=기존 스케줄러 동작(`cnt`도 동일)이라 timestep-drain 순서수정=별도 슬라이스 디퍼·우회=finish offset). **잔여(honest loud, 비-silent)**: empty-match repetition `[*0:n]`/`[*0:$]`/`sig[*]`(empty-sequence concat 대수가 fixed-pipeline desugar와 상충)·multi-term cross-clock segment lane(per-clock multi-term 파이프라인+핸드오프 체인=별도 트랙)·N2c sequence local var(per-attempt thread storage 근본상충)·outer-`\|=>` prop-ref skew 고급형 |
| ~~9~~ | ~~HIER-REST~~ | ~~계층 참조 잔여: WRITE·3-seg indexed·indexed-segment parse(`g[0].x`)·whole packed·hierarchical parameter~~ | ✅ **완료(2026-06-19, 1470 green, 7커밋)** | — | **🏁 트랙 완결**: 계층 WRITE(전체-net·element·bit·part-select)·3-seg/다차원 element r/w·whole 다차원 packed r/w(`dut.pm`)·indexed-segment `g[0].x`/`bank[3].c.r`/`g[0].u.r`(parser가 const-idx를 scope-seg `g[0]` 문자열로 fold, AST 무변경)·hierarchical param read·same-module generate packed/array element. 전부 순수 IR-0(elaborate/parser-only, fmt_ver 9 무변경). 적대 워크플로우 2회→**silent-wrong 3 수정**(force-on-select·P0-NZE non-zero-LSB elem bit·`g[0].pm[k]` flat-bit). 잔여 v1 한계(loud-never-silent): non-literal generate idx `g[P].x`·ascending part-select write `v[lsb:msb]`. 상세 DEVLOG 42탄. §4.4 참조 |
| ~~**P0-IPU**~~ | ~~indexed part-select WRITE의 음수/LSB-미만 base offset(`v[-2+:4]`·`v[k-:3]` underflow)~~ | ✅ **완료(2026-06-19, 1477 green)** | — | `write_chunk`이 low net-bit 위치를 **i64 signed**로 유지(`eval_select` READ 측 미러)→부분-OOB에서 in-range 비트만 기록(저비트 drop), 슬라이스 shift/wrap 제거. sim-engine 런타임 수정(IR/골든 무변경; X/Z sentinel·high-OOB=byte-identical). local+hier·`+:`/`-:`·런타임·non-zero-LSB 전부 iverilog 일치. 7 테스트 |
| ~~10~~ | ~~SVPART~~ | ~~부분 SV caveat: 2-state types·import ambiguity·…~~ | ✅ **완료(2026-06-19, DEVLOG 47탄)** | — | **2-state 정수타입** `bit/byte/shortint/int/longint`(렉서+AST `.vu` 재핀+parser+elaborate): X-free init-0 + **X-coercion-on-write**(§6.11.3, `two_state_nets` 사이드카; one-shot, init-0은 staged도) + 정확 width/sign/typename. **wildcard-import 모호성**(§26.8): const **AND func/task** unbind→use-site loud·explicit-win(silent-wrong 수정). 적대→2-state X-retain·routine 모호성 누락 적발·수정. **잔여(loud/pre-existing)**: `$unit`/`$root`·modport subroutine import/export·`export ::*`·typed func-return/for-init local(integer/logic도 거부=pre-existing 파서갭)·X-coercion staged-vrun 미직렬화 |
| ~~11~~ | ~~GATE~~ | ~~gate-level primitives + UDP + gate delays + drive strength~~ | ✅ **프리미티브 완료(2026-06-19, DEVLOG 47탄)** | — | 12 게이트 프리미티브(and/or/nand/nor/xor/xnor/buf/not/bufif0/1/notif0/1)→continuous-assign desugar(다입력 reduction·buf/not multi-output·tristate `ctrl?data:1'bz`·게이트 delay→ContAssign.delay·anonymous/named/multi-instance). 적대→**z-leak silent-wrong 수정**(buf/bufif pass-through z→x via `~~`, IEEE §7.3/§7.4). **잔여(loud/문서화, 비-silent)**: UDP(`primitive`/`table`)·drive-strength·multi-driver 트라이스테이트 버스(=E3001, vita는 wired-net 해소 부재=P1-8 설계)·multi-value `#(rise,fall)` rise-only(=assign 선례) |

#### 조건부 / 장기 (Conditional / Long-term)

| 순위 | id | 항목 | 상태 | 우선순위 | 비고 / blocked-on |
|---|---|---|---|---|---|
| 12 | MVP-CUT | MVP 의도적 컷: string concat/decl-init/port·atoi/itoa·string-키 foreach·wildcard assoc `[*]`·queue range-slice `q[0:2]`·array 메소드(sort/find/sum/with)·package var/internal import/scoped call·cross-frame disable·block-local `automatic`(form2)·~~staged-vrun frame-call 사이드카 직렬화~~ ✅2026-06-22 | loud-reject (documented cut) | Conditional | 각 IN 기능의 의도적 컷(loud). string은 DONE이나 concat=`lib.rs:5107` loud('use `$sformatf`'). **frame-call/class/2-state staged-vrun 사이드카 = ✅ 2026-06-22 STAGED-DROP 해소**(§5, 14번째 trailer; one-shot==staged). 필요 시 개별 해제 |
| 13 | N4 | clocking block (clocking/endclocking, `cb.sig` 입력 샘플, `@(cb)`, default/skew/`#1step`) | NO-GO (conditional) | Conditional | **blocked: Preponed/sampled-value 스케줄링 리전 도입.** 스파이크 후 revert(`@(cb);x=cb.sig`서 NBA 샘플러 1 clocking-cycle lag→`cb.q=11` vs 정답 `12` silent-wrong). iverilog 거부=오라클 無 |
| 14 | BACKEND | compiled/JIT native-eval 잔여 lane(signed>64·>128bit·sysfunc·real) + PDES BSP v1 / cycle-based compiled mode | deferred / NO-GO | Conditional | **blocked(PDES): 지속 W≥64 + grain≥200ns 실워크로드**(Amdahl 상한 T4≈2.5x). native lane=저-ROI VM-only(P5 차분 게이트가 정확성 강제). 현 `--threads`=VCD writer 스레드(byte-identical)만 |
| 15 | VCD-EXT | 확장 VCD `$dumpports*` / FST 파형 포맷 | deferred / out-of-scope | Low | 표준 7 `$dump` 태스크 DONE. `$dumpports*`=deferred(grep 0). FST=명시적 비목표 |
| 16 | VHDL | VHDL 프론트엔드 (entity/architecture/process/wait, std_logic_1164/numeric_std) | deferred (long-term) | Low | Phase-4+. 별도 lexer/parser/AST를 공유 sim-ir 위에('parse까지만 언어 의존' 근거). E7xxx VHDL 밴드 예약. SV 트랙 완료 후 |

#### 비계획 (정책 / out-of-scope)

| 순위 | id | 항목 | 상태 | 우선순위 | 비고 |
|---|---|---|---|---|---|
| 17 | DEFPARAM | `defparam` | won't-fix | Wont-fix | 파서가 `defparam` 키워드를 E2002 거부(parse-time loud; `ModuleItem::Defparam` AST variant + elaborate E3009 핸들러는 존재하나 파서가 도달 전 거부=dead). IEEE deprecated; `#(.param())`로 충분. W3052 예약. 계획 없음 |
| 18 | IMPLICIT-NET | `` `default_nettype `` / implicit nets (추론) | policy loud-reject (E3010) | Wont-fix | 정책=명시 에러(추론 없음, 사실상 `default_nettype none`). W2003 예약. worklib 이후 옵션화만 검토 |
| 19 | OOS | synthesis tool 자체·waveform GUI viewer·UPF/power·SDF back-annotation·DPI-C·shortreal·trireg charge-strength/scalared/vectored·UVM ecosystem(N5 제외)·`unique`/`priority` 다중-match 검사 | out-of-scope | Out-of-scope | 설계 의도상 비목표(gap 아님). shortreal=키워드 없음(real만). trireg=bare 선언만 parse. 계획 없음 |

> **요약:** 정확성 핀(N3.2~N3.4-followon ✅) · Medium 묶음(SYS-READ·SYS-INTRO·DIR-PP) ✅ · HIER-REST ✅ · P0-IPU ✅ · N5 coverage ✅ · 잔여 low-priority 4종(N5-G·SYS-INTRO잔여·SVPART·GATE) ✅(DEVLOG 47탄) · **🏁 N7 class/OOP 코어+상속+가상 완료(2026-06-19, 1578 green, DEVLOG 48탄): class·필드·new/ctor·this·handle/null·ref-copy·extends/super·virtual 동적 디스패치(vtable, hand-IEEE)·value+void 메서드·return; 동반 파서갭 수정(typed func-return·return 문·begin-less 다중문 바디); 적대 6렌즈→8 silent-wrong 수정; defer=randomization/program/union/vif/chandle/parameterized/static/local/real/string/array 멤버** · **🏁 SVA-REST 완결(2026-06-20, 1607 green, DEVLOG 49탄)**: property ops(`always`/`not`/`implies`/`iff`/`until`/`s_until`/`s_eventually`/`nexttime`)·`cover property`·`let` 매크로·`$assertoff/on/kill` 런타임 게이트·`seq[+]`=`[*1:$]`·`assume property` — 전부 IR-0(sim-ir/fmt_ver 9 무변경, AST `.vu` 재핀만). liveness=end-of-sim `final` pend 체크. · **N6=DEFER 확정(사용자 결정, libm 0-dep 유지)**. · **🔴 dyn array `new[N]` 2-state 원소 기본값 수정(2026-06-20, 1608 green, DEVLOG 50탄)**: spec 전수 재감사가 적발한 유일 신규 silent-wrong — `int/bit/byte/shortint` dyn 원소가 X(정답 0, iverilog 오라클)였음. `two_state_heap_handles` 사이드카+`DynNew` 2-state 분기로 수정(IR-0). OOB/missing-element read의 X-default는 별개 문서화 design pin이라 미변경. **➡️ Planned 비었음(silent-wrong 0) — 잔여는 전부 honest loud(비-silent)**: empty-match repetition `[*0:n]`/`[*0:$]`/`sig[*]`(empty-sequence concat 대수 상충)·multi-term cross-clock segment lane·N2c sequence local var(thread-storage 근본상충)·outer-`\|=>` prop-ref skew 고급형. 조건부/장기 = N6(libm)·N7-REST(randomization/program/union/vif/chandle/parameterized/static — constraint solver 등)·MVP-CUT·N4 clocking·BACKEND·VCD-EXT·VHDL. 비계획 = defparam·implicit-net·out-of-scope. 저우선 후속(별개 슬라이스, loud·비-silent) = ascending outer-dim packed part-select·non-literal generate idx `g[P].x`·class 멤버 shadowing·`$dist_*` transcendental(libm)·`$system`·UDP·multi-driver tristate bus.

#### 4.5.1 Medium 묶음 게이트 플랜 (2026-06-18, 8-agent 워크플로우)

SYS-READ·SYS-INTRO·DIR-PP를 IR-0-now vs 단일 **v9 bump**으로 분할(워크플로우 understand×3+probe×4→synth). **원칙: IR-0 슬라이스 먼저, frozen-IR(새 SysFuncId/SysTaskId)은 한 번의 v9 bump에 일괄, iverilog 오라클 우선.** ⚠️ 진단 비대칭: 미인식 `$func`=E3009-LOUD(전 디자인 kill) vs `$task`=W3056-skip → 미구현 introspection func가 디자인 전체를 죽이므로 const-fold(rank 2)가 고가치.

| rank | 슬라이스 | items | IR-0/bump | 오라클 | 상태 |
|---|---|---|---|---|---|
| 1 | DIR-PP | token-paste·stringify·begin_keywords·unconnected_drive | IR-0 | iverilog | ✅ 완료 |
| ~~2~~ | ~~SYS-INTRO const-fold~~ | ~~`$size`/`$left`/`$right`/`$low`/`$high`/`$increment`/`$dimensions`/`$unpacked_dimensions`/`$isunbounded`~~ | ✅ **완료(2026-06-18, 1300 green)** | iverilog(+hand-IEEE) | 신규 `dim_desc`(NetId→ordered (left,right) per dim, unpacked-then-packed) **decl 시점 AST range서 계산**(`record_dim_desc`, netvar+port 양 경로)→`try_introspect_fold`가 32-bit Const(IR-0, `$bits` 선례). dim 순서=unpacked decl순→packed; `$increment=left>=right?1:-1`; 0-dim scalar/OOB dim→32-bit X. **scalar `reg s`(0-dim)≠`reg [0:0]`(1-dim)** 구별(net msb/lsb로는 불가). 적대 3렌즈→**HIGH 2 regression 수정(loud→silent-wrong)**: integer/time=implicit packed dim(31:0/63:0, real은 0-dim 유지)·ANSI packed multidim port=port-path `record_dim_desc`. **`$typename`=defer**(string 포맷, hand-IEEE). const-context fold(localparam/range spec)=pre-existing 미지원(slice 무관)=한계 박제 |
| ~~3~~ | ~~SVA sampled desugar~~ | ~~`$changed`(=`$past!==e`)·`$sampled`(=identity)~~ | ✅ **완료(2026-06-18, 1303 green)** | hand-IEEE | `is_sva_sampled_fn`+`rewrite_sampled`에 추가: `$changed`=`CaseNe(prev,e)`(=$stable 부정)·`$sampled(e)`=`e`(identity, 재귀로 nested 해소). 기존 prev-reg 머신 재사용(IR-0). 직접 엣지 probe(hier/multiarg/outside-assertion=loud·nested ok). **`$countbits`=defer**(iverilog 13.0 crash=오라클無, x/z 카운트는 `$countones`로 desugar 불가→per-bit case-eq IR-0 OR v9 SysFuncId, 저우선) |
| ~~4~~ | ~~**v9 shape bump (1회)**~~ | ~~SysFuncId×13(Fgets/Fscanf/Sscanf/Fread/Feof/Fgetc/Ungetc/Dist{Uniform,Normal,Exponential,Poisson,ChiSquare}/Cast) + SysTaskId×5(WritememB/WritememH/Cast/MonitorOn/MonitorOff) + fmt_ver 8→9 + golden regen~~ | ✅ **완료(2026-06-18, 1303 green)** | — | **format_version 8→9.** 18 variant 일괄 추가(SysFuncId×13 append-after-StrCmp, SysTaskId×5 append-after-StrPutC). golden regen=`REGEN_GOLDEN=1`(canonical txt + registry.ron)→canonical diff=enum append 2줄만(타 형상 무이동), 신규 `SimIr` 해시 `ff9d5be…`. **Process 서브핀 UNCHANGED**(`61db2e…`)=Process는 Stmt/Expr를 arena INDEX(u32)로만 도달→루트만 flip, 국소성 확정. compiler exhaustiveness가 4 match 사이트 강제(elaborate `ir_bits_of`·engine `eval_sysfunc`/`width`/`builtins` dispatch)=전부 inert placeholder(file/dist/cast func eval-path=X like Fopen·task=no-op Continue). **inert 증명**: elaborate가 신규 name→variant 매핑·emission 0(grep)+전체 suite 1303/1303(=bump 前 동일 count)+clippy/fmt clean. 형상-only(v5/v7/v8 선례) |
| ~~5~~ | ~~SYS-READ~~ | ~~`$fgets`/`$fscanf`/`$sscanf`/`$fread`/`$feof`/`$fgetc`/`$ungetc`/`$writememb`/`$writememh`~~ | ✅ **완료(2026-06-19, 1347 green)** | iverilog | 설계 워크플로(understand×4∥probe×4→synth)가 2-PR 분할 권고. **🏁 6 슬라이스 전부 완료, 각 적대 리뷰 통과.** ✅ **5a writemem**(`34c4623`, readmem 미러: `// 0x00000000` 헤더·hex/bin·X/Z 니블 압축·range/descending·OOB 비종료) ✅ **5b 읽기 프리미티브**(`df8a349`, `$fgetc`/`$feof`/`$ungetc`: `$fopen` StmtEffect intercept 패밀리+신규 `read_state`(lazy EOF·`$ungetc` **LIFO 스택**)+`readable_fds`(write-only "w"/"a"는 read 거부); 적대 3렌즈→HIGH 2 수정: write-only fd readable화·`$ungetc` x/z char). ✅ **5c1 `$fgets`**(`2c9b000`, value_plusargs 템플릿: full-width-N·MSB-right-justify·newline-retain) ✅ **5c2 `$fread`**(`50a1581`, big-endian slot-fill(capacity=ceil(W/8), MSB측부터·partial=저바이트 보존)·memory ascending·start/count window+clamp·element-select=loud; **🏁 PR-A 완성**). 적대 리뷰(fgets/fread 3렌즈)→HIGH 2+MEDIUM 1 수정: `$fread` start/count x/z=0 coerce(absent 아님)·`$fgets` embedded-NUL 절단(line은 소비)·leading-NUL/sub-byte dest=0 clear(unchanged 아님). 설계 probe 오류 1건 적발(`reg[12:0]`←`0xFFFF`=`0x1fff` not `0x0fff`). ✅ **PR-B `$fscanf`/`$sscanf`**(`d2cac27`, scanf 포맷 파서+첫 멀티 ref-write `scan_run`: %d/h/o/b·%c·%s·width·suppress·literal-match·newline-spanning·EMPTY=-1). 적대 리뷰(scanf 3렌즈)→HIGH 4+MEDIUM 2 수정: wide값>64bit(`scan_build_numeric` 128-bit wrap + per-digit 4-state)·4-state x/z digit(x-extension)·부호 %d한정·width-overflow panic·%c=1글자·%0d=no-match·EOF -1/0 peek. defer(문서화): %f/%e/%g reals·mem[i] dest. |
| ~~6~~ | ~~SYS-INTRO stateful~~ | ~~`$dist_uniform`(seeded Annex LCG)·`$cast`(hand-IEEE)·`$monitoron`/`$monitoroff`~~ | ✅ **완료(2026-06-19, 1385 green, `53dde9c`)** | iverilog(+hand-IEEE) | 순수 IR-0(v9 변수 기-존재, name→id+엔진만; sim-ir/fmt_ver 9 무변경). **`$dist_uniform`**=Annex `uniform`/`rtl_dist_uniform`(pure f64, NO libm→3-OS deterministic AND iverilog-byte-identical; 0-seed→259341593; seeded direct-rhs intercept `StmtEffect::SeededDist`/`k_dist_seeded`). **4 transcendental `$dist_normal/exponential/poisson/chi_square`=DEFER**(libm log/sqrt/exp=3-OS 非결정→loud E3009). **`$monitoron/off`**=GLOBAL `Postponed.monitor_disabled`(re-`$monitor` 무영향, 確立 print는 항상 발화·change-reprint만 게이트). **`$cast`**=hand-IEEE §6.24.2(iverilog 13.0 거부=오라클無; integral 항상 성공→1; func=direct-rhs `StmtEffect::Cast`/`k_cast`, task=builtins). `emit_blocking_intercept`가 `$random` direct-rhs tail 공유(IR-0 byte-identical). 적대 3렌즈→**3 HIGH+1 MEDIUM 수정**: H1 `$cast` task-form silent-drop→loud guards·H2+H3 monitor enable per-state+flush-gate→global flag+확립 print 항상·M1 `<32bit seed` silent truncate→loud E3009. det/golden/VM/staged CLEAN. defer(문서화): `$dist_uniform` non-direct-rhs loud(=$random 선례). 22 테스트 |

**defer**: `$exit`(오라클 無, Finish 재사용 가능·저가치)·`$system`(3-OS-hostile shell-out=byte-identical 계약 위반 → loud-reject/out-of-scope). 리스크: v9 bump=골든 전 regen(1회 일괄)·`$dist_*` LCG 순서 충실 이식 필수·fd 정수값은 tool-internal(behavior pin)·`$ungetc`=0 반환(own-contract)·`$feof` lazy/post-read.

---

## 5. 보강/성능 하드닝 백로그 (2026-06-22 적대 리뷰)

> 11개 서브시스템 전수 적대 재감사(이미-구현 항목 한정, silent-wrong/미구현 기능 제외). 축=**perf**(복잡도·핫패스 alloc·스케일링·디스패치·I/O) / **robustness**(자원 cap·장기-sim 누수·결정성 불변·사이드카 결합·복구·hand-IEEE pin). 원칙 불변: **3-OS byte-identical 최우선**, IR-0 선행, frozen sim-ir 변경은 format-bump 동반 일괄, perf는 byte-identical 계약에 비용 정당화. 32 findings → dedup 후 본 표(중복 dead-perf 재제안 0건). `IR-0`=엔진/elaborate-local · `sidecar`=SimOpts/out-of-band trailer · `format-bump`=루트 해시 flip(해당 없음).
>
> **진행(추천 순서 1→4 착수, 2026-06-22):**
> ✅ **#1 STAGED-DROP 완료** — staged `velab→vrun`에 14번째 append-only trailer(`StagedExtraSidecars` 단일 구조체=field-order 단일 진실)로 13개 누락 사이드카(func_table·task_calls·two_state_nets·class_layouts/vtable/field_inits/new_sites/handle_nets/field_widths/calls·assert_fire/ctl) 직렬화. staged==one-shot 회귀 2종(class·재귀 automatic fn — `!==`로 X-결과도 포착) + golden/fmt_ver 9 무변경(out-of-band). ⭐교훈: `if(x != 42)`는 x=X에서 false-pass → 자가검증 가드는 `!==`.
> ✅ **#2 PP-FANOUT-CAP 완료** — `PreOpts.max_output_bytes`(기본 256 MiB) 누적-출력 budget을 emit 깔때기(`emit_verbatim`/`emit_collapsed`)에서 강제 + `scan_text` early-return(blown 시 2^N fan-out을 O(depth)로 unwind=시간경계). 더블링 매크로(`Mi=`Mi-1 `Mi-1`, 큰 M0)가 ~327MB에서 loud `E1004`로 bail(RSS 287MB·exit1, `has_errors()`가 parse 전 차단). cap은 `u32::MAX` 훨씬 아래라 `out.len() as u32` 세그먼트 오프셋 wrap도 동시 차단. `PpRecursiveMacro` 재사용(MsgCode 무증가). 1616 green·byte-identical(정상 디자인은 cap 미도달). ⭐교훈: `xx`(3B) 단위 fan-out은 preprocessor out(~12MB)이 아닌 **parser concat AST**(4M 노드=2.1GB)를 부풀림 → preprocessor cap과 별개의 **parser-side concat-element DoS**를 인접 발견(아래 PARSE-CONCAT-CAP, separate·decision 필요).
> ✅ **#3 VCD-SCRATCH 완료** — `VcdWriter`에 재사용 `scratch: Vec<u8>` 추가, 값변화당 `String` alloc(벡터는 2개: String+`id.to_string()`)를 제거. 단일 진실 `encode_*_into(&mut Vec<u8>)` byte-writer(REALG-DEDUP 교훈대로 String `encode_value`는 이걸 delegate=포매터 분기 방지), 4개 핫 호출부는 `write_change`(scratch clear→`encode_change_into`→`write_all`)로 통일. `real`은 호출부 선계산(borrow 충돌 회피). **A/B 실측: VCD-write 핫패스 44.2ms→31.3ms ≈1.41x**(dump-heavy wall 77.9→65.0ms 1.20x·share 56.7%→48.1%), 1617 green·**byte-identical**(value_change byte-exact 특성화 테스트). IR-0.
> ✅ **#4 CLASS-HEAP-CAP 완료** — class 힙 GC 부재(insert-only)로 per-cycle `new()`가 무한 누수(1M=161MB)이던 것에 `SimOpts.max_class_objs`(기본 1M) budget 추가. 단일 할당 chokepoint `k_class_alloc`에서 `class_heap.len() > cap` 시 `fatal_class_limit`(신규 `F-RUN-CLASS-LIMIT`/VITA-F4024, graceful `$finish`·exit Fatal·single-shot)로 OOM 대신 loud 중단. delta-limit `F-RUN-NO-CONVERGE` 자원-한도 패턴 미러. MsgCode 55→56(+doc-15 bijection). **회귀 2종**(cap 5서 6번째 alloc=F4024·cap 1000=clean exit0). ⭐교훈: loop-at-time-0 누수는 **delta-limit(F4016)가 먼저** 차단(둘 다 1M)이고, class cap은 **time-spread(per-posedge new) 패턴 전용 backstop**(delta가 매 step 리셋) — 두 가드가 상보적으로 ~1M객체(RSS 82MB)에 바운드. 1619 green·sidecar(SimOpts out-of-band).
> **🏁 추천 순서 1~4 전부 완료.**
>
> **진행(§5.1 #5~6 + hostile-input cap 묶음, 2026-06-22 53탄):**
> ✅ **STMT-DEPTH 완료**(hostile cap) — 파서 문장 재귀 무가드(expr만 cap)로 `begin begin … end end` 중첩이 SIGABRT이던 것을 `stmt_depth`+`MAX_STMT_DEPTH=256`(expr 대칭)→`Stmt::Error`로 차단. cap 경로 토큰 미소비여도 `block_body`의 `pos==before` bump 가드가 진행 보장. ⭐교훈: cap=1024는 firing 전 2MB 테스트스택 overflow(≈3프레임/레벨)→256.
> ✅ **SEQ-DEPTH 완료**(hostile cap) — `expand_sequence`의 `##`/`[*]`/and/or 자체 재귀 무가드(prop-level and/or만 cap)로 35k 중첩이 raw stack overflow이던 것을 `sva_seq_depth`+wrapper, cap=`SVA_SEQ_ALT_CAP`(256)→loud `ElabUnsupported`+`sva_never_alt`로 차단. `##` 파서 루프는 iterative라 parser는 생존, elaborate 재귀만 cap.
> ✅ **ELAB-ERR-CAP 완료**(hostile cap) — elaborate 에러 emission 무cap으로 broken construct가 generate iteration당 진단 flood(수천)이던 것을 `error_count`+`MAX_ELAB_ERRORS=200` soft cap(모든 `Severity::Error`가 경유하는 `error()` 단일 funnel)→초과 시 1회 suppression notice 후 무음(had_error 래치=loud)으로 차단. generate bail-early-out은 GenPhase 비대칭 패닉 위험으로 미채택.
> ✅ **GEN-NET-CAP 완료**(§5.1 #6) — UNROLL_CAP per-loop·중첩 곱셈(1000×1000=1M nets/670MB OOM)을 `add_net`에 `MAX_TOTAL_NETS=1<<17`(≈85MiB) 집계 budget+`net_budget_blown` once-fire→no-op(arena 동결)+loud로 차단. cap=1<<17 선택은 v1 ceiling 충분+테스트 4.6s(1<<20은 31s). RED는 cap 무력화로 검증(디자인이 clean 통과·exit0).
> ✅ **FORK-TIE-CAP 완료**(hostile cap) — `compose_child_tie`의 16/16-bit tie 패킹이 >65535 proc/>65536 arms서 overflow/alias(cap은 주석뿐)로 결정성 break이던 것을 `exec_fork` spawn 지점 런타임 가드(`parent_tie>=0xFFFF`\|\|`arms>0x10000`)→`fatal_run`(RunFatal 재사용·새 MsgCode 無) graceful fatal로 차단. 잘못된 "never overflow" 주석 교정.
> ✅ **WIDE-ARITH-CAP 완료**(§5.1 #5) — multi-word `*`/`/`/`%`/`**`가 replication으로 16M-bit 도달 시 MUL 34s/DIV 163s stall(진단 0)이던 것을 `arith_wide`에서 폭 > `WIDE_ARITH_CAP`(1<<20=MAX_NET_WIDTH) 시 X-poison(div0 degrade 선례)+`simulate` 1회 static scan→`W-RUN-WIDE-ARITH`(W4025) loud 경고로 차단. `+`/`-`는 O(n) 무제한. MsgCode 56→57. self-width scan=도달가능 eval과 일치(더 넓은 context는 net cap 선차단).
> ✅ **PARSE-CONCAT-CAP 완료**(robustness, **사용자 결정=전역 노드 예산**) — `{a,a,…,4M}` flat concat이 노드당 80B `Expr`로 OOM이던 것을 `expr()` `node_count`+`MAX_AST_NODES=1<<21`(2M≈168MiB) 전역 예산→초과 시 `node_budget_blown` 래치+expr comma-loop 5곳(concat·replication·call_args·gen-case·case label) push 중단+1회 loud parse error로 차단. per-list 아닌 전역이라 many-lists 우회도 차단. genvar/port 이름 리스트(`ident()` 경로)는 본 finding 범위 외(별개·비증폭). 회귀 2종(2.2M→bounded·3-elem=clean).
> **🏁 §5.1 #5~6 + hostile-input cap 묶음(STMT/SEQ/FORK-TIE/ELAB-ERR) + PARSE-CONCAT-CAP 전부 완료. 1633 tests green, format_version 9 무변경(전부 IR-0/sidecar·WIDE-ARITH만 MsgCode 56→57), clippy·fmt clean. §5.1 잔여=LOGEQ-WORD부터의 perf 묶음.**
>
> **진행(잔여 perf 묶음 + robustness 5종, 2026-06-23 54탄):** §5.2 perf + §5.3 robustness 잔여를 **순차 TDD·서브시스템 배치**로 처리(병렬은 18-에이전트 read-only 분석으로 앵커 검증·spec 산출에 활용; 구현은 sim-engine 단일 크레이트 ~12종 공유라 충돌+빌드이득0+collision-burn 회피 위해 순차). **16/18 완료**(전부 byte-identical 게이트 통과·format_version 9 무변경·새 MsgCode 0):
> ✅ **LOGEQ-WORD**(eval `log_eq`/`case_eq` word-parallel)·**MW-DIV-HOIST**(`mw_divmod` neg 호이스트+in-place)·**FMT-CACHE**(SimState `fmt_cache` 디코드 메모, render-reparse deferred)·**MCD-RECLAIM**(최소 미사용 비트 스캔 재사용)·**FD-RECLAIM**(saturating+aux 정리)·**FORCE-REEVAL**(scratch 버퍼, 민감도맵 deferred)·**WAITER-POOL**(expr_now/level_fire pool, zero-skip deferred)·**CLS-FIELD-RD**(fw cold-arm 이동)·**CLS-CALL-VEC**(class_calls Vec 인덱스)·**VM-WIDEZERO**(needs_wide deferred-init)·**VM-ARITY-ASSERT**(debug 경계 assert+teeth)·**POW-LANE**(const n≥2 Mul-chain, w·n≤128 가드+오라클 2발산 차단)·**VM-REGPOOL**(RegFile/OffFile pool)·**REALG-DEDUP**(단일 `vcd_writer::fmt_g`+적대 골든표)·**TRAILER-PIN**(StagedExtraSidecars wire 핀)·**GEN-3X-STR part b**(walk_scopes_key scratch String).
> ⏸️ **잔여 2종(deferred)**: **RULEV-MTIME**(decision — 정합 mtime fast-path는 wire 변경 필요·storage-free는 unsound) · **SVA-QUAD**(risk — 등가성 증명·golden 불요 확인했으나 무오라클+얇은 커버리지로 silent-wrong assertion 위험, 테스트 강화 선행 권고). GEN-3X-STR part a(unroll plan cache)도 IR-순서 보존 리스크로 보류. **1650 tests green**(perf/robustness 신규 테스트 +17, clippy·fmt clean).

### 5.1 추천 착수 순서 (leverage = severity × 실-RTL impact / effort)

1. ~~**STAGED-DROP**(high·M·sidecar)~~ ✅ **완료(2026-06-22)** — staged `velab→vrun`가 사이드카 13/25개 누락 → class/재귀-automatic-fn/2-state/assertoff가 1급 CLI 플로에서 silent-wrong(재귀 fn은 exit0)이던 것을, 14번째 append-only trailer(`StagedExtraSidecars`)로 전부 직렬화/디코드. staged==one-shot 회귀(class `c.x=42`·재귀 `fac(5)=120`). `cli/src/lib.rs` write_velab_file+vrun 디코더.
2. ~~**PP-FANOUT-CAP**(high·S·IR-0)~~ ✅ **완료(2026-06-22)** — `~30줄` 더블링-매크로 파일이 ~8GB DoS(active-set는 self-recursion만, depth256은 fan-out 미차단)이던 것을, `PreOpts.max_output_bytes`(256 MiB) 누적 budget을 emit 깔때기에서 강제(`output_budget_ok`)+`scan_text` early-return으로 차단. 큰-M0 더블링이 327MB에서 loud E1004 bail(RSS 287MB). u32 오프셋 wrap도 동봉 차단. `hdl-preprocess/lib.rs` emit_verbatim/collapsed·scan_text.
3. ~~**VCD-SCRATCH**(high·M·IR-0)~~ ✅ **완료(2026-06-22)** — 값변화당 String 2-alloc이 측정 56% VCD 핫패스 지배이던 것을, `VcdWriter.scratch: Vec<u8>` 재사용 + `encode_*_into` byte-writer(단일 진실)+`write_change` 통일로 제거. **A/B 실측 1.41x**(VCD-write 44.2→31.3ms), byte-identical. `vcd-writer/lib.rs`.
4. ~~**CLASS-HEAP-CAP**(medium·M·sidecar)~~ ✅ **완료(2026-06-22)** — class_heap GC 없음(insert-only)·per-cycle `new` 누수(1M=161MB)에 `SimOpts.max_class_objs`(1M) cap+`F-RUN-CLASS-LIMIT`(F4024) graceful $finish 추가(`k_class_alloc` chokepoint). delta-limit 선례 미러. time-spread 패턴 backstop(loop-at-time-0은 delta-limit이 선차단). MsgCode 55→56.
5. ~~**WIDE-ARITH-CAP**(medium·M·IR-0)~~ ✅ **완료(2026-06-22)** — multi-word `*`/`/`/`%`/`**` 폭 > 1<<20 시 X-poison(stall 차단)+`simulate` 1회 `W-RUN-WIDE-ARITH`(W4025) loud 경고. `+`/`-`는 O(n) 무제한. `eval.rs` arith_wide·`lib.rs` simulate.
6. ~~**GEN-NET-CAP**(medium·S·IR-0)~~ ✅ **완료(2026-06-22)** — UNROLL_CAP per-loop, 중첩 곱셈(1000×1000=1M nets). `add_net`에 `MAX_TOTAL_NETS=1<<17` 집계 budget+once-fire 플래그→no-op+loud. `elaborate/lib.rs`.

이후: ~~LOGEQ-WORD → FMT-CACHE → SVA-QUAD → MCD-RECLAIM → REALG-DEDUP → FORCE-REEVAL~~ + 저우선 perf/robustness 잔여 → **2026-06-23 54탄 16/18 완료**(상단 진행 블록 참조). **잔여 2종**: RULEV-MTIME(decision·wire format) · SVA-QUAD(risk·무오라클, 테스트 강화 선행). ~~저우선 robustness cap 묶음(STMT-DEPTH·SEQ-DEPTH·FORK-TIE·ELAB-ERR-CAP)~~ ✅ **4/4 전부 완료(2026-06-22)** — hostile-input 하드닝 일괄.

### 5.2 perf 백로그

| 항목 | 서브시스템 | 축 | sev | 이슈(anchor) | 제안 | effort | IR |
|---|---|---|---|---|---|---|---|
| ~~VCD-SCRATCH~~ ✅2026-06-22 | VCD writer | perf | high | ~~값변화당 String 2-alloc, 측정 56% 핫패스~~ 완료: `scratch: Vec<u8>` 재사용+`encode_*_into` byte-writer+`write_change` | **A/B 1.41x**(44.2→31.3ms)·byte-identical·1617 green | (done) | IR-0 |
| ⏸️SVA-QUAD(deferred·**risk**) | SVA desugar | perf | medium | `##[m:n]`/`[*m:n]`가 O(N²) shift-reg(공유 prefix 無), N(N+1)/2 regs(`elaborate/lib.rs:14808 Delay arm`·`15087 synth_seq_pipeline`) | **분석 완료, 구현 보류(무오라클 리스크)**. ⓐ**등가성 증명**: 공유 window=`OR(delay_m..delay_n)` 후 rhs 적용 == 팬아웃 OR — delay-reg 선형 + AND가 OR에 분배 → `pipeline(OR_d a_d)=OR_d pipeline(a_d)`. ⓑ**golden regen 불요 확인**: SVA 테스트는 assertion 결과(exit/`"assertion"`)만 검증·VCD 바이트 아님, bounded-delay는 sva*.rs에만 등장→golden-VCD 테스트 미포함. ⓒ**구현 스케치**: `SeqHop::DelayRange{min,max}` 추가(19 match site), expand_sequence Delay arm이 팬아웃 대신 단일 DelayRange hop 생성, synth_seq_pipeline hop 루프에 windowed-OR arm(guard_and 매 stage), window_len 갱신. **보류 사유**: iverilog가 concurrent assertion 미지원=**오라클 부재**(POW-LANE식 차분검증 불가), 기존 커버리지 `##[1:2]`뿐(얇음), correctness-critical 서브시스템의 silent-wrong assertion 위험. **선행 필요**: m=0·대범위·throughout guard·중첩 시퀀스 hand-IEEE 테스트 강화 후 착수 권고 | — | M | IR-0 |
| ~~FMT-CACHE~~ ✅2026-06-23(decode) | system tasks | perf | medium | ~~fmt-string을 packed const에서 매 호출 재디코드(bit-unpack+UTF-8)(`builtins.rs:1511,1536`)~~ 완료(decode 캐시): SimState `fmt_cache: RefCell<Vec<Option<Box<str>>>>`(len=consts) 지연 메모, `fmt_const_string(cid)`가 format 경로(expr_const_string·str_const_of_expr) 2곳에서 디코드 1회화. **잔여(별개·deferred): render_template char-reparse pre-segment(part b)** — ROADMAP 명시대로 ROI 낮아 보류 | 특성화 1종(2개 fmt-string 루프 interleave + trailing string-arg = cid cross-contamination teeth)·display_semantics 11 green | M | IR-0 |
| ~~FORCE-REEVAL~~ ✅2026-06-23(alloc) | event scheduler | perf | medium | ~~reeval_active_forces가 매 delta 전체 레지스트리 clone+재평가(`sched.rs:1454`)~~ 완료(alloc 제거): `scratch_force_entries` mem::take/restore로 per-delta Vec alloc 제거(요소 clone은 alias 안전상 유지). **잔여(deferred): net→forces 민감도 맵**(part 2, collect_expr_reads — designs with many forces 출현 시) | 기존 force 스위트(legality_semantics)가 byte-identical 게이트·full gate green | M | sidecar |
| ~~LOGEQ-WORD~~ ✅2026-06-23 | 4-state eval | perf | medium | ~~==/!=/===/!== per-bit get_vu 루프(형제는 전부 word-parallel)(`eval.rs:909,940`)~~ 완료: `log_eq`/`case_eq`를 `nwords(w)` 워드 루프(val/unk plane XOR-OR)로 교체, `resize_keep_sign` canonical plane이라 bit-exact. `casez_eq` idiom 정합 | 특성화 5종(high-word 차이·x-poison·mixed-sign zero-extend, 리팩터 전후 green) | S | IR-0 |
| ~~VM-REGPOOL~~ ✅2026-06-23 | native-eval VM | perf | low | ~~vm_exec가 RegFile+OffFile 매 activation 신규 alloc(self-flagged C9)(`backend.rs:425`)~~ 완료: Scheduler `vm_regs_pool`/`vm_offs_pool`, vm_exec가 버퍼를 param으로 받고 `vm_run_body`가 pop(=owned, self 미alias)→resize→호출→clear/push로 return. write-before-read는 기존 계약, pool은 재진입에도 안전(pop이 distinct buffer) | backend_equiv 21·threads green(VM==interp byte-identical) | M | sidecar |
| ~~VM-WIDEZERO~~ ✅2026-06-23 | native-eval VM | perf | low | ~~narrow-only prog도 256B wide buf zero-init(`native_eval.rs:987`)~~ 완료: NativeProg `needs_wide`(=wmax>0) 플래그, run()이 narrow일 때 `wbuf`를 **deferred-init**(미초기화)하고 wide stack에 빈 slice 전달(no-unsafe·W* opcode 미실행이라 미인덱스) | backend_equiv 21 green(VM==interp byte-identical) | S | sidecar |
| ~~CLS-FIELD-RD~~ ✅2026-06-23 | class/OOP | perf | low | ~~class_field_read가 fw(heap+BTree)를 cold 에러 arm에서만 쓰는데 매 read 계산(`state.rs:1266`)~~ 완료: `fw = class_field_width(...)`(heap borrow+layout lookup)를 happy path에서 제거, stale/short 에러 arm(drop(heap) 후)으로 이동 | 증명적 동일 code-motion·class_oop 29 green | S | IR-0 |
| ~~MW-DIV-HOIST~~ ✅2026-06-23 | 4-state eval | perf | low | ~~restoring 나눗셈이 loop-invariant mw_neg(&bx) 재계산+rem 매비트 재alloc(`eval.rs:1764-1776`)~~ 완료: `neg_bx` 루프 밖 호이스트 + 신규 `mw_add_inplace(&mut rem, &neg_bx)`(mw_add와 bit-identical carry)로 per-bit alloc 제거 | 특성화 3종(multiword divisor 정확/비정확 remainder·short-path, 전후 green) | S | IR-0 |
| ~~POW-LANE~~ ✅2026-06-23 | native-eval VM | perf | low | ~~`**` 한 노드가 전체 트리를 오라클로 bail(실제 bail set은 Pow뿐)(`native_eval.rs:444`)~~ 완료: const 지수 `2..=16`를 native Mul chain으로 lowering. **적대 검증으로 2개 발산 차단**: ①오라클이 `checked_pow().unwrap_or(0)`로 u128 오버플로시 0(mod-2^w 아님)→`w*n≤128` 가드(a^n<2^128 보장)로 quirk 회피 ②`a**1`은 오라클이 `arith(Pow)` X-poison인데 native passthrough라 발산→n≥2만(n≤1·signed·비-const·w*n>128 bail) | unit 2종: assert_matches_oracle(clean+X-laced, 여러 w,n) + bail 경계 5종(n=0/1/17·w*n>128·non-const·signed) | M | IR-0 |
| ~~WAITER-POOL~~ ✅2026-06-23(pool) | event scheduler | perf | low | ~~propagate_changes가 매 delta Vec<bool> 2개 alloc(`sched.rs:1537`)~~ 완료(pool): `scratch_expr_now`/`scratch_level_fire` mem::take/restore로 per-delta `Vec<bool>` 2개 alloc 제거. **잔여(deferred): Expr/Level waiter count zero-skip**(all-Edge 디자인 precompute 생략) | 기존 waiter 스위트(inbody_events/threads)가 byte-identical 게이트·full gate green | S | sidecar |
| ~~GEN-3X-STR~~ ✅2026-06-23(part b) | elaborate generate | perf | low | ~~per-level format! 스코프 walk(`elaborate/lib.rs:4701`)~~ 완료(part b): `walk_scopes_key`가 루프마다 `format!` 신규 alloc하던 것을 **로컬 scratch String 재사용**(clear+rebuild, hit 시 move-out)으로 교체. byte-identical 키. **잔여(deferred·risk): (a) unroll plan 캐시 replay** — generate 3-phase 재unroll 제거는 IR arena append 순서 보존이 critical(byte-identity)이라 low-payoff 컴파일타임 대비 위험과다, FORCE-REEVAL/WAITER-POOL과 동일 판단으로 보류 | full gate 153 green(generate/scope 스위트) | M | IR-0 |
| ~~CLS-CALL-VEC~~ ✅2026-06-23 | class/OOP | perf | low | ~~class_calls BTreeMap<ExprId> 매 Expr::Call lookup(형제는 O(1) Vec)(`state.rs:375,2082`)~~ 완료: SimState.class_calls를 `Vec<Option<(Option<u32>,u32)>>`(ExprId 직접 인덱스)로. SimOpts는 BTreeMap 유지(elaborate 산출), lib.rs 경계에서 변환. **비-class 디자인은 빈 Vec**(get OOB→None=zero-alloc byte-identical) | class_oop 29 green(virtual dispatch)·full gate 152 | M | sidecar |
| DYN-HEAP-VEC | dyn array/queue/assoc | perf | low | dyn_heap BTreeMap O(logK)(ordering 미사용·never iterated)(`state.rs:173`) | Vec<Option<DynObj>> NetId 인덱스. **multi-hot-handle 워크로드 出現 전 착수 금지** | M | IR-0 |
| ⏸️RULEV-MTIME(deferred·**decision**) | CLI worklib | perf | low | RULE-V가 매 vrun 전체 소스 재read+rehash(mtime fast-path 無)(`cli/src/lib.rs:2116`) | **결정 필요**: 정합한 fast-path는 **해시-시점 mtime 저장** 필요→postcard wire 변경(신규 15번째 trailer 또는 format bump). storage-free 단축(source vs velab 파일 mtime)은 **unsound**(해시는 vcmp 시점 기록, velab은 그 후 작성→그 사이 source 편집 시 source_mtime<velab_mtime이어도 stale 누락=silent-wrong). `.mtimecache` 별도 파일도 신규 아티팩트. payoff=low(vrun 시작, sim 외)인데 비용=wire 결정 → **사용자 결정 대기**(A:15번째 trailer append-only / B:format bump / C:drop) | — | M | sidecar |
| QUEUE-MID-ON | dyn array/queue/assoc | perf | low | queue 중간 insert/delete O(n)→정렬삽입 O(n²)(`builtins.rs:270,281`) | **monitor-only — 스펙 내재 비용(iverilog도 O(n)), 코드변경 비권장**, perf_baseline 항목만 옵션 | S | IR-0 |

### 5.3 robustness 백로그

| 항목 | 서브시스템 | 축 | sev | 이슈(anchor) | 제안 | effort | IR |
|---|---|---|---|---|---|---|---|
| ~~STAGED-DROP~~ ✅2026-06-22 | CLI/sidecars | robustness | high | ~~staged velab→vrun가 사이드카 13/25 누락→class/재귀fn/2-state/assertoff silent-wrong(재귀fn exit0)~~ 완료: 14번째 `StagedExtraSidecars` trailer 직렬화/디코드 | staged==one-shot 회귀 2종·1615 green·골든 무변경 | (done) | sidecar |
| ~~PP-FANOUT-CAP~~ ✅2026-06-22 | preprocess | robustness | high | ~~더블링 매크로 ~30줄→~8GB DoS(depth256은 fan-out 미차단)~~ 완료: `max_output_bytes`(256MiB) emit-funnel budget+`scan_text` early-return, E1004 loud, u32 오프셋 wrap 동봉 차단 | 1616 green·byte-identical | (done) | IR-0 |
| ~~**PARSE-CONCAT-CAP**~~ ✅2026-06-22 | parser | robustness | medium | ~~`{xx … 4M}` flat concat이 노드당 AST=2.1GB, expr depth cap(256)은 깊이만~~ 완료(**사용자 결정=전역 노드 예산 1<<21**): `expr()`에 `node_count`+`MAX_AST_NODES=1<<21`(2M·`Expr`=80B≈168MiB) 카운터, 초과 시 `node_budget_blown` 래치+1회 loud error, expr comma-loop 5곳(concat·replication value·call_args·gen-case·case label)이 flag 확인 후 push 중단. per-list 아닌 전역이라 many-lists 우회도 차단(코퍼스 max 26→80,000×) | 회귀 2종(2.2M concat→bounded parse error·3-elem=clean). 잔여(별개): genvar/port **이름** 리스트는 `ident()` 경로(expr 미경유)라 미카운트 — concat/arg와 다른 비증폭 surface, 본 finding 범위 외 | M | IR-0 |
| ~~CLASS-HEAP-CAP~~ ✅2026-06-22 | class/OOP | robustness | medium | ~~class_heap GC 無→per-cycle new 누수(1M=161MB)~~ 완료: `max_class_objs`(1M) cap+`F-RUN-CLASS-LIMIT`(F4024) graceful $finish(`k_class_alloc`) | 회귀 2종·1619 green·MsgCode 55→56 | (done) | sidecar |
| ~~WIDE-ARITH-CAP~~ ✅2026-06-22 | 4-state eval | robustness | medium | ~~multi-word arith per-eval cap 無, 합법 net+replication으로 16M-bit→163s stall~~ 완료: `arith_wide`에서 `*`/`/`/`%`/`**` 폭 > `WIDE_ARITH_CAP`(1<<20=MAX_NET_WIDTH) 시 X-poison(div0 degrade 선례), `+`/`-`는 O(n)이라 무제한. `simulate` 1회 static scan→`W-RUN-WIDE-ARITH`(W4025) loud 경고. MsgCode 56→57(+doc-15) | 회귀 2종(524289×2=1M+bit→X+warn·32b=42 exact·silent). self-width scan=도달가능 eval과 일치(더 넓은 context는 net cap이 선차단) | (done) | IR-0 |
| ~~GEN-NET-CAP~~ ✅2026-06-22 | elaborate generate | robustness | medium | ~~UNROLL_CAP per-loop, 중첩 곱셈→1M nets/670MB~~ 완료: `add_net`에 `MAX_TOTAL_NETS=1<<17`(≈85MiB) 집계 budget+`net_budget_blown` once-fire 플래그→초과 시 no-op(arena 동결)+loud `ElabUnsupported`. MAX_NET_WIDTH idiom(width→count) | 회귀 2종(33×4096=135k→loud·64=clean). cap=1<<17 선택은 v1 ceiling 충분+테스트 4.6s(1<<20은 31s) | (done) | IR-0 |
| ~~MCD-RECLAIM~~ ✅2026-06-23 | system tasks | robustness | medium | ~~MCD 채널비트 $fclose 미회수→30개 후 고갈, 이후 silent no-op(iverilog는 회수)(`sched.rs:2804`)~~ 완료: `next_mcd_bit` 카운터 제거, $fopen None-mode가 `(1..31).find(!mcd_files.contains)`로 **최소 미사용 비트** 할당 → close된 비트 자동 재사용. 미회수 시 lowest-unused==순차라 byte-identical(`mcd=2` 기존 테스트 유지) | 회귀 2: open/close/reopen→비트1 재사용(red m3=8→green m3=2)·fopen_fd_values byte-identical | S | sidecar |
| ~~REALG-DEDUP~~ ✅2026-06-23 | VCD writer | robustness | medium | ~~%g 포매터 2벌(copy-paste-divergent), VCD본은 오라클 無·3값 테스트뿐(`vcd-writer/lib.rs:540`,`builtins.rs:1874`)~~ 완료: 두 구현이 구조적 동일 확인(`!(-4..P).contains`≡`exp<-4\|\|exp>=P`) 후 단일 `pub vcd_writer::fmt_g(x,p)`로 통합 — vcd `fmt_g16`→`fmt_g(x,16)`, builtins `format_g`→`fmt_g(x,prec.unwrap_or(6).max(1))`, builtins strip_trailing_zeros 제거. 적대 double 골든표(p=6/16 지수경계·subnormal·inf/NaN/-0) | float_format_determinism_golden·percent_g_shortest·vcd 12 green=byte-identical | M | IR-0 |
| ~~FORK-TIE-CAP~~ ✅2026-06-22 | event scheduler | robustness | low | ~~compose_child_tie u32 overflow/alias(>65535 proc, cap는 주석뿐)→결정성 break~~ 완료: `exec_fork` spawn 지점에 런타임 가드(`parent_tie>=0xFFFF` \|\| `arms>0x10000`)→`fatal_run`(RunFatal 재사용, 새 MsgCode 無) graceful fatal. 잘못된 "never overflow" 주석 교정 | 회귀 2종(65537 arms→loud·3-arm=clean). spawn-site 런타임 가드가 elaborate-time보다 직접적(두 값 모두 in-hand) | (done) | IR-0 |
| ~~STMT-DEPTH~~ ✅2026-06-22 | parser | robustness | low | ~~문장 중첩 recursion cap 無(expr만)→deep begin/if SIGABRT~~ 완료: `stmt_depth`+`MAX_STMT_DEPTH=256`(expr 대칭)→`Stmt::Error`. cap 경로 토큰 미소비여도 `block_body`의 `pos==before` bump 가드가 진행 보장 | 회귀 2종(20k 중첩=clean error·100=parse). ⭐교훈: cap=1024는 firing 전 2MB 테스트스택 overflow(≈3프레임/레벨)→256 | (done) | IR-0 |
| ~~SEQ-DEPTH~~ ✅2026-06-22 | SVA desugar | robustness | low | ~~expand_sequence depth cap 無→35k 중첩 ##/[*] raw stack overflow~~ 완료: `sva_seq_depth`+wrapper, cap=`SVA_SEQ_ALT_CAP`(256, prop-level and/or과 동일 예산)→loud `ElabUnsupported`+`sva_never_alt`(had_error가 IR 폐기). `##` 파서 루프는 iterative라 parser는 생존, elaborate 재귀만 cap | 회귀 2종(100k 중첩=loud exit1·20=clean)·SVA 175 green | (done) | IR-0 |
| ~~VM-ARITY-ASSERT~~ ✅2026-06-23 | native-eval VM | robustness | low | ~~run() overflow 안전이 별도 arity() 수기 전사에 의존, drift시 cap통과후 panic(`native_eval.rs:355`)~~ 완료: run() 루프에 `#[cfg(debug_assertions)]` push-경계 assert — 각 op의 실제 sp/wsp 이동 vs `arity()` 비교(잘못된 명시 arity AND `_=>(2,1,0,0)` catchall 오라우팅 모두 포착). release는 byte-identical(전부 cfg-out) | teeth 검증(LoadScalar npush 1→2 교란시 backend_equiv 패닉, revert green)·vm_arity 2종(narrow zoo arith/div/shift/reduce/ternary/concat/repl/sel + wide >64) | S | IR-0 |
| ~~ELAB-ERR-CAP~~ ✅2026-06-22 | elaborate recovery | robustness | low | ~~에러 cap/dedup 無→broken construct가 generate iteration당 진단 flood~~ 완료: `error_count`+`MAX_ELAB_ERRORS=200` soft cap을 `error()` 단일 funnel(모든 `Severity::Error`가 경유)에 추가, 초과 시 1회 suppression notice 후 무음(had_error 래치 유지=loud). parser `error_limit=50` 선례 | 회귀 2종(4096 flood→≤210·3=uncapped). bail-early-out은 GenPhase 비대칭 패닉 위험으로 미채택(emission cap만, CPU는 per-loop UNROLL_CAP·DEPTH_CAP·add_net cap이 바운드) | S | IR-0 |
| ~~FD-RECLAIM~~ ✅2026-06-23 | system tasks | robustness | low | ~~fd 단조증가·미회수·aux 맵 미정리, 증가 unguarded(`sched.rs:2790`,`builtins.rs:449`)~~ 완료: `next_fd += 1`→`saturating_add(1)`(wrap 차단), fd-form $fclose가 read_state/readable_fds/bad_fd_warned 정리(누수 방지·bad-fd 래치 리셋). fd 번호는 단조 유지(번호 재사용 안 함=IEEE)라 **관찰가능 변화 없음**=hygiene | 회귀 1: readable fd open/close 8회 루프 정상(characterization, observable diff 없음을 정직 박제) | S | sidecar |
| ~~TRAILER-PIN~~ ✅2026-06-23 | CLI/sidecars | robustness | low | ~~11+ trailer가 schema_hash 밖·골든 pin 無→same-width 편집+bump 누락시 silent-wrong(`cli/lib.rs:1365`)~~ 완료: 손수 유지되는 cli-local `StagedExtraSidecars`(STAGED-DROP 감사가 지목한 fragile trailer)의 postcard wire-shape를 populated 픽스처 blake3로 핀(REGEN_GOLDEN line). 필드 add/remove/reorder/type 변경→hash flip→format_version 결정 강제. **잔여(별개): 타 13 trailer는 plain sim-engine/sim-ir 타입**(SchemaShape cascade 회피, 본 핀은 hand-maintained 1종 집중) | teeth 검증(필드 추가→wire flip, revert→green) | S | sidecar |

### 5.4 감사 음성결과 — 이미 충분히 하드닝된 축/서브시스템 (전수성 기록)

이번 적대 재감사는 다음을 **검토 후 추가 조치 불필요**로 판정(전수성 보증):

- **dirty-list 넷 스케일링(net_to_edge/waiter 자료구조)** — perf_nets_scaling 512→8192 nets flat(R2 이후), 이미 측정-기각. 본 백로그의 WAITER-POOL은 *별개*(in-body forever@() waiter용 Vec<bool> alloc, net-wakeup 맵과 직교).
- **real native-eval lane** — REAL_HEAVY VM 0.90x, synth 핫패스 부재, 측정-기각.
- **has_xz/to_u128 마이크로 최적화** — inline-Value 라운드 후 이미 word-parallel.
- **front-end per-CU 병렬(T2)/parallel elaborate(T3)** — parse ~10ms 비병목·single-CU concat / arena ID 순서=골든 계약. PP-FANOUT-CAP는 T2와 무관한 robustness cap.
- **engine-internal PDES(T4)** — Amdahl ~2.5x 상한, corpus W=1..8서 0~손실. 재진입은 지속 W≥64+grain≥200ns 워크로드 出現 시에만.
- **snapshot_prev / decimal_bits O(n²)** — 이미 삭제 / Horner base-10¹⁹ 수정 완료.
- **StmtEffect side-effect intercept exhaustiveness** — rustc match 망라성이 이미 가드(eval_sysfunc `_ =>` 와일드카드 無); intercept는 disjoint `which` predicate라 순서 무관. **드롭**.
- **[time]-keyed assoc signed-i64 misorder** — 기술적으로 정확하나 dyn_storage.rs:1434 테스트에 **문서화된 의도적 v1 한계**로 박제됨(값/lookup 정상, ≥2^63 키 iteration order만, [time] 도메인서 비현실적). **드롭**.

**충분-하드닝 판정 서브시스템:** dyn array/queue/assoc(perf)는 DYN-HEAP-VEC/QUEUE-MID 둘 다 K=1서 무비용·스펙 내재라 *투기 착수 금지*; native-eval Value 마이크로옵스(has_xz 등)는 완료; 스케줄러 net-wakeup 맵은 R2 flat. 본 백로그 perf 항목은 전부 byte-identical 계약을 만족하거나(IR-0/sidecar) SVA-QUAD처럼 골든 재생성을 명시 disclose함.
