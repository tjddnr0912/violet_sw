# DEVLOG — vitamin (016_claude_rtl) 슬라이스별 개발 이력

> `CLAUDE.md` 상태 블록에서 분리한 **슬라이스별(탄) 누적 개발 로그**(Stage C 바이트코드 VM → Phase-1 감사 → Phase-2 → Phase-3 SVA/검증 → Medium 묶음). 단일 진실 공급원은 `docs/preview/` SPEC이며, 향후 과제는 [ROADMAP.md](ROADMAP.md), 잔여 작업은 [REMAINING_WORK.md](REMAINING_WORK.md)에 있다. 각 항목은 커밋 해시·테스트 그린 수·적대 리뷰 결과를 핀(pin)한다.

## Phase-1 마일스톤 (요약)

**상태:** **전 파이프라인 동작** — one-shot `vita design.sv` + staged `vcmp→velab→vrun` 모두 시뮬레이션해 VCD+stdout 산출. timescale(doc-08 전체 모델)·다차원 unpacked·packed 배열·VCD 계층 naming·casex/casez·fork-join·func/task·**SV 자료형(enum/typedef/packed struct)** 등 Phase-1 합성 RTL 대부분 구현·검증(**1125 테스트 green, iverilog 차분 일치**). 6축 감사 52항목+후속 큐 5항목 전부 클리어. 혼합-timescale postponed `$strobe`/`$monitor` multiplier 버그 수정(`fbb869c`) + 멀티-top 다중 root elaborate 수정(미인스턴스 모듈 전부를 root로, 골든 무영향).

## 슬라이스별 이력 (Stage C ~ 41탄)

**Stage C 컴파일드 백엔드(바이트코드 VM) — C1·C2 완료**(`Backend::Bytecode`가 suspend-free P9 클래스를 VM 실행, P5 차분 게이트가 인터프리터와 byte동일 강제, frozen sim-ir 무변경). **profile-driven 최적화 4R로 누적 ~6x**(eval-heavy 2781→461ms; net I/O·shift·resize word化 + inline-Value, 전부 interp·VM 공유 경로). **🔨 native-eval C4-lite 착수**(`native_eval.rs`): ≤64bit 정수 서브셋(Const·Signal·Add/Sub/Mul·비트연산·Not/Plus/Minus)을 VM 전용 native 레지스터로 평가, 그 외는 `eval_ctx` fallback — **식-바운드 expr-heavy VM 0.92x→0.42x(≈2.3x)**, 클럭-바운드 불변, 인터프리터=오라클·P5 차분게이트 byte동일 강제, frozen sim-ir 무변경. **향후 과제·전략 = [`docs/ROADMAP.md`](docs/ROADMAP.md), 잔여 트래커 = [`docs/REMAINING_WORK.md`](docs/REMAINING_WORK.md)(2026-06-10 7축 감사로 전면 리뉴얼 — P0 silent-wrong 정확성 9 · P1 의미론 9 · P2 운용 12 · P3 메모리 5 · **P4 병렬화 트랙 신설**(`--threads` 설계, byte-identical 계약) · P5 문서부채; Gemini shift fix 검토·채택 포함), perf 이력 = doc-18 §실측.** SPEC `docs/preview/`가 여전히 단일 진실 공급원. **2026-06-10 감사 트래커 전 항목 소진(571 green): P0~P5 전체**(severity `$fatal`=exit1·radix b/o/h·`%m`·in-body @(*)·finish-flush·per-bit 멀티드라이버·E3018/W3056/E-DUP-UNIT·`--help/--version/--threads/--timeout`·VCD/델타 진단·원자적 아티팩트·parser/array 캡·fork free-list 등) **+ P4 T1 `--threads` VCD writer 스레드(byte-identical 게이트) + native-eval 확장(비교/시프트/DivMod/ternary/리덕션/논리 — expr-heavy VM 0.42x) + P2-12 정책(`time` 타입 64-bit unsigned 수용·`` `pragma`` 수용-무시·timescale clamp·implicit-net=E3010 명문화) + P5 문서 동기화(-W* 미래형·예약 codes·exit 0/1/3 실태)**. 사이드테이블 7종은 `Sidecars` 번들로 통합(.velab trailer 세그먼트 append-only). 

**같은 날 후속 2탄: perf 축 + Phase-1.x 전체 소진(HEAD `8664627`, 611 green)** — 스케줄러축 라운드1(클럭-바운드 ≈1.85x, 핫루프 할당 9원 제거) · native-eval 구조 lane(select/concat/replicate, STRUCT_HEAVY VM 0.36x) · vita-log `-Wno-*`/`-Werror=` 게이트+exit class 2 · filelist `-f`/`-F` · `vita explain` · **format_version 4**(런타임 `#delay`=ExprId 평가, `$dumpflush`/`$dumplimit`, REGEN_GOLDEN 스위치) · force/release(sample-once, per-net forced 플래그). vita-log는 이제 실코드(stub 1개=hdl-builtins만). 

**3탄(2026-06-10, HEAD `e6e4e8e`, 647 green): 갱신 후보 ③~⑧ 전부 소진** — ③native-eval C6 lane(array-indexed `LoadIndexed` + 65..=128bit 별도 u128-pair wide 스택, WIDE_HEAVY 0.59x·MEM_HEAVY 0.72x, narrow 무변경) · ④vita-log 2단계(`--log` 단일-writer tee·`-q/-v/--verbosity`·counts epilogue `errors= warnings= notes=`) · ⑤blocking intra-assignment delay 실semantics(capture-now/write-later; NBA `<= #d`=loud E3009→v5 bump 묶음) + **expression force 연속 재평가**(IEEE §9.3.2, `active_forces` 레지스트리 — iverilog는 "evaluated once" 자인이라 의도적 발산, const-rhs 차분 유지) · ⑥Phase-2 관문 평가(ROADMAP §F: v5 bump 일괄=NBA-delay·named-event·dynamic-storage, IR-무변경=immediate-assert·interface 스파이크 선행) · ⑦3-OS CI(`.github/workflows/vitamin-ci.yml` — ubuntu(iverilog 오라클)/macos-14/windows, fmt/clippy/test --locked) · ⑧net_to_edge 후보는 `perf_nets_scaling` 프로브(512→8192 평탄)로 **측정 폐기**. 다음 후보 = §F 진입 시퀀스(immediate assert→interface 스파이크→dynamic-storage 설계→v5 bump) · ⑨P4-T2. *(filelist 버킷·스케줄러 R2는 2탄 직후 완료 — `0759b3d`/`b5161c1`.)* 

**4탄(2026-06-10/11, 674 green): §F 진입 시퀀스 완주 + format_version 5** — (E) immediate assert(파서 `If` desugar, AST 동결 유지, 디폴트 실패=`$error` 합성→stderr+exit1, iverilog 차분) · (D) interface 스파이크(GO — 신호=net+심볼 aliasing, SimIr 무변경 확정, 구현은 .vu flip 일괄 대기) · (F) disable 실동작(동봉 named block의 break/continue=doc-17 "Disable 후 Goto", **lazy exit-BB pre-scan으로 기존 디자인 CFG byte-불변**, 비동봉/fork-경계=loud)+proc-assign/deassign(force 기계 weak-rank 재사용 — `assign_ranks` 사이드카·trailer 세그먼트, force가 assign을 latent로 밀고 release가 복귀; iverilog const-rhs 차분, 식-RHS는 "evaluated once" 자인이라 hand-IEEE 핀) · (C) **dynamic-storage 설계 문서**(handle-net+엔진 힙, v5 형상 diff 전량 확정) · **v5 bump**(NonblockingAssign.delay+NetKind Dyn 3종+SysFunc/SysTask 메서드 10종, REGEN_GOLDEN 재핀, 기능 0) · (A) **NBA transport delay**(`delayed_nba` wheel+전역 seq 정렬, 겹침 활성화 각자 캡처 운반, 차분 4레인) · (B) **named-event 카운터 desugar**(sim-ir 0 — `event e`=64-bit Reg init0, `->e`=`e=e+1`, `@(e)`=AnyEdge; 같은-슬롯 이중 trigger 보존, 값-표면 전부 loud, `.vu` AST 재핀, 차분 3레인). ⭐교훈: **동시-틱 tie(Active $finish vs due-update/edge-wake)는 도구-발산 영역** — vvp는 슬롯-톱 적용/즉시 wake, LRM은 region 순서(우리) — 차분 디자인은 tie 회피+주석 핀. 잔여 = **(C) 엔진 증분 ⑥front-end(.vu flip=dyn 문법+interface)**. 

**5탄(2026-06-11, 691 green): (C) 엔진 증분 ③dyn array(3a heap/new/size/delete + 3b 인덱스 r/w `dyn_is_handle` 깔때기 라우팅) · ④queue 완료** — `DynObj::Queue{VecDeque}`, push=SysTask 디스패치(원소형 §5.5 cast·cap warn+drop), `q[size()]`=push_back 동등 append(IEEE §7.10.1 — iverilog 라이브가 설계문서 "무시" 가정을 정정), **pop=`StmtEffect::QPop` 문장-레벨 인터셉트**(side-effect는 WRITE phase, P7a read-phase 순수성 유지; Kernel `k_queue_pop_rhs`/`k_queue_pop` 2메서드) + pop-rhs 바디 `is_codegen_able` 제외(VM=interp fallback, teeth=VM byte-parity 테스트로 입증), 비지원 배치 pop(NBA rhs·중첩 식)=eval arm X+W4020 미-pop, pop SelfWidth=원소형(signed −1/unsigned 255 확장 차분), `q[$]`=DynSize−1 desugar 시임, empty pop=X+warn-once. **이어 ⑤assoc 완료(701 green)** — `DynObj::Assoc{BTreeMap<i64,Value>}`; **키=signed i64 전역**(음수·>u32 합법 → u32 offsets 쌍 불가)이라 `Offsets::AssocKey(Option<i64>)` variant+`k_write_lvalue` ABI slice→`&Offsets`(NBA·CA·VM·QPop 깔때기 공유=by-construction), READ는 Signal arm이 u32 coercion 전 `is_assoc` 분기→64-bit 키 평가(X/Z·real=invalid), exists/num=순수 eval arm(VM parity 자동)·delete(k)=SysTask(미존재 키=무음 §7.9)·delete()=DynDelete 공용, native-eval LoadIndexed는 Assoc bail(u32 도메인 발산 차단). ⚠️ **iverilog 13.0은 assoc 선언 자체 거부 → 유일한 hand-IEEE 핀 레인**(§7.8/§7.9, expression-force 선례). 전부 hand-built SimIr 시임 테스트(문법은 ⑥), frozen sim-ir 0줄. 

**6탄(2026-06-11, 722 green): ⑥front-end 일괄 완료 — §F 전 항목 소진.** (C) dyn 문법(lexer `$` 토큰·`Dim::{Dyn,Queue,Assoc}`·`ExprKind::{New,Dollar}`(new=컨텍스추얼+net-named-new 폴백)·메서드=기존 `Call{HierPath 2seg}` AST 그대로·elaborate 인터셉트 7곳: 핸들 decl(array_len 0)/BitSelect r/w(정적 체인보다 선행)/`q[$]`=dollar_subst 치환/`d=new[n]`·`x=q.pop_*()` Blocking 특수형/메서드 expr·stmt 디스패치/오용 전부 E3009; dyn·queue=iverilog 차분 e2e 13, `q[$-1]`은 iverilog가 문법 거부→hand-IEEE) + (D) interface(스파이크 그대로 — `TopItem::Interface(ModuleDecl 재사용)`·`Modport`·`AnsiPort.iface`, **인스턴스는 nets 단계 4c 조기 평탄화**(부모 body `i.sig` 가시성, 멱등 가드), 포트 바인딩=심볼 aliasing(BTreeMap prefix-range, net·cont-assign 0), resolve_net 다중-세그 dot-join, **iverilog는 interface port도 거부→hand-IEEE 핀**, e2e 8 — alias=동일 net 증명은 엣지 센스티비티 관통 테스트). `.vu` 해시 재핀 1회(dyn+iface 일괄), SimIr 무변경(fmt_ver 5). 

**7탄(2026-06-11, 728 green, 전부 푸시됨): 권장 순서 전체 소진** — ⑨P4-T2 측정 폐기(parse 12k라인≈10ms 비병목+단일-CU concat 의미론; T0a/T0b는 기완료 확인, dump-heavy 실측 0.78s·writer 스레드 −3%) · 운영 인프라 3종(`--dump-filelist` 4-applet 공통/filelist canonical dedup(E8003 silent-arm, sticky 도입 전 CONFLICT arm 예약)/RULE-V composite 기록(vcmp=원문+(-D/-I), velab=소비 .vu blake3 — 소비 게이트는 worklib 시)) · §A word化 완결(`copy_bits` funnel — select/concat/replicate/merge_x, STRUCT_HEAVY interp 551→382ms ≈1.44x, VM 불변) · **foreach desugar**(dyn/queue — 합성 인덱스+body rename, AST/IR 0, iverilog 차분; 인덱스 지역성은 v1 블록-로컬 평탄화 한계를 rename으로 우회) · **적대 리뷰 적발 2건 수정**(rename 워커: 블록-decl init/EventCtrl ctrl 미개명 = silent outer-capture — teeth 입증 회귀 포함). 

**8탄(2026-06-11, 765 green, 적대 리뷰 9불변식 PASS): 구현 트랙 ①~⑤ 전부 소진 — format_version 6.** ①**v6 bump**: queue `insert(i,v)`/`delete(i)`(iverilog 차분) + assoc `first/next/last/prev`(`StmtEffect::AssocIter` 문장-인터셉트, hand-IEEE §7.9.4, 좁은 ref var=truncate+−1+W4020, dyn/queue는 dense walk=foreach 내부 타깃) + **foreach=uniform first/next desugar 재작업** + string 키(`NetKind::AssocStr`·`Offsets::AssocStrKey`·사전식 순서; AST `AssocKey::Str` 재핀) — ⭐부수 적발: **string 리터럴 LSB-first 패킹=IEEE §5.9 위반 라이브 버그**("ab"=25185 vs iverilog 24930), 패킹+`const_string` 대칭 플립·회귀 핀. ②**iface 후속**: modport 방향 강제(listed-only 가시성+input=read-only, `walk_scopes_key` 단일-진실 리팩터)·`#(parameter)`(부모-스코프 해소)·ANSI 헤더 포트(LATE pass wire_ports 재사용)·generate-내 iface 바인딩(외향 워크). ③**bounded queue `[$:N]`**: `QueueBoundTable` 사이드카(8번째 trailer)+tail-truncate 단일 규칙(iverilog 3-표면 전부 재현)+staged 왕복 e2e. ④**§A 완결**: wide 구조 트리오(WSelect/WConcatPair/WRepl, WIDE_STRUCT_HEAVY VM 0.44x≈2.3x)+real lane 측정-폐기(0.90x, 합성 핫패스 부재)+has_xz/to_u128 검사-폐기(이미 word-parallel). ⑤**예약 진단 arm**: E8003 CONFLICT(sticky ctx=상속 `` `timescale``, dup시에만 워크)+E9003 `vrun --upstream`(라이브 재해시 vs composite, exit 2). 

**9탄(2026-06-11): PDES 연구 트랙 종결(조건부 NO-GO) — Phase-1 작업 큐 완전 소진.** 프로브 3종(`perf_pdes_sync_cost`/`engine_grain`/`bsp_mock`, 영구 계기) 실측: τ 상주 spin-pool 0.3~0.5µs/delta(naive spawn 31~93µs=즉사)·g≈700ns/activation·BSP mock 디스패치 측 W≥64에서 3~4x — 그러나 sample 분류상 직렬 잔류(apply_nba/propagate) ~20% → **Amdahl 상한 T4 ≈2.5x**, corpus 워크로드(W=1~8)는 0~손해. ⭐**정정: byte-identical은 차단 요인 아님** — NBA-pure 클래스(P9 재사용) 병렬+run-splitting+per-process 로그 (batch_idx,seq) 머지로 by-construction 보존 가능(설계 스케치 doc-18 §PDES). 재진입 조건 핀: 지속 W≥64+grain≥200ns 실워크로드 → BSP v1. 이후는 작업 큐가 아니라 스코프 결정 — **퓨처 플랜 = ROADMAP §4**(확장 트랙 P2-A~F+조건부/장기 + MVP 컷 인벤토리→해제 매핑; 권장 순서 ①worklib ②v7 bump 묶음(system tasks+string+package+정밀화) ③절차 고급 ④SVA). 

**10탄(2026-06-12, 788 green): Phase-2 진입 — ①P2-A worklib v1 완료**(`vcmp --work`=lib.toml 정규형+content-addressed CU blob / `velab -L/--top`=클로저 로딩+**unit_map 승자 기반 머지**(passenger 그림자 결함 자가 적발·수정) / **vrun RULE-V 자동 게이트**=매니페스트·blob·소스/include raw 재해시→E9003 exit 2 / E9005 신설(52 codes) / 9번째 trailer WorkConsumed=legacy bytes 불변; 적대 리뷰 9불변식 PASS, 디퍼=`-y`/`-v`/`-P`/`--lib-map`·per-unit blob) **+ ②v7 관문 평가**(casez/casex=BinOp 신설 필요→bump 합류·random=IEEE Annex 핀·math transcendentals=libm 결정성 리스크로 디퍼) **+ ③정밀화 IR-0 ①instance array 언롤**(iverilog 핀: 선언순 첫 인덱스=MSB 청크, W=P 공유/N·P 슬라이스, child-scope 파라미터 폭 해소; ⭐`[N-1:0]`(N=0)=합법 `[-1:0]` 2-원소 발견). 잔여 = 정밀화 ②~⑥(슬라이스/bounds/inertial/dump-array/multi-word) → v7 bump 일괄. 

**11탄(2026-06-12, 851 green, 5커밋 push): 정밀화 IR-0 ②~⑥ 전량 소진 — 소묶음 완결.** ②**unpacked 배열 대입**(IEEE §7.6 위치 대응(선언순 좌→좌, `array_dim_desc` 방향맵) element-wise desugar, NBA 스왑/transport `#d` 포함; **iverilog가 기능 자체 미지원→hand-IEEE 핀**; 대입 외 whole-array=silent word-0→loud 승격(`$dump*` 인자만 보존); 적대 리뷰 CONFIRMED 5건 전부 수정 — modport-input 우회·out-formal 별칭 word0·`new`-명명 guard 구멍·signedness·**`[0:0]` 배열 전면 면제**(chain 게이트를 선언 기반 `net_is_static_array`로 — 기존 element≡bit-select 오라우팅도 동시 수정)) ③**per-dim bounds**(d≥2 인덱스마다 `lo≤idx≤hi` 가드 IR 합성+덧셈-안정 sentinel `0x8000_0000`, 1-D byte-불변; **⚠️ iverilog도 inner-dim alias(silent-wrong 동행)→hand-IEEE §7.4.6 핀**; packed bit-space도 동일 깔때기로 수정) ④**`assign #d` inertial**(per-CA 세대 카운터로 pending 무효화 — 좁은 펄스 필터·정확-경계 펄스 생존, iverilog 차분 5핀; 엔진-로컬 0 IR) ⑤**`$dump*` per-element+depth/scope**(원소당 `$var`=`mem[4]`/`g[1][2]`, dims=`NetDimsTable` 10번째 trailer 사이드카(9번째 WorkConsumed는 항상-기록으로 전환—옵션 뒤 세그먼트 모호성 제거); scope 인자=elaborate `fq\x01raw` 2-후보 string const 인코딩, level=iverilog 핀(1=자기 스코프만), 배열 인자=iverilog가 에러라 hand-확장, 추가 호출=W4021 1회(53 codes), 미해소 scope=전체 폴백) ⑥**multi-word 산술**(`arith_wide`+mw 커널: ripple add/sub·school mul·short/restoring divmod(부호=IEEE: 몫 절삭·나머지=피제수)·square-multiply pow(음수 지수 IEEE 표)+`%d` 임의 폭 십진(`mw_decimal` 10^19 청크 — 이전: signed>64=unsigned 표시·>128 silent 절단) — **wide는 iverilog 완전 차분**). VM parity 3핀(array-assign/bounds/wide=커널 공유 by-construction), frozen sim-ir 0줄·fmt_ver 6 유지. ⭐교훈: 적대 리뷰가 "이 슬라이스의 새 표면"만이 아니라 **인접 기존 결함**([0:0] 오라우팅, string 패킹 선례처럼)을 끌어냄 — 슬라이스마다 리뷰 가치 재확인. 

**12탄(2026-06-12, 903 green, 10커밋 push): v7 bump 일괄 전량 소진 — format_version 7.** ①형상 bump(기능 0, BinOp 2+SysFuncId 18+SysTaskId 7+NetKind::String, Process 서브핀 불변) ②casez/casex 정밀(`CasezEq/CasexEq` — casez x=strict 4-state exact, redor 공식 폐기; W3011 예약 전환) ③bit-vector(`$countones/$onehot(0)/$isunknown` word-parallel known-결과 + **`$bits`=elaborate const-fold 3-레인**(array view/ir_bits_of/(3b) decl-order prescan — param 바인딩이 net보다 선행이라 프리스캔 필수)) ④**`$random`=IEEE Annex N 핀**(69069 LCG+float-mantissa; ⭐어구대로 (b−a)스케일+trunc은 양·음 모두 ±1 발산 — dist_uniform end+1 접힘=2^32 스케일+floor-캐스트가 4-draw 라이브 일치)·`$urandom(_range)`=splitmix64 자체 계약·`$stime`; **시드-부작용 = StmtEffect 인터셉트 패밀리 확립**(seeded $random/$value$plusargs/$fopen/$sformatf — direct-rhs만, 그 외 E3009, VM 제외) ⑤plusargs(CLI `+arg`→SimOpts out-of-band·prefix probe·first-match·%s 비-const 패킹 렌더 결함 동시 수정) ⑥파일 I/O(fd 0x8000_0003…·MCD bit1…(bit0=stdout 브로드캐스트)·closed-fd=W4022 warn-once) ⑦`$readmemb/h`(선언-인덱스 도메인 @addr(hex)·1364-2005 lowest-ascending·디렉티브-유무 shortfall 규칙·W4023; **55 codes**) ⑧**AST flip 1회**(package/import/string/`::`/PkgScoped — `[string]` 키가 키워드化 회귀 즉수정, .vu 재핀) ⑨**P2-D package**(run()-진입 일괄 fold($pkg$ 합성 스코프)→import가 const=(3b)전/func·task=(3.5)후 skip-if-present — **로컬 승리** iverilog 핀; `pkg::sym`=expr·const 양 컨텍스트 fold) ⑩**P2-C string**(힙 핸들+`Value.is_str` resize 바이패스(is_real 선례)+§6.16 leading-NUL strip — **단일청크 String dest는 total-width resize 우회**(net width 0→1bit 절단 실결함); 비교 전부 StrCmp 라우팅(signed 0); $sformatf/$sformat=커널측 렌더; **iverilog가 toupper/getc/putc/compare 거부+string NBA는 vvp 내부에러→hand-IEEE**; ⭐인접 결함: VCD declare skip에 AssocStr 누락(v6)을 String과 동시 수정). math transcendentals만 잔존 디퍼. 

**13탄(2026-06-12, 914 green): v7 적대 리뷰 3건 수정 + P2-E 완료 + P2-F 관문 — §4.3 권장 순서 전 항목 소진.** ①리뷰 4건 전부 라이브 재현으로 닫음(2건 반박 — case(string)·==는 strip 불변식으로 이미 정확): F1 `$bits(string)`=silent 1(ir_bits_of String 가드)·F2' string 형식인자 경유 관계연산=packed 비교(subst 관통 `ir_expr_is_string`)·F3 `{s,x}=v`가 string을 silent 클리어(concat-lvalue loud). ②P2-E: ⭐관문 적발 — join_any/none **이미 전체 동작**(차분 핀만)·disable fork=자손 transitive kill(activity dead+run_body 단일 초크, IR-0)·do-while=파스 desugar·unique/priority=no-match 위반을 `$warning` arm desugar(priority-if=iverilog 문법 거부→hand-IEEE)·final=ProcKind::Final(.vu 재핀)+final_procs 사이드카(11번째 trailer)+종료 시퀀스. 디퍼 박제: wait fork=WaitCause flip→v8·automatic/recursive=콜스택 모델. ③P2-F 관문=조건부 GO(단일클럭 |->/|=> + $rose류 = **전부 desugar-가능 IR-0** 노선 확정, 실구현은 Phase-3 결정). 다음 = Phase-3 스코프 결정 또는 v8 bump 묶음(wait fork+SVA 서브셋). 

**14탄(2026-06-14, 933 green, 5커밋 push): ②v8 bump 수행 + ①Phase-3 진입(wait fork + SVA 서브셋) — format_version 8.** ①**v8 bump**(`69888e9`, 형상-only inert): `WaitCause::Fork` unit(Terminator::Wait가 resume 보유→payload 불요, Process 서브핀 Suspend→WaitCause 경유 flip 예상) + AST flip(`Stmt::WaitFork`/`Stmt::ConcurrentAssert`/`ImplicationKind` 일괄 .vu 재핀) — 모든 WaitCause match에 이미 `_` 암 존재라 컴파일 강제 0, elaborate emit 0=진정 inert. ②**wait fork**(`bb0c5ba`, IEEE §9.6.1): `Activity.wait_fork` 누적 직계자식 배리어(여러 join_none·join_any 잔여 전부 스캔, on_child_complete decrement), **disable-fork 훅 불요**(parked 부모는 disable fork 호출 불가 + 스캔이 dead 제외=disable-then-wait 자동, deadlock 불가 증명) · iverilog 차분 5케이스. ⭐**적대 리뷰 적발 일반 엔진 버그 동봉 수정**: edge-sensitive `always @(posedge clk)`가 mid-body suspend(#delay/wait/@/wait fork) 중 **영구 net_to_edge 엔트리로 entry부터 spurious 재발화**(silent-wrong+비종료, plain #delay로도 재현=pre-existing) → `Activity.busy` 가드(Step::Suspended set/Done clear, 정적 wake만 억제·in-body waiter 보존), in-body #delay clocked-always가 iverilog와 byte-일치(t=97 re-arm 포함). 동시-틱 resume-vs-edge tie는 도구-발산 핀. ③**SVA 서브셋**(Phase-3 진입, S1 `cf8a6f1`/S2 `b09e7b7`/S3 `34354ac`): **⭐iverilog 13.0이 concurrent assertion AND $past/$rose/$fell/$stable 둘 다 거부**("not supported"/"not defined")→**전면 hand-IEEE 핀**(assoc/iface/string-method 선례, reader의 "iverilog가 SVA 지원" 주장은 라이브서 거짓). S1=파서(`property` kw·`|->`/`|=>` 토큰·`parse_concurrent_assert`+무-implication=`1'b1 |-> e` desugar). S2=ConcurrentAssert를 **합성 clocked always 체커**로 desugar(`pending_sva` 수집→프로세스 루프 후 `materialize_sva_checkers`, named-event/foreach 선례; `|->`=`if(ante&&!cons)$error`, `|=>`=1-bit X-init pend reg 1-클럭 지연; $error=immediate-assert severity 재사용). S3=`rewrite_sampled`로 sampled fn→prev-reg(signal당 공유·full-width·NBA `prev<=sig`; $past=prev·$stable=`prev===x`·$rose=`~prev[0]&x[0]`·$fell=`prev[0]&~x[0]`). **적대 리뷰 3렌즈(각 라이브 검증)**: F1(HIGH 수정)=`|=>` 다중비트 antecedent가 1-bit pend로 절단→silent pass, reduction-OR 샘플로 수정 · F3(HIGH 수정)=hierarchical `$past(u.x)`가 마지막 세그먼트 키로 aliasing→multi-seg 거부 E3009 · F2(lenient-X 문서화)=X/Z=don't-fire(엄격 IEEE는 X=fail이나 서브셋에 disable iff 부재→$past 첫클럭 spurious 회피) · determinism 렌즈=CLEAN(순수 IR-0·골든 무손상). 잔여 디퍼: automatic/recursive 콜스택·math transcendentals·full sequence(##n/repetition/throughout)=오토마타 트랙·SVA action block(else $fatal)·module-level assert property 문법. 

**15탄(2026-06-15, 945 green, `c4efbd5` push): full SVA 시퀀스 슬라이스 S4 — 상수 `##n`+`[*n]` antecedent, format_version 8 유지(IR-0, `.vu` AST-hash만 재핀).** 관문 평가(워크플로우 6-agent: 코드 이해 4 + iverilog 라이브 프로브 + 설계 합성) → **GO/IR-0 판정**(`##n`·`[*n]`은 `|=>` pend-reg를 N-스테이지 시프트레지스터로 일반화 가능, frozen sim-ir 무변경; **iverilog는 bare `|->`조차 컴파일 거부=오라클 0 재확인**). 구현: AST `Sequence{Boolean/Delay/Repeat}`+`ConcurrentAssert.antecedent` Expr→Sequence(AST flip) · lexer `##`=HashHash·`[*`=LBracketStar · parser `parse_sequence` 서브문법(##=left-assoc 느슨·[*]=postfix 타이트, deferred 전부 loud) · elaborate `flatten_sequence`(Sequence→(term,hop) 리스트, `[*n]`=##1 체인 전개)+`synth_seq_pipeline`(cur=|t0 매클럭 재시드=오버랩 내재, 각 후속 term=cur를 hop개 1-bit `fresh_sva_reg`로 지연 후 `|term` AND, 최종 cur=match-this-clock → 기존 |->/|=> 체크에 주입). **단일-term=flat path byte-identical**(기존 11 SVA 테스트 무변경, VCD 차분 0 검증). **적대 리뷰 4렌즈(각 라이브 재현)**: 의미론 CLEAN(off-by-one 판별자 IEEE-정확)·결정성/골든 CLEAN·오버랩-머지 CLEAN(단일비트 머지가 boolean 선형 서브셋엔 정확, 위반 카운트 정확)·파서/렉서 **MEDIUM 1건 수정**=내가 추가한 `[*` 토큰이 기존 `[*]` wildcard-assoc 철자를 삼켜 정밀 진단 degrade(여전히 loud)→`parse_dim` LBracketStar 처리+`at_dim_start` 게이트로 복구+masking 테스트를 양 철자 "wildcard" 메시지 단언으로 강화. ⭐교훈: 시퀀스용 신규 토큰이 인접 기존 표면(wildcard dim)과 충돌 — 슬라이스마다 적대 리뷰 가치 재확인. 잔여(Phase-3 후속) = 범위 `##[m:n]`/`[*m:n]`·unbounded `[*0:$]`(오토마타)·`[->n]`/`[=n]`·`throughout`/`within`·sequence consequent·multi-clock·`disable iff`·module-level 문법. **사용자 스코프 선택=오토마타 트랙까지 → 多슬라이스 진행 중**: **S5(`d1c8120`, 950 green): 유계 범위 `##[m:n]`/`[*m:n]`** — `expand_sequence`가 disjunction(병렬 파이프라인 OR-reduce)로 fan-out, AST/IR 무변경(min/max 재사용), flat byte-identical, `SVA_SEQ_ALT_CAP=256` blowup 가드. 적대 리뷰 3렌즈 CLEAN(terminal repeat 상한=run≥m IEEE-정확·mid-sequence 윈도 독립 obligation). **S6(`85f7f10`, 955 green): unbounded delay `##[m:$]`** — fan-out 불가→`SeqHop::{Fixed,AtLeast}` + **never-reset armed latch**(`armed<=armed|cur`, prefix 매치 후 모든 후속 term 클럭이 재완료, X-init=첫 매치 전 don't-know·`X|1=1` 복구), `seq_delay_reg` 헬퍼로 Fixed 경로 byte-identical 유지. 적대 리뷰 3렌즈 CLEAN(16/16 latch 의미론·X-propagation 비-poisonable·`|=>` 연동). **S7(`c609b86`, 958 green): `throughout`** — `Sequence::Throughout` variant(AST flip, sim-ir 골든 무변경 fmt_ver 8), guard=`|cond`를 seed+모든 shift stage에 AND(`SeqAlt=(terms,Option<guard>)`), bounded inner만(unbounded→loud), flat byte-identical, contextual keyword(`wire throughout;` 안전). 적대 2렌즈 CLEAN. **S8(`eb1499f`, 964 green): `[->n]` goto + `[=n]` nonconsec** — **existence-latch FSM**(goto: `reg_0..reg_{n-1}` 단계별 + b-gated advance, match=`b&avail_{n-1}`; nonconsec: goto + `ext` latch `cur=match_g|(ext&~b)`·`ext<=cur`). ⭐**정정 확정**: `|->`=any-completion만 필요→exact-count 불요→순수 IR-0(이전 "single-bit 머지 불가→infeasible"는 오판). 렉서 `[->`/`[=` 토큰·AST `RepeatKind` flip(sim-ir 골든 무변경 fmt_ver 8)·single count·boolean operand만(ranges/seq operand=loud). leading=act 매클럭·X-init lenient·Bool-only byte-identical. 적대 3렌즈 CLEAN(goto overlap-divergence·nonconsec n-th-b boundary 손-트레이스 truth table 일치·렉서 무충돌). **S9(`eb2b4bd`, 967 green): `within`** — `seq1 within seq2`를 `synth_within`이 `match(seq2) & OR_{i=0}^{L-k1} reg^i(match(seq1))`로 desugar(seq1이 seq2 윈도 안에서 완료; L=seq2 윈도 길이·k1=seq1 길이). `Sequence::Within` variant(AST flip·sim-ir 골든 무변경). contextual keyword·top-level antecedent 한정·bounded boolean operand만(unbounded/goto/nonconsec/throughout/nested→loud). 적대 2렌즈 CLEAN(윈도-엣지 off-by-one 정확·straddle 거부·k1>L=never·byte-identity·골든 무변경). **🏁 full SVA 시퀀스 서브셋 완성(S4-S9): `##n`·`##[m:n]`·`##[m:$]`·`[*n]`·`[*m:n]`·`throughout`·`[->n]`·`[=n]`·`within` 전부 순수 IR-0(트랙 전체 sim-ir 무변경, `.vu` AST-hash 재핀만), 각 슬라이스 적대 리뷰 통과, 3-OS CI green.** 

**16~21탄(2026-06-15/16, 1088 green): SVA 검증 트랙 마무리 + 정확성 핀 3건.** 16탄(1004, `148b4bf`)=SVA 트랙 A 프레이밍. 17탄(1018, `6e573d3`)=bit-select event control 패닉 수정(`@(posedge clk[0])` POISON_NET→OOB; LSB 상수 bit-select=underlying net arm, 그 외·level=loud E3009). 18탄(1025, `2233ccd`)=P0-10 unsized 리터럴 폭(≥2³¹ silent 절단→IEEE §3.5.1 값-담게-성장, `MAX_NET_WIDTH` 캡). 19탄(1030, `8241c61`)=`decimal_bits` O(n²) parse DoS 수정(40000자리 27s→25ms: schoolbook→Horner base-10¹⁹/2⁶⁴ limbs + `MAX_DECIMAL_DIGITS` 시간경계 가드 + `alloc_width` clamp). 20탄(1051, `e1bc4f3`)=named `property`/`sequence` 선언·인스턴스화(`sequence/property NAME;…end*`+`assert property(NAME)`·시퀀스 본문 `NAME`, 순수 IR-0). 

**21탄(1088, 4커밋: A1 `989e7b1`·A2 `393b010`·A3 `af39b9a`·A4 `43d41c2`): SVA 트랙 A1–A4 완주**(각 TDD+적대 4렌즈 라이브 재현) — A1 named seq/property **formal arguments**(위치 바인딩+formal→actual 치환, IR-0·.vu 재핀 0=필드 기-reserve) · A2 **action-block sampled values**(pass/fail action 내 `$past/$rose/$fell/$stable`=prev-reg 공유)+시퀀스 local var loud · A3 **multi-clock 정준**(`@(c1) ante |=> @(c2) cons` 두-프로세스 핸드오프, **유일 AST 변경=`consequent_clock`→.vu 재핀**) · A4 **generate-scope decls + property-references-property**(generate 재귀 수집·bare OVERLAP consequent `…|->q`→`!q.ante||q.cons` 체인). 전 SVA 트랙 sim-ir 무변경(format_version 8 유지). ⭐교훈: 슬라이스마다 적대 리뷰가 인접/일반 silent-wrong 적발(A1 `resolve_named_top` bare-param-seq·A3 c2>c1 over-fire); `Sensitivity` PartialEq=span 포함→클럭 비교는 span-무관 헬퍼. 잔여 SVA(별개·후속): multi-term 시퀀스 cross-clock(A3 확장)·outer-`|=>` prop-ref skew·action block 내 sampled local var·sequence local variable·recursive property. 

**22탄(2026-06-16, 1125 green, `592cb10` push): 사용자 선택=충실구현 → faithful deferred immediate asserts (`assert #0`=Observed·`assert final`=Reactive), format_version 8 유지(순수 IR-0).** 설계 워크플로(9-agent: 이해→3-design 경쟁→synth)=**IR-0 사이드카+엔진 진짜 Observed/Reactive 리전** 채택(first-class `Stmt::DeferredAssert` IR variant은 v9 bump 비용만 추가·엔진 작업 동일이라 기각). 파서 `#0`/`final`→`Stmt::DeferredAssert`(`AssertDefer`; 非-zero `#n`=loud) · elaborate=어셔션당 **flush 마커**(사이드카-억제 no-op `$display`)를 Branch 전 방출+`defer_marks`/`defer_acts` 사이드카(action SysTask StmtId=push_stmt 훅, 경로-무관) · 엔진=`Postponed`에 2 maturation 맵+run() NBA-후 Observed→Reactive 리전(§4.4 order, Postponed 직전)+disable-fork retain+$finish 드레인+`emit_severity_message` 팩터. **flush 키=`(marker, aid, gen)`**(⭐설계 단계 의미론 트레이스가 워크플로 plan의 잠복 버그 선제 적발: action-StmtId 키는 fail→pass 재평가서 빈 pass-arm이 fail 리포트를 못 flush→마커-키로 교정). `.vu` AST-hash 1회 재핀, sim-ir/fmt_ver 8 byte-불변, 무-deferred 디자인 byte-identical. **적대 4렌즈(워크플로, 오라클 無=iverilog 13.0 deferred assert 거부) → FIX-FIRST 판정·HIGH 2건 라이브 재현 후 수정**: D1(P0 silent-wrong=재활용 aid가 완료된 prior 인스턴스의 pending 실패를 flush→실패 소멸)=`Activity.gen` 세대 스탬프(key 3-tuple, disable-fork retain도 `(aid,gen)`) · D2(§16.4.3 오값=action 인자가 maturation서 재평가)=reach서 메시지 렌더(time/scope도 reach=동일 슬롯). 문서화 hand-IEEE 한계: 非-$display/$error action=inline(loud W-note)·`disable <block>`=非-flush(over-report=safe 방향). 15 신규 테스트(글리치 필터+immediate 대조·flush-once·리전 순서·fork per-instance·recycle 비소멸·arg-timing·disable-fork·deferred $fatal). ⭐교훈: 오라클-無 기능은 적대 리뷰 라이브 재현이 유일 teeth(D1/D2 둘 다 거기서 적발); 설계 트레이스가 plan 잠복 버그 차단. 다음=스코프 결정(class/OOP·커버리지·automatic/recursive 콜스택·math transcendentals). 

**23탄(B-track, 2026-06-17, 1145 green, `cb79261`): automatic/recursive 콜스택 모델 완주("E3023" 해제) — B1(재귀/auto 함수=`Expr::Call`/`run_frame_call` `&self` eval)→B2(재귀/auto 태스크=`Terminator::Call`/`run_task` executor+nested call)→B3(disable-unwind through frames)→B4(per-decl `automatic` lifetime override, iverilog 거부=hand-IEEE). 순수 IR-0(sim-ir/fmt_ver 8 불변, frame nets+FuncTable/TaskCallInfo 사이드카 out-of-band; 형상은 PR1-B/M3서 frozen 예약). frame=automatic→fresh window/static→shared slab(iverilog 공유슬롯 손상 충실), 재귀=Rust 스택+`MAX_CALL_DEPTH` 가드. ⚠️사이드카 비직렬화→one-shot `vita`만(staged vrun 미지원). 상세=memory [[vitamin-frame-call-design]]. 

**24탄(N1, 2026-06-17, 1161 green, `f4aab23`): Recommended sequence(ROADMAP §4.4) 착수 → NBA repeat-event `<= [repeat(n)]@(ev)` (blocking 형 `cfef719`의 NBA 쌍둥이). parent 블록 안 함(iverilog 프로브 확정)→선형 desugar 불가·frame-task 동기실행이라 arg-capture 불가 → **capture-now(RHS+LHS-index)/`fork…join_none`/NBA-write desugar**, `repeat(0)`=plain NBA, LHS-index=sample-NOW(blocking은 write-time=비대칭). 순수 IR-0(`Stmt::NonBlocking.event` AST flip=`.vu`/schema 재핀, sim-ir/fmt_ver 8 불변). 적대 4렌즈(7-agent 워크플로)→3 confirmed: **2 silent-wrong 수정(blocking 쌍둥이 동봉)** — X/Z 상수 count=0회(런타임 오분류 수정, `count_lit_is_xz`)·const part-select `[msb:lsb]` lvalue 1-bit temp(`const_of_expr_u32` Add/Sub 재귀=엔진 fold와 lockstep) — + 동시-틱 region tie=**pin**(committed 값 $strobe/이후틱/final 전부 일치, vita LRM-faithful; faithful 매치는 engine event-armed-NBA 필요). ⭐교훈: 오라클 있어도 적대 리뷰가 인접 pre-existing silent-wrong 적발. 다음=N2(SVA 잔여). **N2 트랙 대부분 소진: N2a-1(`e804546`, multi-clock seq antecedent `@(c1) a ##1 @(c2) b |-> c`)·N2b(`9fbb02d`, 2-cycle non-overlap prop-ref skew) + 25탄 N2d(`7a2fe9c`, 1207 green): property-level `and`/`or` 연산자 + recursive property.** `PropExpr{Seq/Impl/And/Or}` 트리 + `prop_expr: Option<PropExpr>`(None=flat byte-identical, 全 corpus 무손상) → elaborate `synth_prop_expr`가 per-clock viol로 환원(`and`→`||`·`or`→`&&`·`|->`→`a&&viol(cons)`·`|=>`→pend-reg skew), **tail recursion `…|=> NAME`=`1'b0` 드롭(per-clock 재시도가 의무 이행)** → always(`b and (1'b1|=>p)`)→`if(!b)`·weak-until(`q or (b and (1'b1|=>p))`)→`if(!q&&!b)` 정확 환원. 순수 IR-0(sim-ir/fmt_ver 8 불변·AST `.vu` schema 재핀만). iverilog SVA 거부=hand-IEEE value-pin(27 테스트). 적대 4렌즈(라이브 재현)→**HIGH silent-wrong 수정**: 비재귀 `|=>`(clock skew 1)를 同-클럭 operand와 `and`/`or` 混合 시 두 attempt-start 클럭 어긋남(`(a|=>b) or q`=false-pass+false-fire 둘 다 재현) → **per-node clock-skew 추적+混합-skew loud reject**(uniform `(a|=>b) and (c|=>d)`=verdict-correct 유지·idiom은 全 skew0=면역) + parser `[*`/`[->`/`[=` 토큰 depth 카운트(N2a-1 신규토큰-vs-bracket-scan 교훈 연장) + nesting depth cap(스택오버플로 방지). N2c=defer(per-attempt thread storage가 thread-collapsing 파이프라인과 근본 상충, honest loud reject). ⭐교훈: 합성 정확성은 적대 리뷰 라이브 재현으로만 확정(설계 트레이스 2회 오판: gate의 vacuous-tautology·design-agent의 t0 latch — 둘 다 per-clock 환원으로 교정). **이어 26탄 N2a-2(`e84656f`, 1218 green): cross-clock SVA를 N-clock chains + `|=>` consequent로 일반화** — `synth_crossclock`을 `##1` 핸드오프 체인으로 일반화(`collect_xclock_segments` 평탄화, 세그먼트당 set-only/discharge 핸드오프)+`|=>`=`hf_final` 추가단(다음 final-clock 엣지서 consequent 체크, check-then-arm). **순수 IR-0·elaborate-only(AST 무변경 → `.vu` 해시 불변)**, N=2 overlap=byte-identical(N2a-1 suite 가드). 적대 3렌즈→**timing/scope CLEAN(silent-wrong 0)·flatten 2 verdict-safe fix**(stale `|=>`-loud 테스트=synthesize-and-fire로 전환·"deferred to N2a-2" 문구 갱신); multi-attempt count-merge·coincident-distinct-clock delay=N2a-1 핸드오프 모델 계승 pin. **🏁 N2 트랙 종결**(N2a-1·N2b·N2d·N2a-2, N2c=defer). 잔여 후속(별도·저우선)=cross-clock multi-term segment lane. **다음 권장=N3**(hierarchical 이름 참조 `tb.dut.x` read-only, ROADMAP §4.4 ⭐ — 흔한 멀티모듈 TB 패턴·iverilog 강한 오라클). 

**27탄 N3(`a95b44b`, 1233 green): hierarchical read-only 이름 참조 완료.** `tb.dut.x`/`top.m.l.v` 식-내 read를 **deferred-ref+arena-patch**로 해소 — multi-seg 식 경로가 기존 dotted 심볼(interface alias)이 아니면 pass-7 lowering서 placeholder `Signal` 방출+기록(child net=pass8라 미존재), 전 인스턴스 elaborate 후(`run` roots 루프 뒤) `resolve_deferred_hier`가 placeholder를 resolved NetId로 패치(WidthTable는 패치 후 IR서 build→width/select 정확). **순수 IR-0·elaborate-only**(sim-ir/fmt_ver 8·AST `.vu`·schema 전부 불변). `hier_lookup`=**IEEE §23.6 commit-to-scope**(leading seg를 innermost enclosing scope(child scope/singleton genblock `g`⇒`g[0]`/net)에 바인드→COMMIT→remainder를 그 scope서 해소, 미발견=error·outward 미계속). WRITE=loud(`resolve_net`, read-only subset). iverilog 강한 오라클 value-pin(dut.x=43/44·m.l.v=9·width 200·bit/part-select·CA·clocked compare·gblk.x=7). 적대 3렌즈(라이브 차분)→**HIGH silent-wrong 2 수정(commit 전)**: ①wrong-net(옛 whole-tail outward strip가 leading seg=inner scope·remainder invalid 시 무관한 outer net 묵묵 채택: `b.v`·local-shadow `cfg.mode`)→commit-to-scope ②packed multidim element `dut.pm[1]`이 1-bit bit-select로 silent mis-lower→multi-dim packed/unpacked array hierarchical read=loud(element select=deferred lane) + MEDIUM named genblock `gblk.x` 해소(`g[0]` 매핑). 디퍼(loud·verdict-safe): indexed-seg parse(`g[0].x`)·array-element read·$dumpvars hier arg·hier param. 15 테스트(commit-to-scope teeth 포함). **N4(clocking block) 관문 평가 → ⛔ 조건부 NO-GO(2026-06-18)**: 파서+`cb.sig` 입력샘플 read+`@(cb)` 전부 시제했으나, **clocking 입력 샘플링=Preponed 리전 필수**(IEEE §14.13)인데 vita 스케줄러 부재 → NBA 샘플러(`always @(ev) cb_q<=sig`)가 관용구 `@(cb); x=cb.sig`(엣지 read)서 **1 clocking-cycle lag**(read=Active resume, 샘플러 NBA=그 후 settle→직전 엣지 샘플 반환; 실측 `cb.q=11` vs 정답 12)=silent-wrong. live-read는 엣지-read만 정답·지연-read 오답 → 양쪽 만족 불가. iverilog가 clocking 거부=hand-IEEE(오라클 無). 시제 코드 revert(미커밋). 재진입=Preponed/sampled-value 스케줄링 리전 도입. ⭐교훈: **자체 테스트가 커밋 전 silent-wrong 차단**(cb.q=11 적발) — 오라클 無 기능은 직접 의미론 트레이스가 1차 teeth. **다음 권장=N3.1**(hierarchical 배열 element read `dut.mem[i]`, ROADMAP §4.4 ⭐). 

**28탄 N3.1(`95cc674`, 1240 green): hierarchical 배열 element read `dut.mem[i]` 완료.** 2-seg hierarchical base의 indexed read를 deferred — **index를 lowering-time에 lower**(params/genvars/function-formal subst 전부 정확)→`idx_eid` 저장, base net은 fixup서 `hier_lookup`→shape로 select 빌드(single-dim unpacked array=`net[idx-lo]`·scalar/vector=bit-select; multi-dim array/packed/dyn/event=loud follow-on). 순수 IR-0(AST 무변경, golden 동결). iverilog 강한 오라클 value-pin(`dut.mem[2]`=204·loop e0..e3·vector bit-select 무회귀). 적대 2렌즈(differential)→**HIGH silent-wrong 수정**: index가 function formal일 때 fixup 재-lower가 inline subst 소실→shadowing outer net 묵묵 read(f=10 vs 13) → **index lowering-time lower**로 일괄 수정(param/genvar index fold·nested hier index `dut.mem[dut.mem[0]>>3]`=20 동시 해결). 22 테스트(teeth 4). ⭐교훈: deferred 기능의 재-lower는 lowering 컨텍스트(subst/param/genvar) 소실 위험 — 컨텍스트-의존 값은 lowering-time에 lower하고 eid만 저장. **다음 권장=N3.1 follow-on**(multi-dim `dut.grid[i][j]`+packed `dut.pm[i]` hierarchical element, 강한 오라클) 또는 **N5**(coverage). **CI-fix(2026-06-18, `84b8b36`, 1241 green): Windows 3-OS CI 복구** — N2d 후 `vitamin-ci` Windows 잡만 실패(macOS/ubuntu green)했는데, 테스트 `deep_andor_nesting`의 2000-deep `a and a…` 프로퍼티가 left-leaning `PropExpr` 트리로 파싱되고 `synth_prop_expr`의 **재귀 `clone()`**(이어 재귀 Drop)이 Windows 메인스레드 **~1 MiB 스택**(Linux/macOS는 8 MiB)을 `SVA_SEQ_ALT_CAP=256` cap 발화 전에 오버플로 → 진단 없이 abort. 수정=`crates/cli/src/main.rs`에서 드라이버 `cli::run`을 **256 MiB 명시 스택 워커 스레드**(rustc/swc 방식)에서 실행 → Windows가 Linux/macOS와 동일 동작, 기존 cap이 깨끗한 진단 생성(`cli::run` 무변경=in-process 테스트 불변, 패닉=`resume_unwind`로 exit 101 보존). 회귀 테스트 `very_deep_andor_chain_does_not_overflow_default_stack`(40k operands=8 MiB도 초과→모든 OS서 wrapper 없이 실패, 로컬 revert로 macOS 오버플로 재현 검증). VCD writer 스레드는 재귀-무(byte 스트리밍만)라 default 스택 안전. 적대 리뷰 3렌즈+synth=SHIP_AS_IS(must-fix 0). ⭐교훈: **재귀 AST clone/drop은 작은 Windows 스택을 cap 발화 전에 오버플로** — depth cap이 walk에만 있고 그 전 clone이 full-depth면 무력; 근본 해결=드라이버를 큰 스택 스레드에서 실행. **이어 follow-up(`712f8b7`): frame_call 런어웨이 테스트 Windows 오버플로** — main 래퍼로 sva 통과 후 다음 deep 재귀 in-process suite(`runaway_recursion_hits_depth_cap_fatal`·`cont_assign_originated_runaway`)가 표면화: `MAX_CALL_DEPTH=65536`까지 재귀하는데 256 MiB on_big_stack worker에서 macOS는 ~4 KiB/frame이라 맞지만 Windows debug frame은 ~2.5× 두꺼워(~0.5 GiB) cap 발화 전 오버플로. **이는 출하 바이너리도 동일 버그**(런어웨이 재귀가 Windows서 256 MiB 래퍼 오버플로→fatal 대신 크래시). `MAX_CALL_DEPTH` 65536→**8192**(cap이 ~80 MiB 최악서 발화=256 MiB 내 3× 여유, 全 OS·바이너리·테스트 동시 보호; legal `cnt(2000)`의 4×·실 RTL 무관). runtime guard(sim-ir 아님)→골든/format_version 무영향. ⭐교훈: **재귀 깊이 cap은 가장 두꺼운 plat(Windows debug frame ~2.5×)의 스택 footprint로 잡아야** — 한 OS서 맞는 cap이 타 OS선 cap 전 오버플로. 

**29탄 N3.1 follow-on(2026-06-18, `7d2f9b4`, 1252 green): multi-dim `dut.grid[i][j]` + packed `dut.pm[i]` hierarchical element read.** `DeferredHierSelect.idx_eid`→`idx_eids: Vec`(전 인덱스 lowering-time lower=N3.1 HIGH fix 확장)·신규 `hier_sel_chain`(BitSelect 체인이 2-seg hierarchical Ident bottom, known-net=interface alias는 normal path)·`resolve_deferred_hier_sel`가 shape dispatch: unpacked→`build_hier_array_read`(local `lower_array_read` 미러+신규 eid-sourced `flatten_word_eids`=local push order 골든 byte-불변)·packed→`build_hier_packed_read`(`lower_packed_read` 미러). 순수 IR-0(sim-ir/AST/.vu/golden/fmt_ver 8 무변경). iverilog 강한 오라클(non-zero-based·descending·asymmetric·array-of-packed whole-elem 일치). 적대 4렌즈(워크플로우, 라이브 iverilog)→**HIGH silent-wrong 수정**: array-of-packed(`reg [3:0][7:0] qm[0:1]`=net_is_static_array AND packed_dims)가 array path로 가 sub-index `dut.qm[i][j]`를 silent 1-bit(vita 1 vs iverilog byte bb)=follow-on 前 loudness 회귀→가드(array-of-packed+surplus index=loud deferred, whole-elem `dut.qm[i]`=정상 32-bit). loud(silent-wrong 0): partial slice·bit-of-bit·scalar multi-index·packed over-index·hier WRITE·event/dyn·3-seg indexed. inner-dim OOB=local parity(X §7.4.6). **잔여 N3.2(별개 pre-existing)**: LOCAL `lower_array_read`/`collect_array_write`도 array-of-packed sub-index를 silent 1-bit(read+write 공유 gap)→전용 슬라이스. ⭐교훈: 적대 리뷰가 **인접 pre-existing 결함**(local array-of-packed read+write silent 1-bit) 적발 — 단 hierarchical read 슬라이스 스코프 내선 loud-reject(회귀 차단)하고 local 정확성 핀은 N3.2로 분리. 33 hier_ref 테스트(teeth: formal-index·sub-index-loud·whole-elem·OOB). 

**30탄(2026-06-18): SPEC 전수 최신화 + ROADMAP §4.5 미구현 풀 인벤토리 + N3.2 완료(1261 green).** ①`docs/preview/` SPEC 20파일 최신화(워크플로우 6버킷 audit→적대 verify→synth, 각 `crates/` grep 확인, `6a14f69` push): freeze 표(frame-call·repeat-event NBA·계층 read-only refs IN 승격·clocking N4 NO-GO), system-tasks(`$past`류·conversion·bit-vector·`$random`·`$stime`·file WRITE·`$readmem*` DONE / `$exit`·`$system`·introspection·`$dist_*`·transcendentals 미구현 — 적대 verify가 `$exit`/`$system` task-form='WARN+skip≠E3009' over-claim 정정), infra(fmt_ver 8·MsgCode 55·trailer 인벤토리). ②**ROADMAP §4.5 미구현 전체 인벤토리**(124 raw→19항목 dedup, 우선순위 순: 계획됨 11 / 조건부·장기 5 / 비계획 3). ③**N3.2 LOCAL array-of-packed 서브원소 read/write 완료**(`qm[i][j]` silent 1-bit→packed byte): `lower_array_read`/`collect_array_write`가 net이 `packed_dims`이고 unpacked D개 뒤 trailing 인덱스 있으면 packed-dim flatten 라우팅(offset=`flatten_word(packed_dims,trailing)`·width=잔여곱·`PartIdxUp`, base=`word:Some(element)` — `lower_packed_read` 미러), 엔진 무변경(`{word:Some,PartIdxUp}`=`base+off`), 순수 IR-0(sim-ir/AST/schema/fmt_ver 8 무변경). iverilog 강한 오라클(descending byte-exact), plain array/vector/packed=byte-identical(`net_is_static_array`+`packed_dims`+trailing 게이트). 적대 3렌즈(라이브 iverilog ~30 probe)=SHIP_AS_IS·in-scope 결함 0. ⭐**N3.2 적대 리뷰가 인접 pre-existing 적발→신규 N3.3**: ascending packed `[0:3]` 방향이 `flatten_word`/`lower_packed_read`서 direction-무관(`coord=idx-lo`)이라 `x[0]`=vita LSB바이트 vs iverilog MSB바이트(배열 무관·plain packed도 동일 silent-wrong). N3.2는 array-of-packed를 plain packed와 byte-consistent로 유지(읽기·쓰기, 둘 다 iverilog와 동일 발산)→악화 0, 수정은 N3.3 전용 슬라이스(packed 골든 영향). 9 array_of_packed 테스트(teeth: descending read/write·3-dim·var-index·full-index=1bit·over-index loud·plain 회귀·ascending=plain consistency). 

**31탄 N3.3(2026-06-18, 1270 green): ascending(little-endian) packed-dim 방향 `reg [0:3][7:0]` 완료 — N3.2/N3.3 적발 silent-wrong 정정.** `flatten_word`/`flatten_word_eids`가 packed coord를 `idx-lo`(min 엔드포인트, direction-무관)로 계산→ascending `[0:3]`을 descending처럼 처리→`x[0]`=LSB바이트(vita `dd`) vs iverilog MSB바이트(`aa`), 무진단. 배열 무관(plain 다차원 packed+array-of-packed+hierarchical read 공유 결함). **수정(IR-0)**: `packed_extents`/`packed_dims`가 per-dim `ascending`(msb<lsb) 기록, ascending 차원은 `coord = hi - idx`(hi=`lo+size-1`=lsb 엔드포인트=내부 bit 0, plain 벡터 `norm_offset_for_net` 미러). 신규 `packed_split` 헬퍼로 **5 PACKED 호출부**(lower_packed_read·collect_packed_write·build_hier_packed_read·N3.2 array-of-packed r/w 2곳)만 방향 전달, **9 UNPACKED 호출부**(array_dim_extents/net_dim_extents/array_dims 경유)는 `&[]`→descending+unpacked **byte-identical**(골든 0 flip). 엔진/sim-ir/AST/schema/`fmt_ver 8` 무변경. iverilog 강한 오라클(asc read/write·mixed asc-desc·asc-inner bit-select·3-dim·non-zero base·parametrized·OOB→xx·X→xx·packed port·hierarchical read 전부 byte-exact). 적대 3렌즈(워크플로우, 라이브 iverilog)→**0 finding·0 regression**(correctness/byte-identity/completeness 전부 CLEAN; 14 flatten_word 호출부 분류 검증=5 packed+9 unpacked, bypass 0, staged==one-shot). 18 테스트(9 신규 `packed_ascending_n33.rs`+9 `array_of_packed.rs` 정정=이제 iverilog-correct). ⭐**적대 리뷰가 인접 신규 적발→N3.4**: 다차원 packed **part-select** `x[3:2]`(원소 범위=`aabb`)가 generic RangeSelect 경로(flat-bit `[3:2]`=`3`)로 가 **silent-wrong**; part-select는 `expr_packed_chain`/`flatten_word` 미경유라 N3.3 무관·pre-existing(ascending part-select는 이미 loud E3009). N3.3 스코프 외로 분리·기록(packed 골든 영향). ⭐교훈: 다차원 packed 방향은 plain 벡터 `norm_offset_for_net`과 lockstep이어야(packed는 별도 flatten 경로라 방향 누락이 silent); 호출부 분류(packed=방향/unpacked=`&[]`)가 byte-identity by-construction 게이트. 

**32탄 Medium 묶음 착수(2026-06-18): 게이트 플랜(8-agent 워크플로우) + rank 1 DIR-PP 완료(1286 green).** 게이트=SYS-READ·SYS-INTRO·DIR-PP를 IR-0-now(DIR-PP·introspection const-fold·sampled desugar) vs 단일 **v9 bump**(file-read·dist·cast·writemem·monitor=새 SysFuncId×13/SysTaskId×5)으로 분할, 6-슬라이스 순서(ROADMAP §4.5.1); ⚠️미인식 `$func`=E3009-LOUD(디자인 kill) vs `$task`=skip라 introspection const-fold 고가치, `$exit`/`$system`=defer(오라클無/3-OS-hostile). **DIR-PP(rank 1, hdl-preprocess only, 순수 IR-0)**: `substitute()`에 token-paste(``` `` ```=두 backtick 삭제·**whitespace 보존**=iverilog parity `a``b`→ab·`a `` b`→`a  b`; 초기 trim 구현은 적대 리뷰가 divergence 적발→수정)+stringification(`` `"…`" `` 내부 param 치환·`` `\`" ``→`\"`·내부 paste 삭제) 추가, **object-like 매크로도 substitute 경유**(empty params=identity, 적대 byte-identical 증명). 4 directives accept-ignore(begin/unconnected_drive=arg line 소비; keyword-set narrowing 미구현=loud-reject 한계). 13 unit+4 e2e(iverilog -E+vvp 오라클). 적대 3렌즈→**HIGH refuted**(stringify 내 nested-macro 미전개=리뷰어 주장 오라클 오류, 실 iverilog도 verbatim=vita 일치)·regression/scope CLEAN·**genuine exotic 2건(spaced/in-stringify paste) 수정으로 iverilog 완전 일치**. ⭐교훈: 오라클-기반 적대 리뷰도 verify 단계서 리뷰어의 oracle 주장 자체를 라이브 재검증해야(HIGH가 false-positive로 판명). 

**33탄 Medium 묶음 rank 2 — SYS-INTRO array-query const-fold(2026-06-18, 1300 green, 순수 IR-0).** `$size`/`$left`/`$right`/`$low`/`$high`/`$increment`/`$dimensions`/`$unpacked_dimensions`/`$isunbounded`를 elaborate const-fold(`$bits` 3-lane 선례, arg=TYPE/net ref 비평가). **신규 `dim_desc`(NetId→ordered (left,right) per dim, unpacked decl순→packed)를 decl 시점 AST range서 계산**(`record_dim_desc`, netvar+port 양 경로 호출)→`try_introspect_fold`가 32-bit Const. dim순=unpacked→packed·`$increment=left>=right?1:-1`·0-dim/OOB dim→32-bit X(`const_x32_expr`). ⭐**scalar `reg s`(empty desc=0-dim, `$size`→x)≠`reg [0:0]`(1-dim)** 구별=net msb/lsb로 불가하던 것을 decl-time descriptor로 해결. `$typename`=defer(string 포맷, hand-IEEE, loud 유지). 14 e2e(iverilog 핀, scalar/`[0:0]`/ascending incr=-1/multidim/dim-arg). 적대 3렌즈(워크플로우)→golden/decl-safety CLEAN·**HIGH 2 regression 수정(loud→silent-wrong=최악)**: ①integer/time이 0-dim X로 fold(AST range無라 empty desc)→implicit packed dim 추가(integer=31:0·time=63:0·**real은 0-dim 유지**=over-reach 방지) ②ANSI packed multidim port `input [3:0][7:0] mp`가 single 32-bit dim→port-path `record_dim_desc`로 2-dim. const-context fold(localparam/range spec)=pre-existing 미지원(`const_eval_in_scope` 미연동, slice 무관)=한계 박제. ⭐교훈: decl-time AST 기반 descriptor가 flattened net metadata(array_len·msb/lsb)의 손실(scalar vs `[0:0]`·implicit-vector type·multidim port) 회피; 단 atom type(integer/time)의 implicit width와 real의 dimensionless 구별은 명시 special-case 필요. 

**34탄 Medium 묶음 rank 3 — SVA `$changed`/`$sampled`(2026-06-18, 1303 green, 순수 IR-0).** 기존 prev-reg 머신(`rewrite_sampled`, $past/$rose/$fell/$stable)에 2개 추가: `$changed(e)`=`CaseNe(prev,e)`(=$stable의 1-bit 부정)·`$sampled(e)`=`e`(identity — region 모델上 sampled=current, 재귀로 nested 해소). `is_sva_sampled_fn`에 등록. hand-IEEE(iverilog가 concurrent assert+$changed/$sampled 모두 거부)→합성 firing으로 검증(3 e2e: change 탐지·identity 비교·$stable 부정). 직접 엣지 probe=hier/multiarg/outside-assertion 전부 loud(기존 $past 가드 재사용, silent-wrong 0)·$sampled(expr)/nested ok. first-clock X-init prev spurious=기존 $past 선례와 동일(lenient). **`$countbits`=defer**: iverilog 13.0이 vpi assertion crash(오라클 無)+x/z 카운트는 `$countones`로 desugar 불가(per-bit case-eq sum 또는 v9 SysFuncId 필요)=저우선. **🏁 Medium 묶음 IR-0 슬라이스(rank 1-3) 전부 완료** — DIR-PP·SYS-INTRO const-fold·SVA sampled. 잔여=rank 4 v9 shape bump(SysFuncId×13+SysTaskId×5, 골든 regen)→rank 5(file READ)·rank 6(dist/cast/monitor). 

**35탄(2026-06-18, 1303 green): rank 4 v9 shape bump 완료 — format_version 8→9(shape-only inert).** SysFuncId +13(file-read $fgets/$fscanf/$sscanf/$fread/$feof/$fgetc/$ungetc + $dist_{uniform,normal,exponential,poisson,chi_square} + $cast func-form, append-after-StrCmp) + SysTaskId +5($writememb/$writememh/$cast task-form/$monitoron/$monitoroff, append-after-StrPutC). golden regen=`REGEN_GOLDEN=1`(canonical txt + registry.ron), 신규 `SimIr` 해시 `ff9d5be…`; **Process 서브핀 UNCHANGED**(`61db2e…`)=Process가 Stmt/Expr를 arena INDEX(u32)로만 도달→루트만 flip(국소성 확정, v8의 WaitCause-via-SuspendState 동시-flip과 대조). compiler exhaustiveness가 4 match 사이트 강제(elaborate `ir_bits_of` 폭=전부 int 32-signed·engine `eval_sysfunc`(file/dist/cast func eval-path=X like Fopen/ValuePlusargs)·`width.rs` SelfWidth·`builtins.rs` SysTask dispatch=no-op Continue placeholder). **inert 증명 by-construction**: elaborate가 신규 `$`-name→variant 매핑 0 + emission 0(grep 양쪽 empty)이라 신규 variant=orphan/dead·엔진 arm=dead code → 전체 suite 1303/1303(=bump 前 동일 count)·clippy/fmt clean·커밋된 .velab 0. v5/v7/v8 "단일 atomic 형상 bump" 선례 답습. 다음=rank 5(file READ, post-bump IR-0, StmtEffect direct-rhs intercept+SimState files map)·rank 6(dist/cast/monitor). ⭐교훈: 형상 bump의 inert 검증=(a)golden canonical diff이 의도한 append만인지 (b)서브핀 flip 패턴이 도달 경로(inline vs arena-index)와 일치하는지 (c)신규 variant emission grep=0 (d)테스트 count 불변 — 4개 모두 by-construction oracle. 

**36탄(2026-06-18~19, rank 5 SYS-READ 부분 진행): file-IO READ family — 설계 워크플로(understand×4∥iverilog-probe×4→synth)가 2-PR 분할 권고 → 슬라이스별 진행 중.** ✅ **5a writemem(`34c4623`, 1309 green)**: `$writememb`/`$writememh`=readmem 미러(`// 0x00000000` 헤더 라인 항상·hex=ceil(w/4) lowercase+per-nibble X/Z 압축(all-x→x·all-z→z·any-x→X(X dominates Z)·z-mixed→Z, 실비트만 검사라 partial top nibble의 phantom 0은 불참)·bin=per-bit 무압축·inclusive range/descending·OOB 비종료(file 미생성)). 전 기대값 직접 iverilog 라이브 검증. ✅ **5b 읽기 프리미티브(`df8a349`, 1320 green)**: `$fgetc`/`$feof`/`$ungetc`=전부 `$fopen` StmtEffect direct-rhs intercept 패밀리(eval은 `&self` pure라 파일 offset 못 advance→결정성 by-construction)+신규 SimState `read_state`{eof, pushback}+`readable_fds`(out-of-band, 골든 무영향). `$fgetc`=byte/−1(EOF)·`$feof`=lazy(−1 반환한 read가 set, bad-fd=−1 NOT 0)·`$ungetc`=0/−1. **적대 리뷰 3렌즈(라이브 iverilog 차분)→determinism/VM-parity/staged·regression/loud 둘 다 CLEAN, HIGH 2 + LOW 1 수정**: ⓐ`$ungetc` pushback=**unbounded LIFO 스택**(probe agent의 "1-deep" 주장이 라이브서 거짓→push A,B,C→read C,B,A; `Option`→`Vec`) ⓑ write-only fd("w"/"a")가 vita서 readable화(silent-wrong)→per-fd read-capability(`'r'`/`'+'` 모드=r/r+/w+/a+ readable, w/a write-only) 추적, write-only는 $fgetc=−1(eof latch 안 함)·$feof=0·$ungetc=−1 ⓒ`$ungetc(c)` c에 x/z 비트=정상 char(low byte, x→0, return 0); EOF sentinel은 정확히 −1만(`has_xz` 게이트 제거). ⭐교훈: probe-agent oracle 주장(LIFO·write-mode)은 라이브 재검증 필수(rank-1 DIR-PP 선례 연장)—설계 가정 2건이 라이브서 뒤집힘. **잔여**: 5c `$fgets`(목적지 str write=value_plusargs 템플릿, full-width-N·right-justify(MSB-first)·newline-retain·EOF=count0+unchanged, 의미론 핀 완료)+`$fread`(big-endian·single-reg/memory·partial-fill 기존값 보존·start/count) → PR-B `$fscanf`/`$sscanf`(포맷 파서+첫 멀티 ref-write). 

**37탄(2026-06-19, 1336 green): 🏁 PR-A 완성 — 5c1 `$fgets`(`2c9b000`)·5c2 `$fread`(`50a1581`).** 둘 다 `$value$plusargs` intercept 패밀리(worker가 목적지 arg write+count를 lhs 반환). `$fgets`=목적지 폭만큼 whole-byte 읽되 newline서 조기 중단(retain), MSB-first right-justify, EOF=count0+unchanged. `$fread`=target single-reg/whole-memory(element-select `mem[i]`=loud), 신규 `fill_reg_slots`(capacity=ceil(W/8), byte i→슬롯 capacity-1-i MSB측부터, partial=미기록 저슬롯 기존값 보존, top 슬롯 부분폭), memory=base부터 ascending(선언방향 무관)·`(start,count)` inclusive window+OOR warn/clamp. 전 거동 iverilog 라이브 직접 검증. **설계 워크플로 probe 오류 1건 적발**: `reg[12:0]`←`0xFFFF`=`0x1fff`(low W bits) not `0x0fff`("top nibble drop"). **적대 리뷰(fgets+fread 3렌즈 라이브 차분)→HIGH 2+MEDIUM 1 수정**: ⓐ`$fread` start/count의 x/z=0 coerce(`val&!unk`), vita는 absent로 오취급해 count None→fill-all 발산 ⓑ`$fgets` embedded-NUL=반환 string/count는 첫 NUL서 절단(C semantics)하되 line은 stream서 소비 ⓒ`$fgets` empty result(leading-NUL/sub-byte dest+데이터 있음)=dest를 0 clear(genuine EOF의 unchanged와 구별). determinism/VM/staged·regression 렌즈 CLEAN. LOW 1 defer(family-wide: 전 `$value$plusargs` intercept가 lhs index를 READ phase서 해소→`arr[w]=$fread(w,fd)` self-aliasing은 iverilog와 발산, pre-existing·contrived). ⭐교훈: probe-agent oracle은 라이브 재검증(설계 0x0fff·LIFO·write-mode 등 4건 뒤집힘); 적대 리뷰가 NUL/clear/x-z 등 silent-wrong 다수 적발. 잔여=PR-B `$fscanf`/`$sscanf`(scanf 포맷 파서+첫 멀티 ref-write 인터셉트). 

**38탄(2026-06-19, 1347 green): 🏁 rank 5 SYS-READ 완료 — PR-B `$fscanf`/`$sscanf`(`d2cac27`).** scanf 포맷 파서(`scan_run`)+첫 멀티 ref-write 인터셉트(N dst를 worker 내부서 write, count를 lhs 반환). `$fscanf`=fd 바이트 스트림(file_read_byte+pushback이 scanf unget 역할, over-read delimiter가 directive·call 간 생존)·`$sscanf`=string VALUE. 파서=whitespace-run(zero+)·literal char(정확매칭, mismatch=stop)·`%[*][width]`(d/h/x/o/b·%c=1글자 no-ws-skip·%s ws-delim); non-suppress 성공=dst[di] write+count, matching 실패=stop; return=count 또는 EOF시 -1(entry peek). **적대 리뷰(scanf 3렌즈 라이브)→HIGH 4+MEDIUM 2 수정**: ⓐwide값>64bit가 single-u64로 silent-zero/no-wrap → `scan_build_numeric`(decimal=128-bit wrap+부호확장, hex/oct/bin=per-digit arbitrary-width; 2^64+1→1, 19-hex→96bit dest) ⓑ%h/%o/%b가 4-state x/z/? 거부 → X/Z bit-group 수용+Verilog x-extension(`x`%h→all-X·`1x`→001x·`ff`→0-extend) ⓒ부호 +/-는 %d 한정(iverilog는 %h/%o/%b서 거부) ⓓ`%c`=정확히 1글자(width 무시), `%0d/%0c/%0s`=no-match(had_width 플래그) ⓔ pathological width=saturating(panic 방지) ⓕ-1/0 return=entry peek(빈 포맷·미지원-conv on 비빈소스→0). defer(문서화·safe): %f/%e/%g reals(vita는 clean stop)·mem[i]/part-select dest(loud). determinism/VM/staged 렌즈 CLEAN. ⭐교훈: 오라클 있어도 적대 리뷰가 다수 silent-wrong(wide값·4-state·부호·width-panic) 적발 — 라이브 차분이 유일 teeth. **rank 5 전체(writemem+fgetc/feof/ungetc+fgets+fread+fscanf/sscanf, 9 커밋)**. 다음=rank 6(`$dist_*`/`$cast`/`$monitoron/off`) 또는 N3.4. 

**39탄(2026-06-19, 1354 green, `63e7070`): N3.4 다차원 packed part-select read+write 완료 (사용자 선택).** `reg [3:0][7:0] x; x[3:2]`가 generic flat-bit 경로로 가 read=`3`(should `aabb`)·**write=no-op**(`12340000` 대신 `00000000`) silent-wrong(N3.3 적대 리뷰 적발 인접 pre-existing). 수정(순수 IR-0, elaborate lowering): `try_packed_part_select`(read, lower_expr)+`try_packed_part_select_lval`(write, collect_lval_chunks)가 bare 다차원 packed net(packed_dims·≥2 dims·const descending)의 part-select를 OUTER 차원 원소-범위로 — offset=`flatten_word([lsb])`(lsb element bit offset, 기존 packed 기계 재사용)·width=count×elem_w(elem_w=inner dims 곱)·`PartIdxUp`, `lower_packed_read` 미러. plain vector/ascending(loud E3009)/단일차원 packed-아님은 fall-through. iverilog 강한 오라클(x[3:2]=aabb·write 12340000·3-dim·non-zero-base `[5:2]`=1122/3344). **적대 3렌즈→HIGH 1 수정(N3.4가 NEW 유발)**: 상한-OOB `x[4:2]`(msb=4 > outer hi=3)가 lsb만 검증돼 net 너머 silent r/w(xxaabb/22330000) → `packed_outer_part_select`가 full [lsb:msb] range를 outer dim에 검증, OOB면 E3009 loud(iverilog 컴파일 거부 매칭, read=placeholder·write=POISON chunk). determinism/VM/staged/golden 렌즈 CLEAN. **신규 follow-on(pre-existing, git stash로 unchanged 확인, out-of-scope)**: array-of-packed 원소 part-select `qm[0][3:2]`(silent-wrong, N3.2 형제)·indexed-part `x[i+:2]` on multi-dim packed(silent-wrong)·nested range-then-index `x[3:2][0]`(iverilog 거부, vita 수용). ⭐교훈: part-select 헬퍼가 lsb만 검증하면 msb-OOB가 새 silent-wrong — full-range 검증 필수; 적대 리뷰가 fix-introduced 결함 적발. 다음=rank 6 또는 N3.4 follow-on. 

**40탄 N3.4 follow-on(2026-06-19, 1363 green, `24162ff`): 세 자매 케이스 완료(순수 IR-0, sim-ir/AST/schema/fmt_ver 8 무변경).** ①array-of-packed 원소 part-select `qm[i][msb:lsb]` r/w → OUTER 원소-범위(신규 `packed_ps_base`/`_lval`=base를 `(net,Option<elem_word>)`로 해소, `qm[0][3:2]`=aabb·write 12340000) ②const indexed part-select `x[c+:w]`/`x[c-:w]`(bare+array elem)→원소-범위 fold(신규 `try_packed_indexed_part(_lval)`+`packed_indexed_range`, `packed_outer_range`=공유 const-range core, offset=(l-olo)*elem_w value-identical; var offset `x[i+:2]`=loud=iverilog abort 오라클無) ③nested range-then-index `x[3:2][0]`/`v[3:0][0]`=loud(iverilog 보편 거부). 적대 3렌즈(라이브 iverilog)→**HIGH M1 수정**: `-:` low-boundary `x[0-:1]`/`x[1-:2]`(최저 원소=index 0)가 `c-w+1`의 `c-w` 선평가로 u32 underflow→debug panic → `(c+1)-w`(가드가 c+1≥w 보장, iverilog=dd/ccdd). det/VM/staged/golden CLEAN. **deferred(MEDIUM, 문서화·loud-never-wrong)**: ascending outer-dim packed part/indexed-part `reg [0:3][7:0]; x[0:1]`(iverilog=aabb)는 loud 유지=별개 ascending 슬라이스. 10 iverilog-pin 테스트. ⭐교훈: const-range 헬퍼의 `-:` 변환은 add-before-subtract(`(c+1)-w`)로 u32 underflow 회피; 적대 리뷰가 또 fix-introduced 경계 결함 적발. 다음=rank 6(`$dist_*`/`$cast`/`$monitoron/off`). 

**41탄 rank 6(2026-06-19, 1385 green, `53dde9c`): 🏁 Medium 묶음 완결 — `$dist_uniform`/`$cast`/`$monitoron-off`(순수 IR-0, v9 변수 기-존재, name→id+엔진만; sim-ir/fmt_ver 9 무변경).** `$dist_uniform`=Annex `uniform`/`rtl_dist_uniform`(pure f64, NO libm→3-OS deterministic AND iverilog-byte-identical; 0-seed→259341593; seeded direct-rhs intercept `SeededDist`/`k_dist_seeded`) — **4 transcendental `$dist_normal/exponential/poisson/chi_square`=DEFER**(libm log/sqrt/exp이 3-OS 비결정→iverilog-byte-identical과 양립불가, loud E3009). `$monitoron/off`=GLOBAL `Postponed.monitor_disabled`(re-`$monitor` 무영향·확립 print는 항상 발화·change-reprint만 게이트). `$cast`=hand-IEEE §6.24.2(iverilog 13.0 거부=오라클無; integral 항상 성공→1; func=direct-rhs `Cast`/`k_cast`, task=builtins `cast_task`). `emit_blocking_intercept`가 `$random` direct-rhs tail 공유(IR-0 byte-identical). 적대 3렌즈(라이브 iverilog)→**3 HIGH+1 MEDIUM 수정**: H1 `$cast` task-form이 non-plain dst·arity를 silent-drop(func은 loud)→`lower_systask` loud guards·H2+H3 monitor enable가 per-MonitorState(매 `$monitor`마다 true 리셋)+flush가 확립 print를 re-gate→global flag+확립 항상(monitoroff-before/around·same-tick 전부 iverilog byte-일치)·M1 `<32bit seed` silent truncate→loud E3009(iverilog 거부 매칭). det/golden(v9 unflip)/VM-parity/staged CLEAN. defer(문서화): `$dist_uniform` non-direct-rhs loud(=$random 선례)·transcendental dist. 22 테스트. ⭐교훈: monitor enable는 global이어야(per-state면 re-establish가 리셋)+확립 print는 enable과 독립(타임스텝 바인딩); task/func 쌍 기능은 양쪽 동일 loud guard 필수(적대 리뷰가 task-form silent-drop 적발). **🏁 Medium 묶음 rank 1-6 전부 완료.** 다음=大型 검증 트랙(N5 coverage·N7 class) 또는 잔여 저우선 후속.

**42탄 🏁 HIER-REST 트랙 완결(2026-06-19, 1470 green, 7커밋 `6ceadc2`→`2462efb`, 전부 순수 IR-0·fmt_ver 9 무변경).** 사용자 지시 "마감 위주 선진행 → HIER-REST 잔여 전부 진행". **(0) P0-NCW**(`6ceadc2`): net/계층 참조 비상수 width(`reg [sig-1:0]`/`[dut.W-1:0]`)가 `clamp_bound_u32(None)`→0으로 **silent width-1**(P0)→`check_const_range_bound`+`nonconst_bound_reason`(net/variable 단일-seg OR 다중-seg 계층 참조 포함 미접힘 bound=E3009 loud; const-fn 등 const-but-unfoldable은 false-loud 회피=함수 CALL descend 안 함). 3 사이트(`range_to_dims`/`packed_extents`/`array_dim_extents`). iverilog 차분 9/9. **(1) HIER-REST①+P0-NZE**(`0c560a6`): 계층 element/bit READ 3+seg(`m.l.mem[i]`=`hier_sel_chain` `==2`→`>=2` 일반화)+WRITE(`dut.mem[i]=`/NBA/런타임idx/2-D/packed, deferred lvalue `HIER_SEL_WRITE_SENTINEL_BASE`+`resolve_deferred_hier_sel_write`가 net shape로 chunk 재빌드). **⭐적대 워크플로우(4렌즈)+자체 프로빙→silent-wrong 2 적발·수정**: ⓐforce/release-on-hier-select가 placeholder(whole처럼 보임)로 `is_whole_single_net` 통과→bit-select rebuild로 R 01 silent-wrong→resolve서 force/release context는 loud(local parity) ⓑ**P0-NZE**=배열 원소 trailing bit-select가 비-0 LSB(`reg [11:4] mem`)·ascending에서 raw offset(silent-wrong)→local+hier 4사이트 `norm_offset_for_net`(0-base=no-op 골든 byte-identical). **(2) HIER-REST-PS**(`e06a16f`): 계층 part-select WRITE `dut.v[3:0]=`/`[o±:w]`/array-elem(`lval_hier_chain` 코어+`HierPart` deferred 확장, offset=net LSB 정규화). **(3) HIER-REST-MP**(`92ed4a5`): whole 다차원 packed r/w `dut.pm`(=flat 벡터, 가드서 `packed_dims` 항 제거; element select는 별도 deferred-sel 레인 무영향; whole 배열은 여전히 loud=iverilog `vpiMemory` 거부 parity). **(4) HIER-REST②**(`9f4d807`): indexed-segment `g[0].x`/`bank[3].c.r`/`g[0].u.r`. ⭐**핵심 통찰=AST 무변경**: generate 스코프가 `format!("{lbl}[{iter_val}]")`=`"g[0]"`로 키잉되므로, 파서가 상수 인덱스를 scope-seg 문자열 `"g[0]"`로 fold하면(`is_gen_scope_segment`="contains `[`") 기존 계층 resolver가 read/write 둘 다 해소→`.vu` 재핀 불요. expr+lvalue 양쪽 postfix `.` arm(이전엔 parse error). **(5) ② follow-up**(`2462efb`): **⭐최종 적대 워크플로우(4렌즈)→silent-wrong 1 적발·수정**=same-module generate 다차원 packed 원소 `g[0].pm[k]`가 resolvable이라 chain-detector(single-seg 게이트) 통과→**flat bit-select**(R 1, should ca)→4개 chain 함수(`expr_array_chain`/`expr_packed_chain`/`lval_*`)를 multi-seg join+lookup으로 일반화(same-module resolvable=원소/슬라이스, cross-instance 미-resolvable=deferred 레인 유지=무영향)→`g[0].mem[i]`도 loud→정확 업그레이드. +leading-zero `g[00]`=값 정규화. **신규 발견(미수정, P0-IPU)**: indexed part-select WRITE의 음수/LSB-미만 base offset(`v[-2+:4]`·`v[k-:3]` underflow)이 슬라이스 shift/drop→잘못된 비트(local+hier 일반 엔진 버그, PS는 local parity; 양수 top-overflow는 정확)→전용 슬라이스(ROADMAP §4.5 P0-IPU). 잔여 v1 한계(loud): non-literal generate idx `g[P].x`·ascending part-select write. ⭐교훈: ①**"loud follow-on으로 디퍼"가 silent-wrong을 가릴 수 있음**=same-module resolvable 다중-seg net은 single-seg 게이트 chain-detector를 빠져 flat bit-select로 묵묵 오역(loud 아님); 처음엔 `g[0].mem[i]`/packed 원소를 "loud 디퍼"로 가정했으나 적대 워크플로우 라이브 차분이 `g[0].pm[k]` silent-wrong 적발=유일 teeth. ②AST flip 회피=generate 스코프 키 문자열을 파서서 직접 합성(상수 인덱스 한정). ③force/release 가드는 placeholder가 whole처럼 보이는 deferred 경로서 resolve-time 재적용 필요. 적대 워크플로우 2회(8 에이전트, ~544k 토큰)가 silent-wrong 3건 견인. 다음=P0-IPU(신규·High) 또는 大型 검증 트랙(N5 coverage·N7 class).
